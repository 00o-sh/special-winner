{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Special Winner","text":"<p>A Kubernetes homelab cluster deployed with Talos Linux and Flux CD for GitOps-driven infrastructure management.</p> <p>Built on the onedr0p/cluster-template, this cluster uses makejinja for configuration templating and delivers a fully declarative, Git-managed infrastructure.</p>"},{"location":"#at-a-glance","title":"At a Glance","text":"Component Technology Version OS Talos Linux 1.13.0 Orchestration Kubernetes 1.36.1 GitOps Flux CD 2.8.6 CNI Cilium 1.19.3 Ingress Envoy Gateway v1.7.2 Secrets SOPS + Age 3.13.0 / 1.3.1 Identity Kanidm SSO/OAuth2 Packages Helm 4.1.4 (v4) Database CloudNative-PG PostgreSQL 17.7 Virtualization KubeVirt 1.7.0"},{"location":"#whats-deployed","title":"What's Deployed","text":"<p>60+ applications across 17 namespaces covering:</p> <ul> <li>Media -- Plex, Radarr, Sonarr, Prowlarr, Bazarr, qBittorrent, and more</li> <li>Virtualization -- KubeVirt with Debian, Ubuntu, and Windows VMs</li> <li>Identity -- Kanidm SSO with OAuth2 integrations</li> <li>Observability -- Prometheus, Grafana, Victoria Logs, Gatus, OpenCost, TeslaMate</li> <li>Databases -- PostgreSQL 17.7 HA cluster (3 instances) + MariaDB 11.7 Galera + Dragonfly</li> <li>Networking -- Cilium, Envoy Gateway, Cloudflare Tunnel, Multus, Macvtap</li> <li>Storage -- OpenEBS, VolSync, Garage S3, NFS</li> <li>CI/CD -- GitHub Actions runners, Forgejo with CI runners</li> <li>Utilities -- Homepage, Forgejo, SMTP relay, and more</li> </ul>"},{"location":"#quick-links","title":"Quick Links","text":"<ul> <li>Getting Started -- Set up the cluster from scratch</li> <li>Architecture -- Understand how everything fits together</li> <li>Applications -- Browse deployed applications</li> <li>Operations -- Day-2 operations and troubleshooting</li> <li>Development -- Template system and CI/CD</li> </ul>"},{"location":"architecture/","title":"Architecture","text":""},{"location":"architecture/#high-level-overview","title":"High-Level Overview","text":"<pre><code>graph TB\n    subgraph Internet\n        CF[Cloudflare Tunnel]\n    end\n\n    subgraph Cluster[\"Kubernetes Cluster (Talos Linux)\"]\n        subgraph GitOps[\"GitOps Layer\"]\n            Flux[Flux CD]\n            Git[GitHub Repository]\n        end\n\n        subgraph Networking[\"Networking Layer\"]\n            Cilium[Cilium CNI]\n            Envoy[Envoy Gateway]\n            DNS[CoreDNS + k8s_gateway]\n            Multus[Multus CNI]\n        end\n\n        subgraph Identity[\"Identity Layer\"]\n            Kanidm[Kanidm SSO]\n        end\n\n        subgraph Data[\"Data Layer\"]\n            PG[CloudNative-PG]\n            MDB[MariaDB Galera]\n            Dragonfly[Dragonfly]\n            OpenEBS[OpenEBS]\n            NFS[NFS Storage]\n        end\n\n        subgraph Apps[\"Application Layer\"]\n            Media[Media Stack]\n            VMs[KubeVirt VMs]\n            Utils[Utilities]\n            Obs[Observability]\n        end\n\n        subgraph Security[\"Security Layer\"]\n            SOPS[SOPS + Age]\n            ExtSec[External Secrets]\n            CertMgr[cert-manager]\n            kGuardian[kGuardian]\n        end\n    end\n\n    CF --&gt; Envoy\n    Git --&gt; Flux\n    Flux --&gt; Apps\n    Flux --&gt; Networking\n    Flux --&gt; Data\n    Flux --&gt; Security\n    Envoy --&gt; Apps\n    Kanidm --&gt; Apps\n    PG --&gt; Apps\n    MDB --&gt; Apps\n    Cilium --&gt; Envoy\n    Multus --&gt; VMs</code></pre>"},{"location":"architecture/#gitops-flow","title":"GitOps Flow","text":"<p>All cluster state is defined in Git. Changes flow through this pipeline:</p> <pre><code>sequenceDiagram\n    participant Dev as Developer\n    participant Git as GitHub\n    participant Flux as Flux CD\n    participant K8s as Kubernetes\n\n    Dev-&gt;&gt;Git: Push manifests\n    Git-&gt;&gt;Flux: Webhook / Poll\n    Flux-&gt;&gt;Flux: Reconcile Kustomizations\n    Flux-&gt;&gt;K8s: Apply HelmReleases\n    K8s-&gt;&gt;K8s: Deploy workloads\n    Flux-&gt;&gt;Git: Update status</code></pre>"},{"location":"architecture/#directory-layout","title":"Directory Layout","text":"<pre><code>special-winner/\n\u251c\u2500\u2500 .github/workflows/     # CI/CD pipelines\n\u251c\u2500\u2500 .taskfiles/            # Task automation (bootstrap, talos, template, vm)\n\u251c\u2500\u2500 bootstrap/             # Initial cluster bootstrap (Helmfile, SOPS secrets)\n\u251c\u2500\u2500 kubernetes/\n\u2502   \u251c\u2500\u2500 apps/              # Application manifests (17 namespaces)\n\u2502   \u251c\u2500\u2500 components/        # Shared components (alerts, nfs-scaler, volsync, sops)\n\u2502   \u2514\u2500\u2500 flux/              # Flux configuration (root Kustomization)\n\u251c\u2500\u2500 talos/                 # Talos Linux config and patches\n\u251c\u2500\u2500 templates/             # Jinja2 templates for config generation\n\u251c\u2500\u2500 scripts/               # Bootstrap and utility scripts\n\u251c\u2500\u2500 docs/                  # This documentation\n\u251c\u2500\u2500 mkdocs.yml             # Documentation site config\n\u251c\u2500\u2500 Taskfile.yaml          # Root task runner\n\u251c\u2500\u2500 .mise.toml             # Development tool versions\n\u251c\u2500\u2500 .sops.yaml             # SOPS encryption rules\n\u2514\u2500\u2500 makejinja.toml         # Template rendering config\n</code></pre>"},{"location":"architecture/#application-structure","title":"Application Structure","text":"<p>Every application follows a consistent pattern:</p> <pre><code>kubernetes/apps/&lt;namespace&gt;/&lt;app-name&gt;/\n\u251c\u2500\u2500 app/\n\u2502   \u251c\u2500\u2500 helmrelease.yaml       # Helm chart deployment\n\u2502   \u251c\u2500\u2500 ocirepository.yaml     # OCI chart source\n\u2502   \u251c\u2500\u2500 secret.sops.yaml       # Encrypted secrets (if needed)\n\u2502   \u2514\u2500\u2500 kustomization.yaml     # Manifest aggregation\n\u2514\u2500\u2500 ks.yaml                    # Flux Kustomization\n</code></pre>"},{"location":"architecture/#namespace-map","title":"Namespace Map","text":"Namespace Purpose Key Apps <code>actions-runner-system</code> CI/CD runners Actions Runner Controller <code>cert-manager</code> TLS certificates cert-manager <code>database</code> Database services CloudNative-PG, MariaDB Galera, Dragonfly, DBGate <code>default</code> Test workloads echo, LibreSpeed <code>external-secrets</code> Secret management External Secrets, 1Password <code>flux-system</code> GitOps Flux Operator, Flux Instance <code>forgejo-runner-system</code> Forgejo CI/CD Forgejo Runner (ScaledJob) <code>identity</code> SSO/Identity Kanidm (OAuth2 for 5+ apps) <code>kube-system</code> Core services Cilium, CoreDNS, Spegel, kGuardian <code>kubevirt</code> Virtualization KubeVirt, CDI, VMs <code>media</code> Media apps Plex, *arr stack, qBittorrent <code>network</code> Network infra Envoy Gateway, Error Pages, Cloudflare, Multus <code>observability</code> Monitoring Prometheus, Grafana, Victoria Logs <code>openebs-system</code> Block storage OpenEBS <code>system-upgrade</code> Upgrades Tuppr <code>utils</code> Utilities Forgejo, Homepage, Penpot, SMTP <code>volsync-system</code> Backups VolSync, Garage, Kopia"},{"location":"architecture/#network-architecture","title":"Network Architecture","text":"<ul> <li>Cilium provides eBPF-based CNI with advanced network policies</li> <li>Envoy Gateway handles HTTP routing with internal and external gateways, with custom error pages via responseOverride redirects</li> <li>Cloudflare Tunnel provides secure external access without port forwarding</li> <li>Multus + Macvtap gives VMs direct network access with dedicated MAC addresses</li> <li>k8s_gateway provides split-horizon DNS for internal service resolution</li> </ul>"},{"location":"architecture/#storage-architecture","title":"Storage Architecture","text":"Storage Class Backend Use Case <code>openebs-hostpath</code> OpenEBS Database volumes, high-performance local <code>nfs-fast</code> CSI Driver NFS VM disks, shared media, ReadWriteMany Garage S3 Garage Backup destination for VolSync/Kopia"},{"location":"architecture/#secret-flow","title":"Secret Flow","text":"<pre><code>graph LR\n    A[1Password] --&gt;|ExternalSecrets| B[Kubernetes Secrets]\n    C[age.key] --&gt;|SOPS| D[Encrypted YAML in Git]\n    D --&gt;|Flux Decryption| B\n    B --&gt; E[Applications]</code></pre> <ul> <li>SOPS + Age: Encrypts secrets in Git (<code>*.sops.yaml</code> files)</li> <li>External Secrets Operator: Pulls secrets from 1Password at runtime</li> <li>Flux: Automatically decrypts SOPS secrets during reconciliation</li> </ul>"},{"location":"applications/","title":"Applications","text":"<p>All applications are deployed via Flux CD from manifests in <code>kubernetes/apps/</code>.</p>"},{"location":"applications/#application-catalog","title":"Application Catalog","text":""},{"location":"applications/#media-stack","title":"Media Stack","text":"App Description Namespace Plex Media server media Radarr Movie collection manager media Sonarr TV series collection manager media Prowlarr Indexer manager media Bazarr Subtitle management media qBittorrent Torrent client media Qui qBittorrent web UI media Autobrr Torrent tracker automation media Recyclarr Quality profile management media Seerr Media request platform media Tautulli Plex monitoring media FlareSolverr Cloudflare bypass media TheLounge IRC client media"},{"location":"applications/#voip-telephony","title":"VoIP &amp; Telephony","text":"App Description Namespace FreePBX Telephony platform (container + VMs) voip"},{"location":"applications/#networking-testing","title":"Networking &amp; Testing","text":"App Description Namespace LibreSpeed Multi-path speed test default"},{"location":"applications/#infrastructure-utilities","title":"Infrastructure &amp; Utilities","text":"App Description Namespace Forgejo Self-hosted Git utils Homepage Cluster dashboard utils n8n Workflow automation utils Penpot Design platform utils SMTP Relay Email relay (Maddy) utils DBGate Database web UI database"},{"location":"applications/#virtualization","title":"Virtualization","text":"App Description Namespace KubeVirt VM operator kubevirt CDI Disk importer kubevirt KubeVirt Manager VM web UI kubevirt"},{"location":"applications/#observability","title":"Observability","text":"App Description Namespace Grafana Metrics visualization observability Prometheus Metrics collection observability Victoria Logs Log aggregation observability Gatus Health monitoring observability OpenCost Cost analysis observability Fluent Bit Log forwarding observability KEDA Event-driven autoscaling observability"},{"location":"applications/#low-code-platform","title":"Low-Code Platform","text":"App Description Namespace UI Bakery Internal tool builder ui-bakery"},{"location":"applications/#cicd","title":"CI/CD","text":"App Description Namespace Actions Runner Controller GitHub Actions runners (special-winner + ambersecurityinc scale sets) actions-runner-system Forgejo Runner Forgejo CI/CD runners forgejo-runner-system <p>See the detailed pages for more on each category:</p> <ul> <li>Media Stack</li> <li>FreePBX</li> <li>Virtualization</li> <li>Observability</li> <li>Utilities</li> <li>LibreSpeed</li> </ul>"},{"location":"applications/freepbx/","title":"FreePBX","text":"<p>FreePBX is a containerized telephony platform deployed in the <code>voip</code> namespace with a MariaDB database backend.</p>"},{"location":"applications/freepbx/#architecture","title":"Architecture","text":"<p>FreePBX runs as two complementary deployment models:</p> <ol> <li>Containerized instance (<code>b1-k3s01</code>) -- Kubernetes-native deployment via Helm chart in the <code>voip</code> namespace</li> <li>KubeVirt VM instances (<code>b1-k3s01</code>, <code>b2-k3s01</code>, <code>b3-k3s01</code>) -- Full Debian 12 VMs running FreePBX with Cloudflare Tunnel access</li> </ol>"},{"location":"applications/freepbx/#container-deployment","title":"Container Deployment","text":"<pre><code>kubernetes/apps/voip/freepbx/\n\u251c\u2500\u2500 app/\n\u2502   \u251c\u2500\u2500 helmrelease.yaml       # FreePBX container deployment\n\u2502   \u251c\u2500\u2500 ocirepository.yaml     # OCI chart source (app-template)\n\u2502   \u251c\u2500\u2500 externalsecret.yaml    # 1Password DB credentials\n\u2502   \u2514\u2500\u2500 kustomization.yaml\n\u251c\u2500\u2500 database/\n\u2502   \u251c\u2500\u2500 database.yaml          # MariaDB databases (b1_asterisk, b1_asteriskcdrdb)\n\u2502   \u251c\u2500\u2500 user.yaml              # MariaDB user (freepbx)\n\u2502   \u251c\u2500\u2500 grant.yaml             # Database permissions\n\u2502   \u251c\u2500\u2500 externalsecret.yaml    # DB password from 1Password\n\u2502   \u2514\u2500\u2500 kustomization.yaml\n\u2514\u2500\u2500 ks.yaml                    # Flux Kustomizations\n</code></pre>"},{"location":"applications/freepbx/#configuration","title":"Configuration","text":"Setting Value Image <code>ghcr.io/00o-sh/fpbx-docker:latest</code> Helm chart <code>bjw-s-labs/app-template</code> (OCI, v4.6.2) CPU request 100m Memory request 512Mi Memory limit 2Gi URL <code>freepbx.00o.sh</code>"},{"location":"applications/freepbx/#service-ports","title":"Service Ports","text":"Port Protocol Purpose 80 HTTP Web interface 443 HTTPS Secure web interface 8001 HTTP Unified Communications Portal (UCP) 8003 HTTP Admin interface"},{"location":"applications/freepbx/#persistence","title":"Persistence","text":"Mount Size Storage Class <code>/etc/asterisk</code> 1Gi openebs-hostpath <code>/var/lib/asterisk</code> 5Gi openebs-hostpath <code>/var/spool/asterisk</code> 5Gi openebs-hostpath <code>/var/log/asterisk</code> emptyDir --"},{"location":"applications/freepbx/#database","title":"Database","text":"<p>FreePBX uses the MariaDB Galera cluster in the <code>database</code> namespace. Database resources are managed via MariaDB Operator CRs.</p>"},{"location":"applications/freepbx/#databases","title":"Databases","text":"Database Purpose <code>b1_asterisk</code> Main Asterisk configuration <code>b1_asteriskcdrdb</code> Call Detail Records (CDR) <p>Both databases use <code>utf8mb4</code> character set with <code>utf8mb4_unicode_ci</code> collation.</p>"},{"location":"applications/freepbx/#connection-details","title":"Connection Details","text":"<pre><code>Host: mariadb-primary.database.svc.cluster.local\nPort: 3306\nUser: freepbx\nMax connections: 100\n</code></pre>"},{"location":"applications/freepbx/#managed-resources","title":"Managed Resources","text":"<p>The database setup uses MariaDB Operator custom resources:</p> <ul> <li>Database -- creates <code>b1_asterisk</code> and <code>b1_asteriskcdrdb</code></li> <li>User -- creates the <code>freepbx</code> user with password from 1Password</li> <li>Grant -- grants <code>ALL PRIVILEGES</code> on both databases</li> </ul> <p>All resources have <code>cleanupPolicy: Skip</code> to prevent accidental data loss on removal.</p>"},{"location":"applications/freepbx/#kubevirt-vm-instances","title":"KubeVirt VM Instances","text":"<p>Three FreePBX VMs run on KubeVirt for full-OS telephony deployments with Cloudflare Tunnel access.</p> <pre><code>kubernetes/apps/kubevirt/virtualmachines/freepbx/\n\u251c\u2500\u2500 b1-k3s01/       # Instance 1\n\u251c\u2500\u2500 b2-k3s01/       # Instance 2\n\u2514\u2500\u2500 b3-k3s01/       # Instance 3\n</code></pre>"},{"location":"applications/freepbx/#vm-specifications","title":"VM Specifications","text":"Instance IP MAC CPU RAM Storage b1-k3s01 192.168.57.14 52:54:00:57:00:14 2 4Gi 50Gi NFS b2-k3s01 192.168.57.15 52:54:00:57:00:15 2 4Gi 50Gi NFS b3-k3s01 192.168.57.16 52:54:00:57:00:16 2 4Gi 50Gi NFS"},{"location":"applications/freepbx/#vm-features","title":"VM Features","text":"<ul> <li>Base image: Debian 12 (containerdisks)</li> <li>Network: Macvtap with static IP (direct L2 access)</li> <li>Live migration: Enabled (NFS storage + evictionStrategy)</li> <li>DNS: <code>{instance}.00o.sh</code> via external-dns</li> <li>Cloud-init provisioning:<ul> <li>FreePBX installation via <code>sng_freepbx_debian_install.sh</code></li> <li>Cloudflare Tunnel (<code>cloudflared</code>) for secure external access</li> <li>SSH key and user account setup</li> <li>qemu-guest-agent for VM management</li> </ul> </li> </ul>"},{"location":"applications/freepbx/#vm-management","title":"VM Management","text":"<pre><code># Via virtctl\nvirtctl console freepbx-b1-k3s01\nvirtctl start freepbx-b1-k3s01\nvirtctl stop freepbx-b1-k3s01\nvirtctl migrate freepbx-b1-k3s01\n\n# Via task runner\ntask vm:console VM=freepbx-b1-k3s01\ntask vm:start VM=freepbx-b1-k3s01\ntask vm:stop VM=freepbx-b1-k3s01\n</code></pre>"},{"location":"applications/freepbx/#dependencies","title":"Dependencies","text":""},{"location":"applications/freepbx/#container-deployment_1","title":"Container Deployment","text":"<ul> <li>MariaDB Galera cluster (<code>mariadb-cluster</code> Kustomization)</li> <li>1Password ExternalSecrets (<code>onepassword</code> Kustomization)</li> <li>Envoy Gateway (<code>envoy-external</code> in network namespace)</li> </ul>"},{"location":"applications/freepbx/#vm-deployment","title":"VM Deployment","text":"<ul> <li>KubeVirt operator and CDI</li> <li>Macvtap CNI plugin (network namespace)</li> <li>1Password ExternalSecrets (Cloudflare Tunnel token)</li> </ul>"},{"location":"applications/freepbx/#flux-dependency-chain","title":"Flux Dependency Chain","text":"<pre><code>graph TD\n    A[mariadb-cluster] --&gt; B[freepbx-database]\n    C[onepassword] --&gt; B\n    B --&gt; D[freepbx app]\n    C --&gt; D</code></pre> <p>The database resources are created in the <code>database</code> namespace first, then the FreePBX container deploys in the <code>voip</code> namespace once the databases are ready.</p>"},{"location":"applications/librespeed/","title":"LibreSpeed","text":"<p>LibreSpeed is a self-hosted speed test deployed with multiple test paths to isolate network bottlenecks at each layer of the stack.</p> <ul> <li>Located in <code>kubernetes/apps/default/librespeed/</code></li> <li>Image: <code>ghcr.io/librespeed/speedtest:5.5.1</code></li> <li>Namespace: <code>default</code></li> <li>Frontend: <code>speed.${SECRET_DOMAIN}</code></li> </ul>"},{"location":"applications/librespeed/#architecture","title":"Architecture","text":"<p>LibreSpeed runs as 5 separate controllers (pods), each serving a different network path:</p> <pre><code>                                    \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n                                    \u2502  speed.${SECRET_DOMAIN}      \u2502\n                                    \u2502  Frontend (server picker UI) \u2502\n                                    \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n                                                   \u2502\n                    \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n                    \u25bc              \u25bc               \u25bc               \u25bc              \u2502\n              \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510 \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510  \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510  \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510        \u2502\n              \u2502 Cloudflare\u2502 \u2502Port Fwd'd \u2502  \u2502Direct Envoy\u2502  \u2502 LAN Direct \u2502        \u2502\n              \u2502 speed-cf  \u2502 \u2502speed-prtfw\u2502  \u2502speed-direct\u2502  \u2502 speed-lan  \u2502        \u2502\n              \u2514\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2518 \u2514\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2518  \u2514\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2518  \u2514\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2518        \u2502\n                    \u2502             \u2502               \u2502               \u2502              \u2502\n  TLS by:     Cloudflare+    Envoy Ext      Envoy Int       Apache (pod)    Envoy Ext\n              Envoy Ext                                                     (frontend)\n                    \u2502             \u2502               \u2502               \u2502\n  Path:       CF CDN \u2192       Router \u2192        Envoy Int \u2192    Cilium LB \u2192\n              Router \u2192       Envoy Ext \u2192     Pod            Pod (8443)\n              Envoy Ext \u2192    Pod\n              Pod\n</code></pre>"},{"location":"applications/librespeed/#controllers","title":"Controllers","text":"Controller Mode Gateway TLS Description <code>speed-cf</code> backend envoy-external Cloudflare + Envoy Full Cloudflare proxy path <code>speed-portfwd</code> backend envoy-external Envoy only Port forwarded, CF proxy disabled <code>speed-direct</code> backend envoy-internal Envoy LAN only, through Envoy <code>speed-lan</code> backend Cilium LB (10.0.6.16) Apache mod_ssl LAN only, no proxy <code>speed-frontend</code> frontend envoy-external Cloudflare + Envoy Server picker UI <p>All backend controllers run <code>ghcr.io/librespeed/speedtest:5.5.1</code> in <code>MODE=backend</code>. The frontend runs in <code>MODE=frontend</code> with a <code>servers.json</code> ConfigMap listing all backends.</p>"},{"location":"applications/librespeed/#test-paths","title":"Test Paths","text":"Server Route TLS Terminated By Reachable From Measures Cloudflare <code>envoy-external</code> (CF proxied) Cloudflare + Envoy Internet + LAN Full CF proxy overhead Port Forwarded <code>envoy-external</code> (CF proxy off) Envoy Internet + LAN Raw internet + Envoy overhead Direct (Envoy) <code>envoy-internal</code> Envoy LAN only Envoy proxy overhead in isolation LAN Direct Cilium LoadBalancer (10.0.6.16) Apache mod_ssl (in-pod) LAN only Raw network baseline, no proxy"},{"location":"applications/librespeed/#interpreting-results","title":"Interpreting Results","text":"<ul> <li>LAN Direct vs Direct (Envoy): Difference = Envoy proxy overhead</li> <li>Direct (Envoy) vs Port Forwarded: Difference = NAT/routing overhead</li> <li>Port Forwarded vs Cloudflare: Difference = Cloudflare CDN overhead</li> <li>LAN Direct: Your maximum achievable throughput (hardware baseline)</li> </ul>"},{"location":"applications/librespeed/#hostnames","title":"Hostnames","text":"Hostname Purpose <code>speed.${SECRET_DOMAIN}</code> Frontend UI (server picker) <code>speed-cf.${SECRET_DOMAIN}</code> Backend via Cloudflare <code>speed-portfwd.${SECRET_DOMAIN}</code> Backend via port forward <code>speed-direct.${SECRET_DOMAIN}</code> Backend via Envoy internal <code>speed-lan.${SECRET_DOMAIN}</code> Backend via Cilium LB (no Envoy)"},{"location":"applications/librespeed/#envoy-performance-tuning","title":"Envoy Performance Tuning","text":"<p>A route-specific <code>BackendTrafficPolicy</code> is applied to the speed test backend routes (<code>speed-cf</code>, <code>speed-direct</code>, <code>speed-portfwd</code>) that overrides the global gateway policy. This allows aggressive throughput settings without affecting other applications.</p>"},{"location":"applications/librespeed/#per-route-backendtrafficpolicy","title":"Per-Route BackendTrafficPolicy","text":"Setting Global Policy Speed Test Policy Why Compression Brotli + Gzip Disabled Speed test data is random; compression wastes CPU for 0% savings Response Override Error page redirects Disabled Skip error page matching on every response Backend Buffer 8Mi 16Mi More buffer for high-throughput data transfers Circuit Breakers 1024 (default) 4096 Remove connection/request concurrency limits Backend Protocol Client protocol HTTP/1.1 Avoids HTTP/2 framing overhead to backend pods <p>How per-route policies work</p> <p>When a <code>BackendTrafficPolicy</code> targets an <code>HTTPRoute</code>, it overrides the gateway-level policy for that route only. Other routes continue using the global policy with compression, error pages, etc.</p>"},{"location":"applications/librespeed/#global-clienttrafficpolicy","title":"Global ClientTrafficPolicy","text":"<p>These settings apply to all traffic through the same gateways (cannot be scoped per-route):</p> Setting Value Notes HTTP/2 Stream Window 512Ki 8x default; limits per-stream in-flight data HTTP/2 Connection Window 8Mi Shared across all streams on a connection Client Buffer 4Mi Client-side receive buffer HTTP/3 Enabled QUIC when supported by client <p>ClientTrafficPolicy limitation</p> <p><code>ClientTrafficPolicy</code> can only target <code>Gateway</code> resources, not individual <code>HTTPRoute</code> resources. The most granular targeting is per-listener via <code>sectionName</code>. This means client-side tuning (HTTP/2 windows, HTTP/3) affects all routes on that listener.</p>"},{"location":"applications/librespeed/#if-envoy-still-limits-throughput","title":"If Envoy Still Limits Throughput","text":"<p>The LAN Direct test bypasses Envoy entirely. If you see a large gap between LAN Direct and Direct (Envoy), consider increasing the global HTTP/2 windows in <code>kubernetes/apps/network/envoy-gateway/app/envoy.yaml</code>:</p> <pre><code># ClientTrafficPolicy\nhttp2:\n  initialStreamWindowSize: 2Mi      # currently 512Ki\n  initialConnectionWindowSize: 16Mi  # currently 8Mi\n</code></pre> <p>Warning</p> <p>Increasing these values globally affects all services. Larger windows use more memory per connection across all Envoy pods.</p>"},{"location":"applications/librespeed/#lan-direct-tls-setup","title":"LAN Direct TLS Setup","text":"<p>The LAN Direct server terminates TLS at the pod level (no Envoy) using a Let's Encrypt certificate provisioned by cert-manager:</p> <ul> <li>Certificate: <code>speed-lan-tls</code> (issued for <code>speed-lan.${SECRET_DOMAIN}</code>)</li> <li>Issuer: <code>letsencrypt-production</code> ClusterIssuer (DNS-01 via Cloudflare)</li> <li>Algorithm: ECDSA</li> <li>Duration: 160 hours</li> <li>Flux dependency: <code>cert-manager</code> \u2192 <code>librespeed-cert</code> (waits for cert readiness) \u2192 <code>librespeed</code></li> <li>Apache: <code>mod_ssl</code> enabled via command override, SSL VirtualHost on port 8443</li> <li>Service: Cilium LoadBalancer maps <code>443 \u2192 8443</code></li> </ul>"},{"location":"applications/librespeed/#adding-external-speed-test-servers","title":"Adding External Speed Test Servers","text":"<p>To add external servers (e.g., a LibreSpeed instance on a VPS), add entries to the <code>servers.json</code> ConfigMap in <code>helmrelease.yaml</code>:</p> <pre><code>{\"name\": \"VPS (NYC)\", \"server\": \"https://speedtest.example.com/\", \"dlURL\": \"garbage.php\", \"ulURL\": \"empty.php\", \"pingURL\": \"empty.php\", \"getIpURL\": \"getIP.php\"}\n</code></pre>"},{"location":"applications/librespeed/#requirements-for-external-servers","title":"Requirements for External Servers","text":"<ul> <li>Must run LibreSpeed in <code>backend</code> or <code>standalone</code> mode</li> <li>Must be HTTPS (mixed content blocked from HTTPS frontend)</li> <li>Must allow CORS from <code>speed.${SECRET_DOMAIN}</code> (or use <code>*</code>)</li> <li>For <code>MODE=backend</code>: endpoints are at root (<code>/garbage.php</code>)</li> <li>For <code>MODE=standalone</code>: endpoints are under <code>/backend/garbage.php</code></li> </ul>"},{"location":"applications/librespeed/#external-accessibility","title":"External Accessibility","text":"<p>When accessed from the internet, only Cloudflare and Port Forwarded servers are reachable. Direct (Envoy) and LAN Direct resolve to internal IPs and will fail the pre-test ping. LibreSpeed shows them as unavailable -- this is expected.</p>"},{"location":"applications/librespeed/#file-structure","title":"File Structure","text":"<pre><code>librespeed/\n\u251c\u2500\u2500 app/\n\u2502   \u251c\u2500\u2500 backendtrafficpolicy.yaml  # Per-route Envoy tuning (no compression)\n\u2502   \u251c\u2500\u2500 helmrelease.yaml           # All controllers, services, routes, configs\n\u2502   \u251c\u2500\u2500 kustomization.yaml\n\u2502   \u2514\u2500\u2500 ocirepository.yaml\n\u251c\u2500\u2500 cert/\n\u2502   \u251c\u2500\u2500 certificate.yaml           # cert-manager Certificate for LAN TLS\n\u2502   \u2514\u2500\u2500 kustomization.yaml\n\u251c\u2500\u2500 ks.yaml                        # Flux Kustomizations (cert \u2192 app dependency)\n\u2514\u2500\u2500 README.md\n</code></pre>"},{"location":"applications/librespeed/#dependencies","title":"Dependencies","text":"<ul> <li>cert-manager: Provides the <code>speed-lan-tls</code> certificate for the LAN Direct path</li> <li>Envoy Gateway: Routes for Cloudflare, Port Forwarded, and Direct paths</li> <li>Cilium: LoadBalancer service for LAN Direct path (IP: 10.0.6.16)</li> <li>External DNS: Automatic DNS record creation for all hostnames</li> </ul>"},{"location":"applications/librespeed/#resources","title":"Resources","text":"<p>Each controller runs with:</p> <pre><code>requests:\n  cpu: 10m\n  memory: 64Mi\nlimits:\n  memory: 256Mi\n</code></pre> <p>5 controllers total = 50m CPU request, 320Mi memory request, 1280Mi memory limit.</p>"},{"location":"applications/media/","title":"Media Stack","text":"<p>The media namespace contains 13 applications for media management and consumption.</p>"},{"location":"applications/media/#architecture","title":"Architecture","text":"<pre><code>graph LR\n    Seerr[Seerr&lt;br/&gt;Requests] --&gt; Radarr\n    Seerr --&gt; Sonarr\n    Prowlarr[Prowlarr&lt;br/&gt;Indexers] --&gt; Radarr\n    Prowlarr --&gt; Sonarr\n    Radarr[Radarr&lt;br/&gt;Movies] --&gt; qBit[qBittorrent]\n    Sonarr[Sonarr&lt;br/&gt;TV] --&gt; qBit\n    Autobrr[Autobrr&lt;br/&gt;Automation] --&gt; qBit\n    qBit --&gt; Plex[Plex&lt;br/&gt;Media Server]\n    Bazarr[Bazarr&lt;br/&gt;Subtitles] --&gt; Radarr\n    Bazarr --&gt; Sonarr\n    Tautulli[Tautulli] --&gt; Plex\n    FlareSolverr[FlareSolverr] --&gt; Prowlarr</code></pre>"},{"location":"applications/media/#storage-pattern","title":"Storage Pattern","text":"<p>All media apps follow a consistent three-tier storage strategy:</p> Mount Type Storage Class Access Mode Purpose <code>/config</code> PVC <code>openebs-hostpath</code> ReadWriteOnce App config, databases (backed up via VolSync) <code>/media</code> NFS External NAS ReadWriteMany Shared media library <code>/tmp</code> emptyDir \u2014 \u2014 Ephemeral scratch space <p>NFS media mount:</p> <ul> <li>Server: <code>nas.3226texas.com</code></li> <li>Path: <code>/mnt/Rust/Media</code></li> <li>Container path: <code>/media</code></li> <li>Plex mounts as read-only; arr apps mount as read-write*</li> </ul>"},{"location":"applications/media/#security-context","title":"Security Context","text":"<p>All media apps run with a standard security profile:</p> <pre><code>securityContext:\n  runAsNonRoot: true\n  runAsUser: 1000\n  runAsGroup: 1000\n  fsGroup: 1000\n  allowPrivilegeEscalation: false\n  readOnlyRootFilesystem: true\n  capabilities:\n    drop: [\"ALL\"]\n</code></pre>"},{"location":"applications/media/#applications","title":"Applications","text":""},{"location":"applications/media/#plex","title":"Plex","text":"<p>Media server for streaming movies, TV shows, music, and photos.</p> <ul> <li>LoadBalancer service at <code>10.0.6.14</code></li> <li>Exposed at <code>plex.00o.sh</code> and <code>plex-direct.00o.sh</code></li> <li>Resources: 100m CPU, 256Mi\u20132Gi memory</li> <li>NFS media mounted read-only</li> </ul>"},{"location":"applications/media/#radarr-sonarr","title":"Radarr &amp; Sonarr","text":"<p>Movie and TV series collection managers:</p> <ul> <li>Automate downloading and organizing media</li> <li>Integration with Prowlarr for indexer management</li> <li>Quality profiles managed by Recyclarr</li> <li>External authentication via Kanidm</li> <li>Resources: 100m CPU, 1Gi\u20134Gi memory</li> </ul>"},{"location":"applications/media/#prowlarr","title":"Prowlarr","text":"<p>Centralized indexer manager for all *arr applications:</p> <ul> <li>Uses FlareSolverr for Cloudflare-protected sites</li> <li>Syncs indexers to Radarr and Sonarr</li> </ul>"},{"location":"applications/media/#qbittorrent-qui","title":"qBittorrent + Qui","text":"<p>Torrent client with a custom web UI:</p> <ul> <li>qBittorrent handles downloads</li> <li>Qui provides a modern web interface</li> </ul>"},{"location":"applications/media/#autobrr","title":"Autobrr","text":"<p>Automation for torrent trackers:</p> <ul> <li>Monitors IRC announces and RSS feeds</li> <li>Sends releases to *arr apps or directly to qBittorrent</li> </ul>"},{"location":"applications/media/#seerr","title":"Seerr","text":"<p>Media request and discovery platform:</p> <ul> <li>Users can request movies and TV shows</li> <li>Integrates with Radarr and Sonarr for automatic fulfillment</li> </ul>"},{"location":"applications/media/#bazarr","title":"Bazarr","text":"<p>Subtitle management:</p> <ul> <li>Automatically downloads subtitles for movies and TV shows</li> <li>Integrates with Radarr and Sonarr</li> </ul>"},{"location":"applications/media/#supporting-services","title":"Supporting Services","text":"<ul> <li>Recyclarr \u2014 Syncs quality profiles to *arr apps from TRaSH Guides</li> <li>Tautulli \u2014 Plex monitoring and statistics</li> <li>FlareSolverr \u2014 Cloudflare bypass proxy for Prowlarr</li> <li>TheLounge \u2014 Self-hosted IRC client for tracker communication</li> </ul>"},{"location":"applications/media/#nfs-dependency","title":"NFS Dependency","text":"<p>Media apps that mount NFS volumes should include the NFS-scaler component to prevent crash-loops when NFS is unavailable:</p> <pre><code># In the app's ks.yaml\nspec:\n  components:\n    - name: nfs-scaler\n</code></pre> <p>The NFS-scaler uses KEDA to monitor <code>probe_success{instance=~\".+:2049\"}</code> and scales the deployment to 0 when NFS is down.</p>"},{"location":"applications/media/#adding-a-new-media-app","title":"Adding a New Media App","text":"<ol> <li>Create directory: <code>kubernetes/apps/media/&lt;app-name&gt;/app/</code></li> <li>Follow the standard HelmRelease pattern with NFS media mount</li> <li>Use UID/GID 1000 in security context (matches NFS permissions)</li> <li>Add NFS-scaler component if the app mounts NFS</li> <li>Add VolSync component for config backup</li> <li>Add to Homepage dashboard</li> <li>Update the namespace kustomization</li> </ol>"},{"location":"applications/observability/","title":"Observability","text":"<p>The observability namespace provides comprehensive monitoring, logging, and alerting.</p>"},{"location":"applications/observability/#stack-overview","title":"Stack Overview","text":"<pre><code>graph TB\n    subgraph Collection\n        FB[Fluent Bit] --&gt;|logs| VL[Victoria Logs]\n        BB[Blackbox Exporter] --&gt;|probes| Prom[Prometheus]\n        SM[ServiceMonitors] --&gt;|metrics| Prom\n    end\n\n    subgraph Visualization\n        Prom --&gt; Grafana\n        VL --&gt; Grafana\n    end\n\n    subgraph Alerting\n        Prom --&gt; AM[AlertManager]\n        AM --&gt; Discord\n        AM --&gt; GitHub[GitHub Status]\n    end\n\n    subgraph Health\n        Gatus[Gatus] --&gt;|uptime| Grafana\n    end\n\n    subgraph Scaling\n        Prom --&gt; KEDA\n    end\n\n    subgraph Cost\n        OC[OpenCost] --&gt; Prom\n    end</code></pre>"},{"location":"applications/observability/#components","title":"Components","text":""},{"location":"applications/observability/#kube-prometheus-stack","title":"kube-prometheus-stack","text":"<p>The foundation of the monitoring stack:</p> <ul> <li>Prometheus \u2014 Metrics collection and storage</li> <li>AlertManager \u2014 Alert routing and notification (at <code>alertmanager.00o.sh</code>)</li> <li>Grafana \u2014 Dashboards and visualization (managed by Grafana Operator)</li> <li>Pre-configured with Kubernetes dashboards</li> </ul> <p>Prometheus configuration:</p> Setting Value Retention 14 days Retention size 50 GB Storage 50Gi on <code>nfs-fast</code> Memory limit 2000Mi"},{"location":"applications/observability/#grafana-dashboards","title":"Grafana Dashboards","text":"<p>Pre-configured dashboards auto-imported from Grafana.com:</p> Dashboard Grafana ID Purpose cilium-agent 16611 Cilium agent metrics cilium-operator 16612 Cilium operator health kubernetes-api-server 15761 API server performance kubernetes-coredns 15762 CoreDNS metrics kubernetes-global 15757 Cluster-wide overview kubernetes-namespaces 15758 Per-namespace resources kubernetes-nodes 15759 Node performance kubernetes-pods 15760 Pod utilization kubernetes-volumes 11454 Persistent volume metrics node-exporter-full 1860 Full node system metrics prometheus 19105 Prometheus self-monitoring"},{"location":"applications/observability/#victoria-logs","title":"Victoria Logs","text":"<p>Log aggregation and search:</p> <ul> <li>Receives logs from Fluent Bit</li> <li>Grafana datasource for log querying</li> <li>Lower resource usage than Elasticsearch/Loki</li> </ul>"},{"location":"applications/observability/#fluent-bit","title":"Fluent Bit","text":"<p>Log forwarding and collection:</p> <ul> <li>Collects logs from all pods via DaemonSet</li> <li>Forwards to Victoria Logs</li> <li>Lightweight with minimal resource overhead</li> </ul>"},{"location":"applications/observability/#gatus","title":"Gatus","text":"<p>Health monitoring and uptime tracking:</p> <ul> <li>Monitors service endpoints</li> <li>Provides uptime dashboards</li> <li>Configurable health checks</li> </ul>"},{"location":"applications/observability/#opencost","title":"OpenCost","text":"<p>Kubernetes cost monitoring:</p> <ul> <li>Real-time cost allocation per namespace, deployment, pod</li> <li>Kanidm SSO integration for dashboard access</li> <li>Prometheus metrics integration</li> </ul>"},{"location":"applications/observability/#keda","title":"KEDA","text":"<p>Event-driven autoscaling:</p> <ul> <li>Powers the NFS-scaler component (scales on NFS availability)</li> <li>Powers Forgejo runner scaling (scales on webhook events)</li> <li>Queries Prometheus for scaling decisions</li> </ul>"},{"location":"applications/observability/#supporting-tools","title":"Supporting Tools","text":"<ul> <li>Blackbox Exporter \u2014 Probe endpoints for HTTP, TCP, DNS, ICMP</li> <li>Kromgo \u2014 Custom metrics publishing</li> <li>Silence Operator \u2014 Declarative alert silencing via CRDs</li> </ul>"},{"location":"applications/observability/#alert-channels","title":"Alert Channels","text":"Channel Integration Purpose Discord Webhook Real-time notifications GitHub Status API PR/commit status updates <p>Alert configuration is modular via <code>kubernetes/components/alerts/</code>:</p> <ul> <li><code>alertmanager/</code> \u2014 Routing rules</li> <li><code>discord/</code> \u2014 Discord webhook config</li> <li><code>github-status/</code> \u2014 GitHub integration</li> </ul>"},{"location":"applications/observability/#built-in-alert-rules","title":"Built-in Alert Rules","text":"<p>The kube-prometheus-stack includes three custom alert rules:</p> Alert Trigger Severity Dockerhub Rate Limiting &gt;100 containers pulling from docker.io in 30s critical OOMKilled Container OOMKilled &gt;1 times in 10min critical ZFS Pool State ZFS pool not in \"online\" state critical"},{"location":"applications/observability/#useful-prometheus-queries","title":"Useful Prometheus Queries","text":""},{"location":"applications/observability/#cluster-resources","title":"Cluster Resources","text":"<pre><code># CPU usage by namespace\nsum(rate(container_cpu_usage_seconds_total[5m])) by (namespace)\n\n# Memory usage by namespace\nsum(container_memory_working_set_bytes) by (namespace)\n\n# Pod restart count (last hour)\nincrease(kube_pod_container_status_restarts_total[1h]) &gt; 0\n</code></pre>"},{"location":"applications/observability/#storage","title":"Storage","text":"<pre><code># PVC usage percentage\nkubelet_volume_stats_used_bytes / kubelet_volume_stats_capacity_bytes * 100\n\n# NFS availability (used by NFS-scaler)\nprobe_success{instance=~\".+:2049\"}\n</code></pre>"},{"location":"applications/observability/#networking","title":"Networking","text":"<pre><code># Network traffic by pod\nsum(rate(container_network_receive_bytes_total[5m])) by (pod)\n\n# LoadBalancer service health\ncilium_services_total\n</code></pre>"},{"location":"applications/observability/#accessing-dashboards","title":"Accessing Dashboards","text":"<ul> <li>Grafana: Available via Envoy Gateway (internal)</li> <li>AlertManager: <code>alertmanager.00o.sh</code></li> <li>OpenCost: Available via Envoy Gateway with Kanidm SSO</li> <li>Gatus: Available via Envoy Gateway (internal)</li> </ul>"},{"location":"applications/utilities/","title":"Utilities","text":""},{"location":"applications/utilities/#forgejo","title":"Forgejo","text":"<p>Forgejo is a self-hosted Git repository service:</p> <ul> <li>Located in <code>kubernetes/apps/utils/forgejo/</code></li> <li>Kanidm SSO for authentication</li> <li>Forgejo Actions CI/CD via <code>forgejo-runner-system</code> namespace</li> <li>KEDA ScaledJobs for on-demand CI runners</li> </ul>"},{"location":"applications/utilities/#homepage","title":"Homepage","text":"<p>Homepage provides a unified dashboard for all cluster services:</p> <ul> <li>Located in <code>kubernetes/apps/utils/homepage/</code></li> <li>Central entry point for accessing all deployed services</li> <li>Status monitoring for services</li> </ul> <p>Note</p> <p>When adding new applications, always add them to the Homepage configuration.</p>"},{"location":"applications/utilities/#n8n","title":"n8n","text":"<p>n8n is a workflow automation platform:</p> <ul> <li>Located in <code>kubernetes/apps/utils/n8n/</code></li> <li>PostgreSQL database via CloudNative-PG</li> <li>Exposed at <code>n8n.00o.sh</code></li> <li>Automatic execution data pruning (168 hours / 50k max)</li> </ul>"},{"location":"applications/utilities/#dependencies","title":"Dependencies","text":"<ul> <li>CloudNative-PG postgres-cluster</li> <li>1Password for secrets (encryption key, database credentials)</li> </ul>"},{"location":"applications/utilities/#penpot","title":"Penpot","text":"<p>Penpot is an open-source design and prototyping platform:</p> <ul> <li>Located in <code>kubernetes/apps/utils/penpot/</code></li> <li>Multi-component: backend, frontend, exporter, Valkey cache</li> <li>PostgreSQL database via CloudNative-PG</li> <li>Persistent storage for assets via VolSync (20Gi)</li> <li>Exposed at <code>penpot.00o.sh</code></li> <li>Kanidm SSO integration</li> </ul>"},{"location":"applications/utilities/#dependencies_1","title":"Dependencies","text":"<ul> <li>CloudNative-PG postgres-cluster</li> <li>VolSync for persistent storage</li> <li>1Password for secrets</li> </ul>"},{"location":"applications/utilities/#smtp-relay","title":"SMTP Relay","text":"<p>Maddy provides centralized SMTP relay:</p> <ul> <li>Located in <code>kubernetes/apps/utils/smtp-relay/</code></li> <li>Accepts email on port 25 via LoadBalancer</li> <li>Relays through external SMTP provider</li> <li>Hostname: <code>smtp-relay.00o.sh</code></li> </ul>"},{"location":"applications/utilities/#usage","title":"Usage","text":"<p>Applications send email to:</p> <pre><code>smtp-relay.utils.svc.cluster.local:25\n</code></pre>"},{"location":"applications/utilities/#security","title":"Security","text":"<ul> <li>Non-root user (UID/GID 1000)</li> <li>Read-only root filesystem</li> <li>All capabilities dropped</li> </ul>"},{"location":"applications/utilities/#cicd-runners","title":"CI/CD Runners","text":""},{"location":"applications/utilities/#github-actions-actions-runner-system","title":"GitHub Actions (actions-runner-system)","text":"<p>Self-hosted GitHub Actions runners:</p> <ul> <li>Uses official Actions Runner Controller (ARC)</li> <li>Ephemeral runner pods</li> <li>Scales on webhook events</li> <li>Cluster access for image pulling and schema publishing</li> </ul>"},{"location":"applications/utilities/#forgejo-runners-forgejo-runner-system","title":"Forgejo Runners (forgejo-runner-system)","text":"<p>Forgejo CI/CD runners:</p> <ul> <li>KEDA ScaledJobs for on-demand scaling</li> <li>Scales based on Forgejo webhook events</li> <li>Isolated execution environment</li> </ul>"},{"location":"applications/utilities/#spegel","title":"Spegel","text":"<p>Spegel enables peer-to-peer container image sharing:</p> <ul> <li>Located in <code>kubernetes/apps/kube-system/spegel/</code></li> <li>Nodes share images directly with each other</li> <li>Auto-enables with 2+ nodes</li> <li>Registry host port: 29999</li> <li>Reduces external registry pulls and bandwidth</li> </ul>"},{"location":"applications/virtualization/","title":"Virtualization","text":"<p>KubeVirt 1.7.0 enables virtual machine management within the Kubernetes cluster.</p>"},{"location":"applications/virtualization/#components","title":"Components","text":"Component Purpose KubeVirt VM lifecycle operator CDI Containerized Data Importer for disk provisioning KubeVirt Manager Web UI at <code>kubevirt.00o.sh</code> Macvtap CNI Direct network access for VMs"},{"location":"applications/virtualization/#feature-gates","title":"Feature Gates","text":"<ul> <li>LiveMigration -- Move VMs between nodes without downtime</li> <li>Macvtap -- Direct network attachment</li> <li>HotplugVolumes -- Attach/detach volumes without restart</li> <li>HostDevices -- PCI device passthrough</li> <li>GPU -- GPU passthrough support</li> <li>NetworkBindingPlugins -- Advanced networking</li> </ul>"},{"location":"applications/virtualization/#current-vms","title":"Current VMs","text":"VM OS CPU RAM Storage debian-desktop Debian 13 + XFCE4 1 1Gi 50Gi NFS debian-server Debian 13 headless 1 1Gi 50Gi NFS ubuntu-server Ubuntu 1 1Gi varies windows-server Windows Server 2022 2 2Gi 60Gi NFS freepbx-b1-k3s01 Debian 12 + FreePBX 2 4Gi 50Gi NFS freepbx-b2-k3s01 Debian 12 + FreePBX 2 4Gi 50Gi NFS freepbx-b3-k3s01 Debian 12 + FreePBX 2 4Gi 50Gi NFS"},{"location":"applications/virtualization/#storage","title":"Storage","text":"<ul> <li>VM disks use NFS (<code>nfs-fast</code> storageClass) with ReadWriteMany access</li> <li>Enables live migration between nodes</li> <li>CDI uses <code>openebs-hostpath</code> for scratch space during disk imports</li> </ul>"},{"location":"applications/virtualization/#networking","title":"Networking","text":"<ul> <li>VMs use Multus with macvtap for direct L2 network access</li> <li>Each VM has a dedicated MAC address</li> <li>DNS endpoints configured via external-dns</li> </ul>"},{"location":"applications/virtualization/#vm-management","title":"VM Management","text":""},{"location":"applications/virtualization/#cli-virtctl","title":"CLI (virtctl)","text":"<pre><code>virtctl console &lt;vm-name&gt;      # Access VM console\nvirtctl ssh &lt;vm-name&gt;          # SSH into VM\nvirtctl start &lt;vm-name&gt;        # Start VM\nvirtctl stop &lt;vm-name&gt;         # Stop VM\nvirtctl migrate &lt;vm-name&gt;      # Live migrate VM\nvirtctl restart &lt;vm-name&gt;      # Restart VM\n</code></pre>"},{"location":"applications/virtualization/#task-runner","title":"Task Runner","text":"<pre><code>task vm:console VM=&lt;name&gt;\ntask vm:start VM=&lt;name&gt;\ntask vm:stop VM=&lt;name&gt;\n</code></pre>"},{"location":"applications/virtualization/#web-ui","title":"Web UI","text":"<p>KubeVirt Manager is accessible at <code>kubevirt.00o.sh</code> with Kanidm SSO.</p>"},{"location":"applications/virtualization/#adding-a-new-vm","title":"Adding a New VM","text":"<ol> <li>Create a VirtualMachine manifest in <code>kubernetes/apps/kubevirt/virtualmachines/</code></li> <li>Define disk source (CDI DataVolume or existing PVC)</li> <li>Configure networking (macvtap interface with MAC address)</li> <li>Add external-dns annotation for DNS entry</li> <li>Add to the namespace kustomization</li> </ol>"},{"location":"development/","title":"Development","text":"<p>Guides for developing and contributing to the cluster repository.</p>"},{"location":"development/#utility-scripts","title":"Utility Scripts","text":"Script Purpose <code>scripts/bootstrap-apps.sh</code> Main cluster bootstrap <code>scripts/volsync-restore-all.sh</code> Mass VolSync point-in-time restore for all 16 backed-up apps <code>scripts/generate-labels.sh</code> Auto-generate <code>.github/labels.yaml</code> and <code>.github/labeler.yaml</code> from directory structure"},{"location":"development/#documentation-site","title":"Documentation Site","text":"<p>The docs site at docs.00o.sh is built with MkDocs Material from the <code>docs/</code> directory. To preview locally:</p> <pre><code>pip install mkdocs-material\nmkdocs serve\n</code></pre> <p>Changes to <code>docs/</code> or <code>mkdocs.yml</code> on main are automatically published via the <code>docs.yaml</code> workflow.</p>"},{"location":"development/#pages","title":"Pages","text":"<ul> <li>Template System -- Jinja2 configuration templating</li> <li>CI/CD Pipelines -- GitHub Actions workflows</li> <li>Contributing -- Conventions and guidelines</li> </ul>"},{"location":"development/ci-cd/","title":"CI/CD Pipelines","text":"<p>GitHub Actions workflows automate testing, validation, and deployment.</p>"},{"location":"development/ci-cd/#workflows","title":"Workflows","text":""},{"location":"development/ci-cd/#flux-localyaml","title":"flux-local.yaml","text":"<p>Validates Flux manifests on pull requests:</p> <ul> <li>Checks Flux configuration with <code>--enable-helm --all-namespaces</code></li> <li>Generates diffs for HelmReleases and Kustomizations</li> <li>Triggers on <code>kubernetes/**</code> file changes</li> </ul>"},{"location":"development/ci-cd/#e2eyaml","title":"e2e.yaml","text":"<p>End-to-end testing of the configuration pipeline:</p> <ul> <li>Runs <code>task init</code> and <code>task configure</code></li> <li>Tests with sample configurations (public/private matrix)</li> <li>Validates with flux-local</li> </ul>"},{"location":"development/ci-cd/#labeleryaml","title":"labeler.yaml","text":"<p>Automated PR labeling:</p> <ul> <li>Area labels based on changed file paths</li> <li>Size labels: xs (&lt;10 lines), s (&lt;30), m (&lt;100), l (&lt;500), xl (500+)</li> <li>Ignores markdown files for size calculation</li> </ul>"},{"location":"development/ci-cd/#label-syncyaml","title":"label-sync.yaml","text":"<p>Synchronizes GitHub labels from <code>.github/labels.yaml</code>:</p> <ul> <li>Triggered on pushes to main</li> <li>Deletes undefined labels</li> <li>Maintains consistent labeling</li> </ul>"},{"location":"development/ci-cd/#label-generateyaml","title":"label-generate.yaml","text":"<p>Auto-generates label configuration:</p> <ul> <li>Updates <code>.github/labels.yaml</code> and <code>.github/labeler.yaml</code></li> <li>Keeps labels in sync with namespace/directory changes</li> </ul>"},{"location":"development/ci-cd/#image-pullyaml","title":"image-pull.yaml","text":"<p>Pre-pulls container images to cluster nodes:</p> <ul> <li>Extracts images from Flux manifests on PRs</li> <li>Compares images between PR and main branch</li> <li>Pulls new images via Talosctl</li> <li>Runs on self-hosted runner (<code>00o-sh-runner</code>)</li> <li>Max 4 parallel pulls</li> </ul>"},{"location":"development/ci-cd/#schemasyaml","title":"schemas.yaml","text":"<p>CRD schema extraction and publishing:</p> <ul> <li>Scheduled daily</li> <li>Extracts CRD schemas via datreeio/crd-extractor</li> <li>Publishes to Cloudflare Pages (<code>kubernetes-schemas</code> project)</li> <li>Runs on self-hosted runner</li> <li>Enables IDE autocompletion for custom resources</li> </ul>"},{"location":"development/ci-cd/#docsyaml","title":"docs.yaml","text":"<p>Documentation site publishing:</p> <ul> <li>Builds MkDocs Material site</li> <li>Publishes to Cloudflare Pages (<code>special-winner-docs</code> project)</li> <li>Triggered on docs/ or mkdocs.yml changes</li> </ul>"},{"location":"development/ci-cd/#renovate-configyaml","title":"renovate-config.yaml","text":"<p>Renovate configuration validation:</p> <ul> <li>Validates <code>.renovaterc.json5</code> on pull requests</li> <li>Runs <code>renovate-config-validator --strict</code></li> <li>Only triggers when <code>.renovaterc.json5</code> is modified</li> </ul>"},{"location":"development/ci-cd/#releaseyaml","title":"release.yaml","text":"<p>Repository release management.</p>"},{"location":"development/ci-cd/#self-hosted-runners","title":"Self-Hosted Runners","text":"<p>Some workflows run on self-hosted runners with cluster access, managed by Actions Runner Controller (ARC) in the <code>actions-runner-system</code> namespace.</p>"},{"location":"development/ci-cd/#runner-scale-sets","title":"Runner Scale Sets","text":"Scale Set Organization Min Runners Max Runners Storage <code>special-winner</code> 00o-sh - - - <code>ambersecurityinc</code> ambersecurityinc 1 3 25Gi"},{"location":"development/ci-cd/#workflows-using-self-hosted-runners","title":"Workflows Using Self-Hosted Runners","text":"<ul> <li><code>image-pull.yaml</code> -- Needs Talosctl for image pulling</li> <li><code>schemas.yaml</code> -- Needs kubectl for CRD extraction</li> </ul>"},{"location":"development/ci-cd/#monitoring","title":"Monitoring","text":"<p>An ARC Grafana dashboard is available for monitoring runner autoscaling metrics (counters, gauges, histograms) at the cluster's Grafana instance.</p>"},{"location":"development/ci-cd/#troubleshooting-cicd","title":"Troubleshooting CI/CD","text":""},{"location":"development/ci-cd/#flux-local-validation-fails","title":"flux-local Validation Fails","text":"<p>The most common PR failure. Check the workflow output for specific errors:</p> <pre><code># Run flux-local locally to reproduce\nflux-local test --enable-helm --all-namespaces\n\n# Common issues:\n# - Invalid YAML syntax in HelmRelease or Kustomization\n# - Missing OCIRepository reference\n# - Duplicate resource names in the same namespace\n</code></pre>"},{"location":"development/ci-cd/#image-pull-fails","title":"image-pull Fails","text":"<p>This workflow runs on the self-hosted runner and needs cluster access:</p> <ol> <li> <p>Check that the <code>00o-sh-runner</code> pods are running:</p> <pre><code>kubectl -n actions-runner-system get pods\n</code></pre> </li> <li> <p>Verify Talosctl connectivity to nodes (the runner needs access to pull images)</p> </li> <li> <p>If the runner is unavailable, the job will stay queued \u2014 check the Actions Runner Controller logs:</p> <pre><code>kubectl -n actions-runner-system logs -l app.kubernetes.io/name=actions-runner-controller-gha-rs-controller\n</code></pre> </li> </ol>"},{"location":"development/ci-cd/#schemasyaml-fails","title":"schemas.yaml Fails","text":"<ul> <li>Requires the self-hosted runner with <code>kubectl</code> access</li> <li>Check that CRDs are installed in the cluster</li> <li>Verify Cloudflare Pages deployment token is valid</li> </ul>"},{"location":"development/ci-cd/#docsyaml-fails","title":"docs.yaml Fails","text":"<ul> <li>Check for MkDocs build errors (broken links, invalid YAML frontmatter)</li> <li> <p>Run locally to reproduce:</p> <pre><code>pip install mkdocs-material\nmkdocs build --strict\n</code></pre> </li> </ul>"},{"location":"development/ci-cd/#general-debugging-tips","title":"General Debugging Tips","text":"<ul> <li>View workflow logs: Go to the Actions tab on GitHub, click the failed run</li> <li>Re-run a failed job: Use the \"Re-run jobs\" button in the Actions UI</li> <li>Check runner availability: Self-hosted runner jobs will queue if no runners are available</li> <li>Concurrency: Most workflows use concurrency groups to prevent duplicate runs \u2014 a new push will cancel the previous run</li> </ul>"},{"location":"development/contributing/","title":"Contributing","text":""},{"location":"development/contributing/#git-commit-conventions","title":"Git Commit Conventions","text":"<p>This repository uses semantic commits with Renovate conventions:</p> <pre><code>&lt;type&gt;(&lt;scope&gt;): &lt;message&gt;\n</code></pre>"},{"location":"development/contributing/#types","title":"Types","text":"Type Usage <code>feat</code> New feature <code>fix</code> Bug fix <code>chore</code> Maintenance <code>ci</code> CI/CD changes <code>docs</code> Documentation <code>refactor</code> Code refactoring"},{"location":"development/contributing/#scopes","title":"Scopes","text":"Scope Usage <code>container</code> Docker images <code>helm</code> Helm charts <code>github-action</code> GitHub Actions <code>kubernetes</code> Kubernetes manifests <code>observability</code> Monitoring and alerting <code>volsync</code> Backup and replication <code>github</code> GitHub configuration <code>mise</code> Development tools <code>media</code> Media applications <code>network</code> Network infrastructure <code>utils</code> Utility services"},{"location":"development/contributing/#examples","title":"Examples","text":"<pre><code>fix(container): update image ghcr.io/app/name ( 1.0.0 \u2192 1.1.0 )\nfeat(helm): update chart app-template ( 3.0.0 \u2192 4.0.0 )\nci(github-action): update action/checkout ( v3 \u2192 v4 )\ndocs(github): update CLAUDE.md with current repository state\n</code></pre>"},{"location":"development/contributing/#application-checklist","title":"Application Checklist","text":"<p>When adding a new application, follow this checklist:</p> <ul> <li> Create directory: <code>kubernetes/apps/&lt;namespace&gt;/&lt;app-name&gt;/app/</code></li> <li> Create <code>helmrelease.yaml</code>, <code>ocirepository.yaml</code>, <code>kustomization.yaml</code></li> <li> Create <code>ks.yaml</code> Flux Kustomization</li> <li> Update namespace <code>kustomization.yaml</code></li> <li> Encrypt secrets with SOPS (if needed)</li> <li> Consider Kanidm SSO integration (web UI apps)</li> <li> Add to Homepage dashboard</li> <li> Add VolSync backup config (stateful apps)</li> <li> Add monitoring (ServiceMonitor/PodMonitor)</li> <li> Add Discord alerts (critical apps)</li> <li> Add NFS-scaler component (NFS-dependent apps)</li> <li> Update CLAUDE.md deployed applications list</li> </ul>"},{"location":"development/contributing/#security-guidelines","title":"Security Guidelines","text":"<ul> <li>Never commit unencrypted secrets -- Use <code>*.sops.yaml</code> files</li> <li>Validate encryption -- Check for <code>sops:</code> metadata</li> <li>Follow security contexts:</li> </ul> <pre><code>securityContext:\n  allowPrivilegeEscalation: false\n  readOnlyRootFilesystem: true\n  capabilities: { drop: [\"ALL\"] }\n</code></pre>"},{"location":"development/contributing/#common-mistakes","title":"Common Mistakes","text":"<ol> <li>Don't modify files in <code>talos/clusterconfig/</code> (generated)</li> <li>Don't commit plaintext secrets</li> <li>Don't skip <code>task configure</code> after template changes</li> <li>Don't push directly to main</li> <li>Be aware of Helm v4 breaking changes from v3</li> <li>Some workflows need the self-hosted runner (<code>00o-sh-runner</code>)</li> </ol>"},{"location":"development/contributing/#bash-script-conventions","title":"Bash Script Conventions","text":"<p>Scripts in <code>scripts/</code> use:</p> <pre><code>#!/usr/bin/env bash\nset -Eeuo pipefail\n\nsource \"$(dirname \"${0}\")/lib/common.sh\"\n\nexport LOG_LEVEL=\"debug\"\nexport ROOT_DIR=\"$(git rev-parse --show-toplevel)\"\n</code></pre>"},{"location":"development/contributing/#logging","title":"Logging","text":"<pre><code>log debug \"Debug message\"\nlog info \"Information message\" \"var=value\"\nlog warn \"Warning message\"\nlog error \"Error message\"  # Exits with code 1\n</code></pre>"},{"location":"development/contributing/#environment-checks","title":"Environment Checks","text":"<pre><code>check_env KUBECONFIG TALOSCONFIG\ncheck_cli kubectl flux sops\n</code></pre>"},{"location":"development/templates/","title":"Template System","text":"<p>The cluster uses makejinja for configuration templating.</p>"},{"location":"development/templates/#configuration","title":"Configuration","text":"<p>Defined in <code>makejinja.toml</code>:</p> <pre><code>[makejinja]\ninputs = [\"./templates/overrides\",\"./templates/config\"]\noutput = \"./\"\nexclude_patterns = [\"*.partial.yaml.j2\"]\ndata = [\"./cluster.yaml\", \"./nodes.yaml\"]\nimport_paths = [\"./templates/scripts\"]\njinja_suffix = \".j2\"\nforce = true\nundefined = \"chainable\"\n</code></pre>"},{"location":"development/templates/#custom-delimiters","title":"Custom Delimiters","text":"<p>To avoid conflicts with YAML and Helm templating:</p> Element Syntax Standard Jinja2 Variables <code>#{ variable }#</code> <code>{{ variable }}</code> Blocks <code>#% if condition %# ... #% endif %#</code> <code>{% if %} ... {% endif %}</code> Comments <code>#| comment #|</code> <code>{# comment #}</code>"},{"location":"development/templates/#custom-filters","title":"Custom Filters","text":"<p>Located in <code>templates/scripts/plugin.py</code>:</p> Filter Usage Example <code>nthhost</code> Get Nth host in CIDR <code>#{ \"10.0.0.0/24\" \\| nthhost(1) }#</code> \u2192 <code>10.0.0.1</code> <code>age_key</code> Extract age key <code>#{ \"public\" \\| age_key }#</code> <code>basename</code> Filename without ext <code>#{ \"path/file.txt\" \\| basename }#</code> \u2192 <code>file</code>"},{"location":"development/templates/#data-sources","title":"Data Sources","text":"<p>Templates read from two configuration files (generated via <code>task init</code>, gitignored):</p> <ul> <li><code>cluster.yaml</code> -- Cluster-wide settings</li> <li><code>nodes.yaml</code> -- Node definitions</li> </ul>"},{"location":"development/templates/#directory-structure","title":"Directory Structure","text":"<pre><code>templates/\n\u251c\u2500\u2500 config/           # Main configuration templates\n\u2502   \u2514\u2500\u2500 talos/       # Talos Linux config templates\n\u251c\u2500\u2500 overrides/       # Output file overrides\n\u2514\u2500\u2500 scripts/         # Custom Jinja2 filters\n    \u2514\u2500\u2500 plugin.py    # nthhost, age_key, basename\n</code></pre>"},{"location":"development/templates/#workflow","title":"Workflow","text":"<pre><code># Generate configs from samples\ntask init\n\n# Edit cluster.yaml and nodes.yaml\n\n# Render templates and validate\ntask configure\n\n# Validate specific configs\ntask template:validate-kubernetes-config\ntask template:validate-talos-config\n\n# Clean up after bootstrap\ntask template:tidy\n</code></pre> <p>Warning</p> <p>Never modify files in <code>talos/clusterconfig/</code> directly -- they are generated from templates. Edit the source templates instead.</p>"},{"location":"getting-started/","title":"Getting Started","text":"<p>This guide walks through deploying the cluster from bare metal to a fully operational GitOps-managed Kubernetes environment.</p>"},{"location":"getting-started/#overview","title":"Overview","text":"<p>There are 5 stages to complete:</p> <ol> <li>Prerequisites -- Prepare machines and install tools</li> <li>Configuration -- Configure cluster and node settings</li> <li>Bootstrap -- Deploy Talos, Kubernetes, and Flux</li> <li>Post-Install -- Verify and configure DNS, webhooks</li> </ol>"},{"location":"getting-started/#minimum-requirements","title":"Minimum Requirements","text":"Role Cores Memory System Disk Control/Worker 4 16GB 256GB SSD/NVMe <p>Tip</p> <p>With 3 or more nodes, make 3 of them controller nodes for a highly available control plane. All nodes are configured to run workloads -- worker nodes are optional.</p>"},{"location":"getting-started/#time-estimate","title":"Time Estimate","text":"<ul> <li>Stage 1-2 (prep + config): Variable depending on hardware</li> <li>Stage 3 (bootstrap): 10-15 minutes (errors during bootstrap are normal)</li> <li>Stage 4 (verification): 5-10 minutes</li> </ul>"},{"location":"getting-started/bootstrap/","title":"Bootstrap","text":"<p>Warning</p> <p>Bootstrap takes 10+ minutes. Errors like \"couldn't get current server API group list\" and \"no matching resources found\" are normal during this process. If interrupted with Ctrl+C, you may need to reset the cluster before retrying.</p>"},{"location":"getting-started/bootstrap/#stage-1-install-talos","title":"Stage 1: Install Talos","text":"<pre><code>task bootstrap:talos\n</code></pre> <p>Push the generated secrets:</p> <pre><code>git add -A\ngit commit -m \"chore: add talhelper encrypted secret\"\ngit push\n</code></pre>"},{"location":"getting-started/bootstrap/#stage-2-install-core-components","title":"Stage 2: Install Core Components","text":"<p>This installs Cilium (CNI), CoreDNS (DNS), Flux (GitOps), and syncs the cluster to Git:</p> <pre><code>task bootstrap:apps\n</code></pre> <p>Note</p> <p>Spegel (peer-to-peer image sharing) automatically activates when a second node joins the cluster.</p>"},{"location":"getting-started/bootstrap/#stage-3-watch-deployment","title":"Stage 3: Watch Deployment","text":"<pre><code>kubectl get pods --all-namespaces --watch\n</code></pre>"},{"location":"getting-started/bootstrap/#what-happens-during-bootstrap","title":"What Happens During Bootstrap","text":"<p>The <code>scripts/bootstrap-apps.sh</code> script runs these steps:</p> <ol> <li>Wait for nodes -- Polls until nodes reach maintenance mode</li> <li>Apply namespaces -- Creates all namespace resources</li> <li>Apply SOPS secrets -- Decrypts and applies bootstrap secrets (deploy key, age key, cluster secrets)</li> <li>Apply CRDs -- Extracts CRDs from Helmfile (<code>00-crds.yaml</code>)</li> <li>Sync Helm releases -- Installs core applications from Helmfile (<code>01-apps.yaml</code>)</li> <li>Apply ClusterSecretStore -- Configures 1Password integration</li> </ol> <p>After bootstrap, Flux takes over and continuously reconciles the cluster state from Git.</p>"},{"location":"getting-started/configuration/","title":"Configuration","text":""},{"location":"getting-started/configuration/#generate-config-files","title":"Generate Config Files","text":"<pre><code>task init\n</code></pre> <p>This creates <code>cluster.yaml</code>, <code>nodes.yaml</code>, <code>age.key</code>, and other configuration files from samples.</p>"},{"location":"getting-started/configuration/#edit-configuration","title":"Edit Configuration","text":"<p>Fill out the generated files using the comments as guidance:</p> <ul> <li><code>cluster.yaml</code> -- Cluster-wide settings (domain, network CIDRs, feature flags)</li> <li><code>nodes.yaml</code> -- Node definitions (hostnames, IPs, roles, disk paths)</li> </ul>"},{"location":"getting-started/configuration/#render-and-validate","title":"Render and Validate","text":"<pre><code>task configure\n</code></pre> <p>This runs makejinja to render Jinja2 templates and validates the output.</p>"},{"location":"getting-started/configuration/#template-system","title":"Template System","text":"<p>The configuration uses makejinja with custom Jinja2 delimiters to avoid YAML conflicts:</p> Delimiter Syntax Standard Jinja2 Variables <code>#{ variable }#</code> <code>{{ variable }}</code> Blocks <code>#% if condition %# ... #% endif %#</code> <code>{% if %} ... {% endif %}</code> Comments <code>#| comment #|</code> <code>{# comment #}</code> <p>Templates are located in <code>templates/config/</code> and <code>templates/overrides/</code>, with custom filters in <code>templates/scripts/plugin.py</code>:</p> <pre><code>nthhost(cidr, index)      # Get Nth host in CIDR range\nage_key(key_type)         # Extract age public/private key\nbasename(path)            # Get filename without extension\n</code></pre>"},{"location":"getting-started/configuration/#verify-encryption","title":"Verify Encryption","text":"<p>Before pushing, verify all secrets are encrypted:</p> <pre><code># All .sops.yaml files should contain 'sops:' metadata\ngrep -r \"sops:\" kubernetes/**/*.sops.yaml bootstrap/**/*.sops.yaml\n</code></pre>"},{"location":"getting-started/configuration/#push-configuration","title":"Push Configuration","text":"<pre><code>git add -A\ngit commit -m \"chore: initial commit\"\ngit push\n</code></pre> <p>Warning</p> <p>Using a private repository? Paste the public key from <code>github-deploy.key.pub</code> into your repository's deploy keys settings.</p>"},{"location":"getting-started/post-install/","title":"Post-Installation","text":""},{"location":"getting-started/post-install/#verification","title":"Verification","text":""},{"location":"getting-started/post-install/#check-cilium","title":"Check Cilium","text":"<pre><code>cilium status\n</code></pre>"},{"location":"getting-started/post-install/#check-flux","title":"Check Flux","text":"<pre><code>flux check\nflux get sources git flux-system\nflux get ks -A\nflux get hr -A\n</code></pre> <p>Tip</p> <p>Run <code>task reconcile</code> to force Flux to sync immediately.</p>"},{"location":"getting-started/post-install/#check-connectivity","title":"Check Connectivity","text":"<pre><code># Test gateway ports (replace variables with actual values)\nnmap -Pn -n -p 443 ${cluster_gateway_addr} ${cloudflare_gateway_addr} -vv\n</code></pre>"},{"location":"getting-started/post-install/#check-dns","title":"Check DNS","text":"<pre><code># Should resolve to your Cloudflare gateway address\ndig @${cluster_dns_gateway_addr} echo.${cloudflare_domain}\n</code></pre>"},{"location":"getting-started/post-install/#check-certificates","title":"Check Certificates","text":"<pre><code>kubectl -n network describe certificates\n</code></pre>"},{"location":"getting-started/post-install/#configure-github-webhook","title":"Configure GitHub Webhook","text":"<p>For push-triggered reconciliation (instead of polling):</p> <ol> <li> <p>Get the webhook path:</p> <pre><code>kubectl -n flux-system get receiver github-webhook \\\n  --output=jsonpath='{.status.webhookPath}'\n</code></pre> </li> <li> <p>Build the full URL:</p> <pre><code>https://flux-webhook.${cloudflare_domain}/hook/&lt;webhook-path&gt;\n</code></pre> </li> <li> <p>In GitHub repository settings, go to Settings &gt; Webhooks &gt; Add webhook:</p> <ul> <li>URL: The full webhook URL from above</li> <li>Secret: Contents of <code>github-push-token.txt</code></li> <li>Content type: <code>application/json</code></li> <li>Events: Just the push event</li> </ul> </li> </ol>"},{"location":"getting-started/post-install/#configure-home-dns-split-dns","title":"Configure Home DNS (Split DNS)","text":"<p>The <code>k8s_gateway</code> service provides DNS resolution for cluster services. Configure your home DNS server to forward queries for your domain to <code>${cluster_dns_gateway_addr}</code>.</p> <p>This enables accessing internal services like <code>grafana.yourdomain.com</code> from any device on your network.</p>"},{"location":"getting-started/post-install/#public-vs-private-access","title":"Public vs Private Access","text":"Gateway Use Case Configuration <code>envoy-external</code> Public internet access Routes through Cloudflare Tunnel <code>envoy-internal</code> Private network only Accessible via split DNS <p>By default, only <code>echo</code> and <code>flux-webhook</code> are publicly accessible. To make additional apps public, set the correct gateway on their HTTPRoute.</p>"},{"location":"getting-started/post-install/#clean-up-templates","title":"Clean Up Templates","text":"<p>Once the cluster is stable and you no longer need <code>task configure</code>:</p> <pre><code>task template:tidy\ngit add -A\ngit commit -m \"chore: tidy up\"\ngit push\n</code></pre> <p>This removes the <code>templates/</code> directory and template-related files.</p>"},{"location":"getting-started/prerequisites/","title":"Prerequisites","text":""},{"location":"getting-started/prerequisites/#machine-preparation","title":"Machine Preparation","text":"<ol> <li> <p>Go to the Talos Linux Image Factory and build an image:</p> <ul> <li>Choose bare-minimum system extensions only</li> <li>Start with CPU-specific extensions: <code>i915</code>, <code>intel-ucode</code>, <code>mei</code> (Intel) or <code>amdgpu</code>, <code>amd-ucode</code> (AMD)</li> <li>Note the schematic ID -- you'll need it later</li> </ul> </li> <li> <p>Flash the Talos ISO/RAW image to USB and boot your nodes</p> </li> <li> <p>Verify nodes are reachable:</p> <pre><code>nmap -Pn -n -p 50000 192.168.1.0/24 -vv | grep 'Discovered'\n</code></pre> </li> </ol>"},{"location":"getting-started/prerequisites/#local-workstation","title":"Local Workstation","text":"<ol> <li> <p>Clone the repository:</p> <pre><code>git clone https://github.com/00o-sh/special-winner.git\ncd special-winner\n</code></pre> </li> <li> <p>Install Mise CLI and activate it in your shell</p> </li> <li> <p>Install required tools:</p> <pre><code>mise trust\npip install pipx\nmise install\n</code></pre> <p>Note</p> <p>Having trouble? Try <code>unset GITHUB_TOKEN</code> and run again. For Python compilation issues: <code>mise settings python.compile=0</code></p> </li> <li> <p>Logout of GHCR to avoid auth issues:</p> <pre><code>docker logout ghcr.io\nhelm registry logout ghcr.io\n</code></pre> </li> </ol>"},{"location":"getting-started/prerequisites/#cloudflare-setup","title":"Cloudflare Setup","text":"<ol> <li> <p>Create a Cloudflare API token with:</p> <ul> <li><code>Zone - DNS - Edit</code> permission</li> <li><code>Account - Cloudflare Tunnel - Read</code> permission</li> <li>Name it <code>kubernetes</code></li> </ul> </li> <li> <p>Create the tunnel:</p> <pre><code>cloudflared tunnel login\ncloudflared tunnel create --credentials-file cloudflare-tunnel.json kubernetes\n</code></pre> </li> </ol>"},{"location":"getting-started/prerequisites/#tool-versions","title":"Tool Versions","text":"<p>All tools are managed via <code>.mise.toml</code>:</p> Tool Version Purpose Python 3.14.3 Template rendering kubectl 1.34.0 Kubernetes CLI Helm 4.1.1 Chart management Flux 2.7.5 GitOps CLI Talos CLI 1.12.4 Talos management Cilium CLI 0.19.1 Network management SOPS 3.12.1 Secret encryption Age 1.3.1 Encryption backend virtctl 1.7.0 VM management k9s 0.50.18 Kubernetes TUI"},{"location":"infrastructure/","title":"Infrastructure","text":"<p>This section covers the core infrastructure components that power the cluster.</p>"},{"location":"infrastructure/#component-overview","title":"Component Overview","text":"<pre><code>graph LR\n    subgraph OS[\"Operating System\"]\n        Talos[Talos Linux 1.12.4]\n    end\n    subgraph Network[\"Networking\"]\n        Cilium[Cilium 1.19.0]\n        Envoy[Envoy Gateway v1.6.3]\n        CF[Cloudflare Tunnel]\n    end\n    subgraph Data[\"Data\"]\n        PG[PostgreSQL 17.7]\n        MDB[MariaDB 11.7 Galera]\n        DF[Dragonfly]\n        OEBS[OpenEBS]\n    end\n    subgraph GitOps\n        Flux[Flux CD 2.7.5]\n    end\n    subgraph Security\n        SOPS[SOPS + Age]\n        Kanidm[Kanidm SSO]\n        CM[cert-manager]\n    end\n\n    Talos --&gt; Cilium\n    Cilium --&gt; Envoy\n    Flux --&gt; Network\n    Flux --&gt; Data\n    Flux --&gt; Security</code></pre>"},{"location":"infrastructure/#pages","title":"Pages","text":"Page Description Talos Linux Immutable Kubernetes OS configuration and management Flux CD GitOps continuous delivery Cilium eBPF-based container networking Envoy Gateway HTTP routing and ingress Storage OpenEBS, NFS, and backup systems Databases PostgreSQL HA, MariaDB Galera, and Dragonfly Certificates &amp; DNS TLS automation and DNS management Secrets SOPS, Age, and External Secrets Identity &amp; SSO Kanidm identity provider"},{"location":"infrastructure/certificates-dns/","title":"Certificates &amp; DNS","text":""},{"location":"infrastructure/certificates-dns/#cert-manager","title":"cert-manager","text":"<p>cert-manager automates TLS certificate management:</p> <ul> <li>Wildcard certificates via Cloudflare DNS-01 challenge</li> <li>Automatic renewal before expiry</li> <li>Certificates stored as Kubernetes Secrets</li> </ul>"},{"location":"infrastructure/certificates-dns/#checking-certificates","title":"Checking Certificates","text":"<pre><code>kubectl -n network describe certificates\nkubectl -n cert-manager get clusterissuers\n</code></pre>"},{"location":"infrastructure/certificates-dns/#dns-architecture","title":"DNS Architecture","text":"<pre><code>graph TD\n    Internet[Internet DNS] --&gt; CF[Cloudflare DNS]\n    CF --&gt; Tunnel[Cloudflare Tunnel]\n    Tunnel --&gt; Envoy[Envoy Gateway]\n\n    HomeDNS[Home DNS Server] --&gt; k8sGW[k8s_gateway]\n    k8sGW --&gt; Envoy\n\n    Pods[Cluster Pods] --&gt; CoreDNS[CoreDNS]\n    CoreDNS --&gt; k8sGW</code></pre>"},{"location":"infrastructure/certificates-dns/#coredns","title":"CoreDNS","text":"<p>Provides in-cluster DNS resolution for pod-to-pod and pod-to-service communication.</p>"},{"location":"infrastructure/certificates-dns/#k8s_gateway","title":"k8s_gateway","text":"<p>Provides DNS resolution for external Kubernetes resources from your home network. Your home DNS server must forward queries for your domain to the k8s_gateway address.</p>"},{"location":"infrastructure/certificates-dns/#external-dns","title":"External-DNS","text":"<p>Automatically creates DNS records in Cloudflare when services are exposed:</p> <ul> <li>Watches Kubernetes resources for DNS annotations</li> <li>Creates A/CNAME records in Cloudflare</li> <li>Used by VMs for dedicated DNS entries</li> </ul>"},{"location":"infrastructure/certificates-dns/#cloudflare-dns","title":"Cloudflare DNS","text":"<p>Manages public DNS records:</p> <ul> <li><code>cloudflare-dns</code> -- DNS record management</li> <li><code>cloudflare-tunnel</code> -- Secure external access without port forwarding</li> </ul>"},{"location":"infrastructure/certificates-dns/#unifi-dns","title":"UniFi DNS","text":"<p>Integration with UniFi network equipment for local DNS management:</p> <ul> <li><code>unifi-dns</code> -- DNS webhook provider</li> <li><code>unifi-toolkit</code> -- Network management toolkit</li> </ul>"},{"location":"infrastructure/cilium/","title":"Cilium Networking","text":"<p>Cilium 1.19.0 provides eBPF-based container networking, fully replacing kube-proxy.</p>"},{"location":"infrastructure/cilium/#key-configuration","title":"Key Configuration","text":"Setting Value Purpose kubeProxyReplacement <code>true</code> Replaces kube-proxy entirely with eBPF routingMode <code>native</code> Direct routing without tunneling overhead loadBalancer.algorithm <code>maglev</code> Consistent hashing for load distribution loadBalancer.mode <code>dsr</code> Direct Server Return (reduces return path latency) ipam.mode <code>kubernetes</code> Uses Kubernetes for IP address management l2announcements enabled Announces LoadBalancer IPs via L2 discovery cni.exclusive <code>false</code> Allows Multus CNI for multi-network support devices <code>bond+</code> Binds to bonded network interfaces"},{"location":"infrastructure/cilium/#architecture","title":"Architecture","text":"<pre><code>kubernetes/apps/kube-system/cilium/\n\u251c\u2500\u2500 app/\n\u2502   \u251c\u2500\u2500 helmrelease.yaml      # Cilium agent + operator config\n\u2502   \u251c\u2500\u2500 ocirepository.yaml    # Chart source\n\u2502   \u2514\u2500\u2500 kustomization.yaml\n\u2514\u2500\u2500 ks.yaml\n</code></pre>"},{"location":"infrastructure/cilium/#load-balancing","title":"Load Balancing","text":"<p>Cilium provides L2 load balancing for <code>LoadBalancer</code> services:</p> <ul> <li>IP Pool: <code>10.0.6.0/24</code> (<code>CiliumLoadBalancerIPPool</code>)</li> <li>Announcement: L2 ARP on bonded interfaces (<code>CiliumL2AnnouncementPolicy</code>)</li> <li>Algorithm: Maglev consistent hashing</li> <li>Mode: DSR (Direct Server Return) for reduced latency</li> </ul> <p>Services with <code>type: LoadBalancer</code> automatically get an IP from the pool. Examples:</p> Service LoadBalancer IP Plex <code>10.0.6.14</code> SMTP Relay <code>10.0.6.15</code>"},{"location":"infrastructure/cilium/#monitoring","title":"Monitoring","text":"<p>Cilium exports Prometheus metrics with two pre-configured Grafana dashboards:</p> <ul> <li>cilium-agent (Grafana ID: 16611) \u2014 Agent metrics, datapath performance</li> <li>cilium-operator (Grafana ID: 16612) \u2014 Operator health and status</li> </ul> <p>Both Prometheus ServiceMonitor and operator metrics are enabled.</p>"},{"location":"infrastructure/cilium/#hubble-observability","title":"Hubble (Observability)","text":"<p>If enabled, Hubble provides network flow visibility:</p> <pre><code>hubble status\nhubble observe --follow\nhubble observe --namespace &lt;namespace&gt;  # Filter by namespace\n</code></pre>"},{"location":"infrastructure/cilium/#checking-status","title":"Checking Status","text":"<pre><code># Overall Cilium health\ncilium status\n\n# Full connectivity test suite\ncilium connectivity test\n\n# Check BPF maps and routing\ncilium bpf lb list          # LoadBalancer entries\ncilium bpf endpoint list    # Endpoint mappings\n</code></pre>"},{"location":"infrastructure/cilium/#troubleshooting","title":"Troubleshooting","text":""},{"location":"infrastructure/cilium/#pods-cant-reach-services","title":"Pods Can't Reach Services","text":"<pre><code># Check Cilium agent status on the node\ncilium status\nkubectl -n kube-system logs -l k8s-app=cilium --tail=50\n\n# Verify endpoints are programmed\ncilium endpoint list\n</code></pre>"},{"location":"infrastructure/cilium/#loadbalancer-ip-not-responding","title":"LoadBalancer IP Not Responding","text":"<pre><code># Verify the IP pool has available addresses\nkubectl get ciliumloadbalancerippool -o yaml\n\n# Check L2 announcement policy\nkubectl get ciliuml2announcementpolicy -o yaml\n\n# Verify the service has an external IP assigned\nkubectl get svc -A | grep LoadBalancer\n</code></pre>"},{"location":"infrastructure/cilium/#connectivity-between-nodes","title":"Connectivity Between Nodes","text":"<pre><code># Run the built-in connectivity test\ncilium connectivity test\n\n# Check node-to-node routing\ncilium bpf tunnel list\n</code></pre>"},{"location":"infrastructure/databases/","title":"Databases","text":""},{"location":"infrastructure/databases/#cloudnative-pg-postgresql","title":"CloudNative-PG (PostgreSQL)","text":"<p>CloudNative-PG runs a PostgreSQL 17.7 high-availability cluster with 3 instances.</p>"},{"location":"infrastructure/databases/#architecture","title":"Architecture","text":"<pre><code>kubernetes/apps/database/cloudnative-pg/\n\u251c\u2500\u2500 app/                    # Operator deployment\n\u2502   \u251c\u2500\u2500 helmrelease.yaml\n\u2502   \u2514\u2500\u2500 ocirepository.yaml\n\u251c\u2500\u2500 cluster/               # PostgreSQL cluster definition\n\u2502   \u251c\u2500\u2500 cluster.yaml       # Main cluster spec\n\u2502   \u251c\u2500\u2500 scheduledbackup.yaml\n\u2502   \u251c\u2500\u2500 objectstore.yaml   # S3 backup config\n\u2502   \u2514\u2500\u2500 externalsecret.yaml\n\u2514\u2500\u2500 recovery/              # Disaster recovery configs\n    \u2514\u2500\u2500 cluster.yaml\n</code></pre>"},{"location":"infrastructure/databases/#configuration","title":"Configuration","text":"Setting Value Instances 3 (HA with pod anti-affinity) Storage 20Gi per instance (openebs-hostpath) Max connections 200 Shared buffers 256MB Effective cache size 512MB Maintenance work mem 128MB CPU request 100m Memory request 512Mi Memory limit 2Gi"},{"location":"infrastructure/databases/#backups","title":"Backups","text":"<ul> <li>WAL archiving to Garage S3 via barman-cloud plugin</li> <li>Scheduled backups with configurable retention</li> <li>Monitoring via PodMonitor for Prometheus</li> </ul>"},{"location":"infrastructure/databases/#connecting","title":"Connecting","text":"<p>Applications connect via the internal service:</p> <pre><code>postgres-rw.database.svc.cluster.local:5432\n</code></pre>"},{"location":"infrastructure/databases/#recovery","title":"Recovery","text":"<p>A recovery cluster definition exists at <code>kubernetes/apps/database/cloudnative-pg/recovery/cluster.yaml</code> for disaster recovery scenarios.</p>"},{"location":"infrastructure/databases/#onboarding-a-new-app","title":"Onboarding a new app","text":"<p>The repository's standard pattern is one shared cluster, per-app database + role, provisioned by an <code>init-db</code> initContainer using <code>ghcr.io/home-operations/postgres-init</code>. The init container reads <code>INIT_POSTGRES_SUPER_PASS</code> (from the <code>cloudnative-pg</code> 1Password item) and creates the database and a non-superuser role for the app.</p> <pre><code>initContainers:\n  init-db:\n    image:\n      repository: ghcr.io/home-operations/postgres-init\n      tag: 18@sha256:...\n    envFrom:\n      - secretRef:\n          name: &lt;app&gt;-secret\n</code></pre> <p>The matching <code>ExternalSecret</code> template:</p> <pre><code>template:\n  data:\n    INIT_POSTGRES_DBNAME: \"{{ .APP_POSTGRES_DB }}\"\n    INIT_POSTGRES_HOST: postgres-rw.database.svc.cluster.local\n    INIT_POSTGRES_PORT: \"5432\"\n    INIT_POSTGRES_USER: \"{{ .APP_POSTGRES_USER }}\"\n    INIT_POSTGRES_PASS: \"{{ .APP_POSTGRES_PASSWORD }}\"\n    INIT_POSTGRES_SUPER_PASS: \"{{ .POSTGRES_SUPER_PASS }}\"\n</code></pre> <p>Apps then connect with their own role (<code>DATABASE_USER</code>/<code>DATABASE_PASS</code>).</p>"},{"location":"infrastructure/databases/#apps-that-need-postgresql-extensions","title":"Apps that need PostgreSQL extensions","text":"<p><code>CREATE EXTENSION</code> for \"trusted\" extensions like <code>pgcrypto</code> or <code>uuid-ossp</code> works as a regular role, but <code>cube</code>, <code>earthdistance</code>, <code>postgis</code>, <code>pg_stat_statements</code>, etc. require superuser. The per-app role created by <code>init-db</code> is intentionally not a superuser, so any migration that tries to install these extensions will fail with:</p> <pre><code>ERROR 42501 (insufficient_privilege) permission denied to create extension \"earthdistance\"\nhint: Must be superuser to create this extension.\n</code></pre> <p>Pre-create the extensions in a second initContainer that connects as the postgres superuser. Example from TeslaMate (<code>kubernetes/apps/observability/teslamate/app/helmrelease.yaml</code>):</p> <pre><code>initContainers:\n  init-db:\n    image:\n      repository: ghcr.io/home-operations/postgres-init\n      tag: 18@sha256:...\n    envFrom:\n      - secretRef:\n          name: teslamate-secret\n  init-extensions:\n    image:\n      repository: ghcr.io/home-operations/postgres-init\n      tag: 18@sha256:...\n    command:\n      - /bin/sh\n      - -c\n      - |\n        PGPASSWORD=\"$INIT_POSTGRES_SUPER_PASS\" psql \\\n          -h \"$INIT_POSTGRES_HOST\" \\\n          -p \"$INIT_POSTGRES_PORT\" \\\n          -U postgres \\\n          -d \"$INIT_POSTGRES_DBNAME\" \\\n          -v ON_ERROR_STOP=1 \\\n          -v app_user=\"$INIT_POSTGRES_USER\" &lt;&lt;'SQL'\n        -- 1. Install + upgrade the non-trusted geo extensions as superuser.\n        CREATE EXTENSION IF NOT EXISTS cube WITH SCHEMA public;\n        CREATE EXTENSION IF NOT EXISTS earthdistance WITH SCHEMA public;\n        ALTER EXTENSION earthdistance UPDATE;\n\n        -- 2. Transfer ownership of every cube/earthdistance member\n        --    function to the app role so the migration's ALTER FUNCTION\n        --    calls succeed when the app runs them as itself.\n        SELECT 'ALTER FUNCTION ' || objid::regprocedure::text\n               || ' OWNER TO ' || quote_ident(:'app_user') || ';'\n        FROM pg_depend\n        WHERE classid='pg_proc'::regclass AND deptype='e'\n          AND refclassid='pg_extension'::regclass\n          AND refobjid IN (SELECT oid FROM pg_extension\n                           WHERE extname IN ('cube','earthdistance'))\n        \\gexec\n\n        -- 3. Pre-create Ecto's schema_migrations table and stamp the\n        --    superuser-only migrations as already applied so the app\n        --    skips them. The DDL they would run has been done above.\n        CREATE TABLE IF NOT EXISTS public.schema_migrations (\n          version     bigint       PRIMARY KEY,\n          inserted_at timestamp(0) WITHOUT TIME ZONE NOT NULL\n        );\n        ALTER TABLE public.schema_migrations OWNER TO :\"app_user\";\n\n        INSERT INTO public.schema_migrations (version, inserted_at) VALUES\n          (20240929084639, NOW()),  -- recreate_geo_extensions\n          (20250407155134, NOW())   -- upgrade_earthdistance\n        ON CONFLICT (version) DO NOTHING;\n        SQL\n    envFrom:\n      - secretRef:\n          name: teslamate-secret\n</code></pre> <p>Notes:</p> <ul> <li>Reuse the <code>postgres-init</code> image \u2014 it ships <code>psql</code> and matches the existing pattern.</li> <li>Connect as <code>-U postgres</code>, not as the app's role, so the <code>CREATE EXTENSION</code> succeeds.</li> <li>The whole script is idempotent: <code>CREATE EXTENSION IF NOT EXISTS</code>, <code>ALTER EXTENSION ... UPDATE</code> (no-op when current), <code>\\gexec</code>-driven ownership transfer (no-op when the role already owns it), <code>CREATE TABLE IF NOT EXISTS</code>, and <code>INSERT ... ON CONFLICT DO NOTHING</code>. Safe to re-run on every pod restart.</li> <li>Set <code>-v ON_ERROR_STOP=1</code> so a failed step aborts the init and surfaces the error in pod events instead of silently letting the app start with a broken schema.</li> <li>Don't promote the app role to SUPERUSER. It looks tempting but PostgreSQL's SUPERUSER attribute is cluster-wide, not per-database \u2014 the app role would gain read/write access to every other CNPG-backed database in the same cluster (kguardian, teslamate, forgejo, \u2026). In a shared-cluster setup that's an unacceptable blast radius.</li> <li><code>GRANT postgres TO app</code> doesn't help for <code>CREATE EXTENSION</code>. Role membership inherits privileges (table GRANTs, etc.) but not role attributes like <code>SUPERUSER</code>, and <code>CREATE EXTENSION</code> on non-trusted extensions checks the SUPERUSER attribute.</li> <li>Why the fake-stamp pattern works: Ecto records applied migrations in <code>schema_migrations</code>. If a migration's version is already in that table when the migrator boots, Ecto skips it. Pre-create the table (matching the DDL Ecto would emit), do the superuser DDL yourself as postgres, and <code>INSERT</code> the version. The app then sees no work to do.</li> <li>What you must keep in sync when the upstream app adds a new superuser-requiring migration: replicate its DDL in step 1/2 above, and add its version to the <code>INSERT</code> in step 3. The migration list as of TeslaMate 3.0.0:</li> <li><code>20190925152807_create_geo_extensions</code> \u2014 not skipped; runs fine as the app role once function ownership is transferred (the inner <code>CREATE EXTENSION IF NOT EXISTS</code> short-circuits before its privilege check, the <code>ALTER FUNCTION</code> calls succeed because of the ownership transfer, and the <code>CREATE INDEX</code> runs as the table owner).</li> <li><code>20240929084639_recreate_geo_extensions</code> \u2014 skipped; <code>DROP EXTENSION cube CASCADE</code> requires extension ownership which can't be granted.</li> <li><code>20250407155134_upgrade_earthdistance</code> \u2014 skipped; we run the same <code>ALTER EXTENSION earthdistance UPDATE</code> as postgres in step 1.</li> </ul>"},{"location":"infrastructure/databases/#mariadb-operator-mariadb-galera","title":"MariaDB Operator (MariaDB Galera)","text":"<p>MariaDB Operator runs a MariaDB 11.7 high-availability Galera cluster with 3 instances.</p>"},{"location":"infrastructure/databases/#architecture_1","title":"Architecture","text":"<pre><code>kubernetes/apps/database/mariadb-operator/\n\u251c\u2500\u2500 app/                       # Operator deployment\n\u2502   \u251c\u2500\u2500 helmrelease-crds.yaml  # CRDs HelmRelease\n\u2502   \u251c\u2500\u2500 helmrelease.yaml       # Operator HelmRelease\n\u2502   \u251c\u2500\u2500 helmrepository.yaml    # Helm repo source\n\u2502   \u2514\u2500\u2500 kustomization.yaml\n\u251c\u2500\u2500 cluster/                   # MariaDB Galera cluster\n\u2502   \u251c\u2500\u2500 mariadb.yaml          # MariaDB CR (Galera)\n\u2502   \u251c\u2500\u2500 backup.yaml           # Scheduled S3 backup\n\u2502   \u251c\u2500\u2500 externalsecret.yaml   # 1Password credentials\n\u2502   \u2514\u2500\u2500 kustomization.yaml\n\u2514\u2500\u2500 ks.yaml                   # Flux Kustomizations\n</code></pre>"},{"location":"infrastructure/databases/#configuration_1","title":"Configuration","text":"Setting Value Instances 3 (Galera multi-master with pod anti-affinity) Storage 20Gi per instance (openebs-hostpath) Max connections 200 InnoDB buffer pool 256MB Max allowed packet 256MB CPU request 100m Memory request 512Mi Memory limit 2Gi"},{"location":"infrastructure/databases/#backups_1","title":"Backups","text":"<ul> <li>Scheduled backups to Garage S3 every 6 hours (<code>0 */6 * * *</code>)</li> <li>Retention: 30 days</li> <li>Compression: bzip2</li> <li>S3 bucket: <code>mariadb-backups</code> (prefix <code>galera</code>)</li> <li>Method: <code>mysqldump</code> with <code>--single-transaction --all-databases</code></li> </ul>"},{"location":"infrastructure/databases/#connecting_1","title":"Connecting","text":"<p>Applications connect via internal services:</p> <pre><code># All instances (load-balanced)\nmariadb.database.svc.cluster.local:3306\n\n# Primary only\nmariadb-primary.database.svc.cluster.local:3306\n\n# Read replicas\nmariadb-secondary.database.svc.cluster.local:3306\n</code></pre>"},{"location":"infrastructure/databases/#operator-installation","title":"Operator Installation","text":"<p>The operator is installed via two separate HelmReleases from the <code>helm.mariadb.com</code> Helm repository:</p> <ol> <li>mariadb-operator-crds \u2014 installs Custom Resource Definitions</li> <li>mariadb-operator \u2014 installs the controller (depends on CRDs)</li> </ol> <p>The operator includes Prometheus metrics via ServiceMonitor and cert-manager webhook integration.</p>"},{"location":"infrastructure/databases/#freepbx-databases","title":"FreePBX Databases","text":"<p>The MariaDB cluster hosts FreePBX databases managed via operator CRs in <code>kubernetes/apps/voip/freepbx/database/</code>:</p> Resource Name Purpose Database <code>b1_asterisk</code> Main Asterisk configuration Database <code>b1_asteriskcdrdb</code> Call Detail Records User <code>freepbx</code> Application user (max 100 connections) Grant <code>ALL PRIVILEGES</code> Full access on both databases"},{"location":"infrastructure/databases/#dragonfly","title":"Dragonfly","text":"<p>Dragonfly is a modern Redis-compatible in-memory datastore:</p> <ul> <li>Deploys the Dragonfly Operator for managing instances</li> <li>Higher performance alternative to Redis/Valkey</li> <li>Used by applications requiring fast caching or session storage</li> </ul>"},{"location":"infrastructure/databases/#dbgate","title":"DBGate","text":"<p>DBGate provides a web UI for database management:</p> <ul> <li>Located in <code>kubernetes/apps/database/dbgate/</code></li> <li>Kanidm SSO integration for authentication</li> <li>Accessible via Envoy Gateway</li> </ul>"},{"location":"infrastructure/envoy-gateway/","title":"Envoy Gateway","text":"<p>Envoy Gateway v1.6.3 provides HTTP routing and ingress using the Kubernetes Gateway API.</p>"},{"location":"infrastructure/envoy-gateway/#architecture","title":"Architecture","text":"<p>Envoy Gateway deploys Envoy Proxy instances that handle incoming traffic:</p> <ul> <li>envoy-internal -- Private network access (split DNS)</li> <li>envoy-external -- Public access via Cloudflare Tunnel</li> </ul>"},{"location":"infrastructure/envoy-gateway/#configuration","title":"Configuration","text":"<pre><code>kubernetes/apps/network/envoy-gateway/\n\u251c\u2500\u2500 app/\n\u2502   \u251c\u2500\u2500 helmrelease.yaml      # Envoy Gateway operator\n\u2502   \u251c\u2500\u2500 ocirepository.yaml    # Chart source\n\u2502   \u251c\u2500\u2500 certificate.yaml      # TLS wildcard certificate\n\u2502   \u251c\u2500\u2500 envoy.yaml            # Gateway config, policies, responseOverride\n\u2502   \u251c\u2500\u2500 grafanadashboard.yaml # Monitoring dashboard\n\u2502   \u251c\u2500\u2500 podmonitor.yaml       # Prometheus metrics\n\u2502   \u2514\u2500\u2500 kustomization.yaml\n\u251c\u2500\u2500 error-pages/\n\u2502   \u251c\u2500\u2500 helmrelease.yaml      # Error pages service (app-template)\n\u2502   \u251c\u2500\u2500 ocirepository.yaml    # Chart source\n\u2502   \u2514\u2500\u2500 kustomization.yaml\n\u2514\u2500\u2500 ks.yaml                   # Flux Kustomizations (gateway + error-pages)\n</code></pre>"},{"location":"infrastructure/envoy-gateway/#exposing-applications","title":"Exposing Applications","text":""},{"location":"infrastructure/envoy-gateway/#internal-only","title":"Internal Only","text":"<pre><code>apiVersion: gateway.networking.k8s.io/v1\nkind: HTTPRoute\nmetadata:\n  name: my-app\nspec:\n  parentRefs:\n    - name: envoy-internal\n      namespace: network\n  hostnames:\n    - \"my-app.example.com\"\n  rules:\n    - backendRefs:\n        - name: my-app\n          port: 80\n</code></pre>"},{"location":"infrastructure/envoy-gateway/#public-via-cloudflare","title":"Public (via Cloudflare)","text":"<pre><code>apiVersion: gateway.networking.k8s.io/v1\nkind: HTTPRoute\nmetadata:\n  name: my-app\nspec:\n  parentRefs:\n    - name: envoy-external\n      namespace: network\n  hostnames:\n    - \"my-app.example.com\"\n  rules:\n    - backendRefs:\n        - name: my-app\n          port: 80\n</code></pre>"},{"location":"infrastructure/envoy-gateway/#sso-integration","title":"SSO Integration","text":"<p>Envoy Gateway supports OIDC SecurityPolicy for Kanidm SSO:</p> <pre><code>apiVersion: gateway.envoyproxy.io/v1alpha1\nkind: SecurityPolicy\nmetadata:\n  name: kanidm-oidc\nspec:\n  targetRefs:\n    - group: gateway.networking.k8s.io\n      kind: HTTPRoute\n      name: &lt;app-route&gt;\n  oidc:\n    provider:\n      issuer: \"https://idm.example.com/oauth2/openid/&lt;client&gt;\"\n    clientID: \"&lt;client-name&gt;\"\n    clientSecret:\n      name: \"&lt;secret-name&gt;\"\n</code></pre>"},{"location":"infrastructure/envoy-gateway/#custom-error-pages","title":"Custom Error Pages","text":"<p>Error Pages provides styled error responses for all routes behind both gateways. When a backend returns an error, the <code>BackendTrafficPolicy</code> <code>responseOverride</code> redirects the client to the error-pages service.</p> <p>Handled status codes: 403, 404, 500, 502, 503, 504</p> <p>The <code>responseOverride</code> in the <code>BackendTrafficPolicy</code> (in <code>envoy.yaml</code>) uses redirect rules:</p> <pre><code>responseOverride:\n  - match:\n      statusCodes:\n        - type: Value\n          value: 404\n    redirect:\n      scheme: https\n      hostname: error.example.com\n      path:\n        type: ReplaceFullPath\n        replaceFullPath: /404.html\n      statusCode: 302\n</code></pre> <p>This applies globally to all routes behind <code>envoy-internal</code> and <code>envoy-external</code> since the policy targets all Gateways via <code>targetSelectors</code>.</p> <p>Template: The error-pages service uses the \"hacker-terminal\" template with <code>SHOW_DETAILS=true</code>.</p>"},{"location":"infrastructure/envoy-gateway/#per-route-backendtrafficpolicy","title":"Per-Route BackendTrafficPolicy","text":"<p>The global <code>BackendTrafficPolicy</code> (compression, error page redirects) targets all Gateways. Individual applications can override this by attaching a <code>BackendTrafficPolicy</code> to their <code>HTTPRoute</code>:</p> <pre><code>apiVersion: gateway.envoyproxy.io/v1alpha1\nkind: BackendTrafficPolicy\nmetadata:\n  name: my-app-backend\nspec:\n  targetRefs:\n    - group: gateway.networking.k8s.io\n      kind: HTTPRoute\n      name: my-app\n  # Override specific settings \u2014 omitted fields use defaults, NOT the gateway policy\n  connection:\n    bufferLimit: 16Mi\n  timeout:\n    http:\n      requestTimeout: 0s\n</code></pre> <p>When a route-level policy exists alongside a gateway-level policy, the route-level policy wins for that route. All other routes continue using the gateway-level policy.</p> <p>LibreSpeed speed test</p> <p>The LibreSpeed deployment uses this pattern to disable compression and error page redirects on speed test routes, since compressing random data wastes CPU and error page matching adds overhead.</p> <p>ClientTrafficPolicy is gateway-only</p> <p>Unlike <code>BackendTrafficPolicy</code>, <code>ClientTrafficPolicy</code> cannot target <code>HTTPRoute</code> \u2014 only <code>Gateway</code> (optionally scoped to a listener via <code>sectionName</code>). Client-side settings (HTTP/2 windows, TLS, HTTP/3) always apply to all routes on a listener.</p>"},{"location":"infrastructure/envoy-gateway/#monitoring","title":"Monitoring","text":"<ul> <li>Grafana dashboard enabled via GrafanaOperator</li> <li>PodMonitor exports metrics to Prometheus</li> </ul>"},{"location":"infrastructure/flux/","title":"Flux CD","text":"<p>Flux CD v2.7.5 provides GitOps continuous delivery, automatically syncing the cluster state from Git.</p>"},{"location":"infrastructure/flux/#architecture","title":"Architecture","text":"<p>The cluster uses the Flux Operator pattern:</p> <ul> <li>flux-operator (v0.41.1) -- Manages Flux components lifecycle</li> <li>flux-instance (v0.41.1) -- Configured Flux deployment with performance tuning</li> </ul>"},{"location":"infrastructure/flux/#performance-tuning","title":"Performance Tuning","text":"<p>The Flux instance is configured with:</p> <ul> <li>10 concurrent workers for Kustomize and Helm controllers</li> <li>1Gi memory limits for controllers</li> <li>Helm caching enabled for faster reconciliation</li> <li>OOM detection enabled</li> <li>SOPS decryption configured for Age keys</li> </ul>"},{"location":"infrastructure/flux/#how-it-works","title":"How It Works","text":"<pre><code>graph TD\n    A[Git Push] --&gt; B[Flux detects change]\n    B --&gt; C[Reconcile Kustomizations]\n    C --&gt; D[Process HelmReleases]\n    D --&gt; E[Apply to Cluster]\n    E --&gt; F[Report Status]</code></pre> <ol> <li>Flux watches the Git repository (via webhook or polling)</li> <li>Kustomizations define which paths to reconcile</li> <li>HelmReleases deploy applications from OCI registries</li> <li>SOPS secrets are decrypted automatically</li> <li>Post-build variable substitution injects cluster secrets</li> </ol>"},{"location":"infrastructure/flux/#key-patterns","title":"Key Patterns","text":""},{"location":"infrastructure/flux/#kustomization","title":"Kustomization","text":"<pre><code>apiVersion: kustomize.toolkit.fluxcd.io/v1\nkind: Kustomization\nmetadata:\n  name: &lt;app-name&gt;\nspec:\n  interval: 1h\n  path: ./kubernetes/apps/&lt;namespace&gt;/&lt;app&gt;/app\n  postBuild:\n    substituteFrom:\n      - name: cluster-secrets\n        kind: Secret\n  prune: true\n  sourceRef:\n    kind: GitRepository\n    name: flux-system\n    namespace: flux-system\n  targetNamespace: &lt;namespace&gt;\n  wait: false\n</code></pre>"},{"location":"infrastructure/flux/#helmrelease","title":"HelmRelease","text":"<pre><code>apiVersion: helm.toolkit.fluxcd.io/v2\nkind: HelmRelease\nmetadata:\n  name: &lt;app-name&gt;\nspec:\n  chartRef:\n    kind: OCIRepository\n    name: &lt;chart-name&gt;\n  interval: 1h\n  values:\n    # Application-specific values\n</code></pre>"},{"location":"infrastructure/flux/#common-operations","title":"Common Operations","text":""},{"location":"infrastructure/flux/#force-reconciliation","title":"Force Reconciliation","text":"<pre><code>task reconcile\n# or\nflux --namespace flux-system reconcile kustomization flux-system --with-source\n</code></pre>"},{"location":"infrastructure/flux/#check-status","title":"Check Status","text":"<pre><code>flux check                    # Health check\nflux get sources git -A       # Git sources\nflux get ks -A                # Kustomizations\nflux get hr -A                # HelmReleases\n</code></pre>"},{"location":"infrastructure/flux/#view-logs","title":"View Logs","text":"<pre><code>flux logs --all-namespaces\n</code></pre>"},{"location":"infrastructure/flux/#suspendresume","title":"Suspend/Resume","text":"<pre><code>flux suspend hr &lt;name&gt; -n &lt;namespace&gt;\nflux resume hr &lt;name&gt; -n &lt;namespace&gt;\n</code></pre>"},{"location":"infrastructure/identity/","title":"Identity &amp; SSO","text":""},{"location":"infrastructure/identity/#kanidm","title":"Kanidm","text":"<p>Kanidm provides centralized identity management and OAuth2/OIDC single sign-on for the cluster.</p>"},{"location":"infrastructure/identity/#architecture","title":"Architecture","text":"<p>Located in <code>kubernetes/apps/identity/kanidm/</code>:</p> <ul> <li>Deployed via the Kaniop Kubernetes operator</li> <li>Single Rust binary with embedded database (no external PostgreSQL needed)</li> <li>~80 MB RAM footprint</li> <li>Manages users, groups, and OAuth2 clients declaratively via CRDs</li> </ul>"},{"location":"infrastructure/identity/#oauth2-integrations","title":"OAuth2 Integrations","text":"<p>Kanidm provides SSO for these applications:</p> Application Namespace Authentication DBGate database OAuth2/OIDC Forgejo utils OAuth2/OIDC KubeVirt Manager kubevirt OAuth2/OIDC OpenCost observability OAuth2/OIDC Penpot utils OAuth2/OIDC <p>Each OAuth2 client is defined as a separate YAML file under <code>kubernetes/apps/identity/kanidm/app/</code>.</p>"},{"location":"infrastructure/identity/#features","title":"Features","text":"<ul> <li>OAuth2/OIDC -- Certified provider with mandatory PKCE</li> <li>Passkeys/WebAuthn -- Best-in-class (maintains <code>webauthn-rs</code>)</li> <li>RADIUS -- Built-in for WiFi WPA2-Enterprise</li> <li>SSH key distribution -- Cached and direct modes</li> <li>Unix/PAM integration -- TPM-backed credential caching</li> <li>LDAP -- Read-only gateway</li> </ul>"},{"location":"infrastructure/identity/#adding-sso-to-a-new-application","title":"Adding SSO to a New Application","text":"<ol> <li>Create a <code>KanidmOAuth2Client</code> CRD in <code>kubernetes/apps/identity/kanidm/app/</code></li> <li>Configure the application with the OIDC provider URL and client credentials</li> <li>Optionally add an Envoy Gateway <code>SecurityPolicy</code> for apps without native OIDC</li> </ol>"},{"location":"infrastructure/identity/#envoy-gateway-oidc-integration","title":"Envoy Gateway OIDC Integration","text":"<p>For apps without native OIDC support, use Envoy Gateway's SecurityPolicy:</p> <pre><code>apiVersion: gateway.envoyproxy.io/v1alpha1\nkind: SecurityPolicy\nmetadata:\n  name: app-oidc\nspec:\n  targetRefs:\n    - group: gateway.networking.k8s.io\n      kind: HTTPRoute\n      name: &lt;app-route&gt;\n  oidc:\n    provider:\n      issuer: \"https://idm.example.com/oauth2/openid/&lt;client&gt;\"\n    clientID: \"&lt;client-name&gt;\"\n    clientSecret:\n      name: \"&lt;secret-name&gt;\"\n</code></pre>"},{"location":"infrastructure/identity/#kguardian","title":"kGuardian","text":"<p>kGuardian provides eBPF-based security monitoring:</p> <ul> <li>Broker (v1.6.0) -- Message broker for security events</li> <li>Controller (v1.7.0) -- Core monitoring engine</li> <li>Frontend (v1.6.2) -- Web UI for security insights</li> <li>Uses external CloudNative-PG database</li> <li>Auto-generates NetworkPolicy and seccomp profiles from observed runtime behavior</li> </ul>"},{"location":"infrastructure/secrets/","title":"Secrets Management","text":""},{"location":"infrastructure/secrets/#overview","title":"Overview","text":"<p>Secrets are managed through two complementary systems:</p> <ol> <li>SOPS + Age -- Encrypts secrets stored in Git</li> <li>External Secrets Operator -- Pulls secrets from 1Password at runtime</li> </ol>"},{"location":"infrastructure/secrets/#sops-age","title":"SOPS + Age","text":"<p>SOPS encrypts YAML files using Age keys.</p>"},{"location":"infrastructure/secrets/#encryption-rules-sopsyaml","title":"Encryption Rules (<code>.sops.yaml</code>)","text":"Path Pattern Encryption Scope <code>talos/.*\\.sops\\.ya?ml</code> Full file encryption <code>(bootstrap\\|kubernetes)/.*\\.sops\\.ya?ml</code> Only <code>data</code> and <code>stringData</code> fields"},{"location":"infrastructure/secrets/#encrypting-a-secret","title":"Encrypting a Secret","text":"<pre><code>sops --encrypt --in-place kubernetes/apps/&lt;namespace&gt;/&lt;app&gt;/app/secret.sops.yaml\n</code></pre>"},{"location":"infrastructure/secrets/#decrypting-a-secret","title":"Decrypting a Secret","text":"<pre><code>sops --decrypt kubernetes/apps/&lt;namespace&gt;/&lt;app&gt;/app/secret.sops.yaml\n</code></pre>"},{"location":"infrastructure/secrets/#key-management","title":"Key Management","text":"<ul> <li>Age public key: defined in <code>.sops.yaml</code></li> <li>Age private key: stored in <code>age.key</code> (gitignored)</li> <li>Environment variable: <code>SOPS_AGE_KEY_FILE</code> points to <code>age.key</code></li> </ul>"},{"location":"infrastructure/secrets/#external-secrets-operator","title":"External Secrets Operator","text":"<p>External Secrets Operator v2.0.0 syncs secrets from external providers into Kubernetes.</p>"},{"location":"infrastructure/secrets/#1password-integration","title":"1Password Integration","text":"<p>A <code>ClusterSecretStore</code> connects to 1Password:</p> <pre><code>kubernetes/apps/external-secrets/\n\u251c\u2500\u2500 external-secrets/    # Operator deployment\n\u251c\u2500\u2500 onepassword/         # ClusterSecretStore config\n\u2514\u2500\u2500 discord-webhook/     # Discord integration\n</code></pre> <p>Applications reference secrets via <code>ExternalSecret</code> resources that pull values from 1Password vaults.</p>"},{"location":"infrastructure/secrets/#creating-an-externalsecret","title":"Creating an ExternalSecret","text":"<pre><code>apiVersion: external-secrets.io/v1\nkind: ExternalSecret\nmetadata:\n  name: app-secret\nspec:\n  secretStoreRef:\n    kind: ClusterSecretStore\n    name: onepassword\n  target:\n    name: app-secret\n  data:\n    - secretKey: password\n      remoteRef:\n        key: my-1password-item\n        property: password\n</code></pre>"},{"location":"infrastructure/secrets/#best-practices","title":"Best Practices","text":"<p>Never commit unencrypted secrets</p> <p>All sensitive data must be in <code>*.sops.yaml</code> files. Verify encryption by checking for <code>sops:</code> metadata in the file.</p> <ul> <li>Use <code>ExternalSecret</code> for runtime secrets from 1Password</li> <li>Use <code>SOPS</code> for secrets that must exist in Git (bootstrap, cluster config)</li> <li>Always validate that <code>*.sops.yaml</code> files are encrypted before pushing</li> <li>Renovate is configured to ignore <code>**/*.sops.*</code> files</li> </ul>"},{"location":"infrastructure/storage/","title":"Storage","text":"<p>The cluster uses multiple storage backends for different workload requirements.</p>"},{"location":"infrastructure/storage/#storage-classes","title":"Storage Classes","text":"Class Backend Access Modes Use Case <code>openebs-hostpath</code> OpenEBS RWO Database volumes, high-performance local storage <code>nfs-fast</code> CSI Driver NFS RWX VM disks, shared media, multi-node access"},{"location":"infrastructure/storage/#openebs","title":"OpenEBS","text":"<p>OpenEBS provides cloud-native local storage:</p> <ul> <li>hostpath provisioner for fast local volumes</li> <li>Used by PostgreSQL, Dragonfly, and other stateful workloads</li> <li>CDI scratch space for VM disk imports</li> </ul> <pre><code>kubernetes/apps/openebs-system/openebs/\n\u251c\u2500\u2500 app/\n\u2502   \u251c\u2500\u2500 helmrelease.yaml\n\u2502   \u251c\u2500\u2500 ocirepository.yaml\n\u2502   \u2514\u2500\u2500 kustomization.yaml\n\u2514\u2500\u2500 ks.yaml\n</code></pre>"},{"location":"infrastructure/storage/#nfs-storage","title":"NFS Storage","text":"<p>CSI Driver NFS provides shared network storage:</p> <ul> <li>ReadWriteMany support for multi-node access</li> <li>Used for VM disks (enables live migration)</li> <li>Used for shared media storage</li> <li>NFS-scaler component prevents pods from crash-looping when NFS is unavailable</li> </ul>"},{"location":"infrastructure/storage/#backup-system","title":"Backup System","text":""},{"location":"infrastructure/storage/#volsync","title":"VolSync","text":"<p>VolSync handles volume replication and backup:</p> <ul> <li>Backs up PersistentVolumeClaims to the on-NFS Kopia repository at <code>/mnt/Speed/VolsyncKopia</code> (not Garage \u2014 VolSync's Kopia mover talks to a filesystem-backed repo, not S3)</li> <li>Scheduled daily at 2 AM, retention 24h/7d/4w/6m/2y</li> <li>Component available at <code>kubernetes/components/volsync/</code></li> <li>Apps using it: forgejo, plex, sonarr, radarr, prowlarr, bazarr, autobrr, qbittorrent, qui, seerr, tautulli, thelounge, unifi-toolkit, gatus, penpot \u2014 see <code>scripts/volsync-restore-all.sh</code> for the canonical list</li> </ul>"},{"location":"infrastructure/storage/#garage","title":"Garage","text":"<p>Garage provides S3-compatible storage. Single-replica deployment in the cluster:</p> <ul> <li>Data dir lives on NFS (<code>/mnt/Speed/Kubernetes/apps/garage/data/</code>) \u2014 safe there because object shards are append-only files, no locking required.</li> <li>Meta dir lives on a local <code>openebs-hostpath</code> PVC named <code>garage-meta</code>. LMDB (Garage's metadata store) does not work over NFS \u2014 file-lock semantics fail with <code>Resource temporarily unavailable</code>, leaving Garage running but unable to read its bucket/key tables. The PVC takes the meta off NFS.</li> <li>Off-node meta durability: a <code>backup-sync</code> sidecar inside the Garage pod rsyncs <code>/meta/</code> (minus the live <code>db.lmdb/</code> dir, which is exclusively locked) to NFS at <code>/mnt/Speed/Kubernetes/apps/garage/meta-backup/</code> every 24h. PrometheusRules <code>GarageMetaBackupSidecarRestarted</code> and <code>GarageMetaBackupSidecarAbsent</code> alert via AlertManager \u2192 Discord if the sidecar fails.</li> <li>Used by: CloudNative-PG (WAL + base backups), MariaDB Operator (scheduled backups), Kanidm (hourly JSON dumps via CronJob). Restore procedure documented in Node Loss Recovery.</li> </ul>"},{"location":"infrastructure/storage/#kopia","title":"Kopia","text":"<p>Kopia is the backup repository used by VolSync's mover:</p> <ul> <li>Filesystem-type repository at <code>/mnt/Speed/VolsyncKopia</code> on the NAS</li> <li>Deduplication + encryption at rest</li> <li>One repo, multiple <code>sourceIdentity</code> namespaces (one per app)</li> <li>Repository credentials managed via the <code>volsync-template</code> 1Password item</li> </ul>"},{"location":"infrastructure/storage/#snapshot-controller","title":"Snapshot Controller","text":"<p>The snapshot-controller enables volume snapshots for point-in-time recovery.</p>"},{"location":"infrastructure/storage/#nfs-scaler-component","title":"NFS Scaler Component","text":"<p>Located in <code>kubernetes/components/nfs-scaler/</code>, this KEDA-based component monitors NFS availability:</p> <ul> <li>Queries Prometheus for <code>probe_success{instance=~\".+:2049\"}</code> metric</li> <li>Scales deployments 0 \u2192 1 when NFS is available</li> <li>Scales down to 0 when NFS is unavailable</li> <li>Prevents crash-loop storms when NFS is down</li> </ul> <p>Apply to any app that mounts NFS volumes.</p>"},{"location":"infrastructure/talos/","title":"Talos Linux","text":"<p>Talos Linux is an immutable, minimal OS designed specifically for Kubernetes. Version 1.12.4 is deployed.</p>"},{"location":"infrastructure/talos/#configuration","title":"Configuration","text":"<p>Talos configuration is managed through talhelper and Jinja2 templates.</p>"},{"location":"infrastructure/talos/#file-locations","title":"File Locations","text":"Path Purpose <code>templates/config/talos/talconfig.yaml.j2</code> Main Talos config template <code>talos/patches/global/</code> Patches applied to all nodes <code>talos/patches/controller/</code> Controller-specific patches <code>talos/patches/worker/</code> Worker-specific patches <code>talos/patches/vm-node/</code> KubeVirt VM node patches <code>talos/patches/${hostname}/</code> Per-node patches <code>talos/clusterconfig/</code> Generated configs (gitignored)"},{"location":"infrastructure/talos/#patch-system","title":"Patch System","text":"<p>Talos uses a layered patch system. Patches are applied in order:</p> <ol> <li>Global patches (all nodes)</li> <li>Role-specific patches (controller or worker)</li> <li>VM-node patches (KubeVirt nodes)</li> <li>Per-hostname patches</li> </ol>"},{"location":"infrastructure/talos/#common-operations","title":"Common Operations","text":""},{"location":"infrastructure/talos/#generate-config","title":"Generate Config","text":"<pre><code>task talos:generate-config\n</code></pre>"},{"location":"infrastructure/talos/#apply-config-to-a-node","title":"Apply Config to a Node","text":"<pre><code>task talos:apply-node IP=10.10.10.10 MODE=auto\n</code></pre> <p>Mode options: <code>auto</code>, <code>no-reboot</code>, <code>reboot</code>, <code>staged</code></p>"},{"location":"infrastructure/talos/#upgrade-talos-version","title":"Upgrade Talos Version","text":"<pre><code>task talos:upgrade-node IP=10.10.10.10\n</code></pre> <p>Tip</p> <p>Update <code>talosVersion</code> in <code>talenv.yaml</code> before upgrading.</p>"},{"location":"infrastructure/talos/#upgrade-kubernetes-version","title":"Upgrade Kubernetes Version","text":"<pre><code>task talos:upgrade-k8s\n</code></pre>"},{"location":"infrastructure/talos/#reset-cluster","title":"Reset Cluster","text":"<p>Danger</p> <p>This destroys the entire cluster. Repeated resets may trigger rate limits from DockerHub or Let's Encrypt.</p> <pre><code>task talos:reset\n</code></pre>"},{"location":"infrastructure/talos/#adding-a-new-node","title":"Adding a New Node","text":"<ol> <li>Boot the new node with Talos in maintenance mode</li> <li> <p>Get disk and MAC address info:</p> <pre><code>talosctl get disks -n &lt;ip&gt; --insecure\ntalosctl get links -n &lt;ip&gt; --insecure\n</code></pre> </li> <li> <p>Add the node to <code>talconfig.yaml</code></p> </li> <li> <p>Generate and apply:</p> <pre><code>task talos:generate-config\ntask talos:apply-node IP=&lt;new-ip&gt;\n</code></pre> </li> </ol> <p>The node joins automatically and begins accepting workloads.</p>"},{"location":"infrastructure/talos/#debugging","title":"Debugging","text":"<pre><code># Check cluster membership\ntalosctl get members --nodes &lt;ip&gt; --insecure\n\n# View node logs\ntalosctl logs --nodes &lt;ip&gt; --insecure\n\n# Check node services\ntalosctl services --nodes &lt;ip&gt;\n</code></pre>"},{"location":"operations/","title":"Operations","text":"<p>Guides for managing the cluster day-to-day.</p>"},{"location":"operations/#quick-reference","title":"Quick Reference","text":"Task Command Force Flux sync <code>task reconcile</code> List all tasks <code>task --list</code> Generate Talos config <code>task talos:generate-config</code> Apply config to node <code>task talos:apply-node IP=&lt;ip&gt; MODE=auto</code> Upgrade Talos <code>task talos:upgrade-node IP=&lt;ip&gt;</code> Upgrade Kubernetes <code>task talos:upgrade-k8s</code> Reset cluster <code>task talos:reset</code> Validate K8s manifests <code>task template:validate-kubernetes-config</code> VM console <code>task vm:console VM=&lt;name&gt;</code> VM start/stop <code>task vm:start VM=&lt;name&gt;</code> / <code>task vm:stop VM=&lt;name&gt;</code> Mass VolSync restore <code>./scripts/volsync-restore-all.sh</code> Generate GitHub labels <code>./scripts/generate-labels.sh</code>"},{"location":"operations/#pages","title":"Pages","text":"<ul> <li>Day-2 Operations -- Routine maintenance and upgrades</li> <li>Backup &amp; Recovery -- VolSync, Kopia, and disaster recovery</li> <li>Node Loss Recovery -- Recovering from a permanently lost node (orphaned PVCs, dangling instances)</li> <li>VM Management -- KubeVirt virtual machine operations</li> <li>Troubleshooting -- Debugging common issues</li> </ul>"},{"location":"operations/backup-recovery/","title":"Backup &amp; Recovery","text":""},{"location":"operations/backup-recovery/#backup-architecture","title":"Backup Architecture","text":"<pre><code>graph LR\n    PVC[PersistentVolumeClaims] --&gt;|VolSync| Kopia[Kopia Repository&lt;br/&gt;NFS: /mnt/Speed/VolsyncKopia]\n    PG[PostgreSQL WAL/base] --&gt;|barman-cloud| Garage[Garage S3]\n    MDB[MariaDB Galera] --&gt;|mariadb-operator Backup| Garage\n    KD[Kanidm hourly JSON] --&gt;|CronJob aws s3 sync| Garage\n    GM[Garage meta PVC] --&gt;|in-pod rsync sidecar| NFSGarage[NFS: garage/meta-backup]\n    Git[Git Repository] --&gt;|GitOps| State[Cluster State]</code></pre> <p>The cluster uses a layered backup strategy:</p> Data Type Backup Method Destination Schedule Application PVCs VolSync + Kopia NFS Kopia repo (<code>/mnt/Speed/VolsyncKopia</code>) Daily at 2 AM PostgreSQL databases barman-cloud (WAL + base backups) Garage S3 (<code>s3://cnpg/postgres-r2/</code>) Continuous WAL + scheduled MariaDB databases mariadb-operator Backup CR Garage S3 (<code>s3://mariadb-backups/</code>) Every 6 hours Kanidm identity DB <code>kanidm-backup-sync</code> CronJob (<code>aws s3 sync</code>) Garage S3 (<code>s3://kanidm/backups/</code>) Hourly Garage's own LMDB meta in-pod <code>backup-sync</code> sidecar (rsync) NFS (<code>/mnt/Speed/Kubernetes/apps/garage/meta-backup/</code>) Daily Cluster state Git repository GitHub On every push Secrets SOPS-encrypted in Git + 1Password GitHub + 1Password On every push <p>Note on backup destinations: VolSync and Garage's meta backup go to NFS; everything else goes to Garage S3. Garage's own metadata is on a local PVC and can't be safely backed up via VolSync (RWO + no CSI snapshot support), so a sidecar rsync was used instead \u2014 see Node Loss Recovery \u2014 Garage metadata recovery.</p>"},{"location":"operations/backup-recovery/#volsync","title":"VolSync","text":"<p>VolSync replicates PersistentVolumeClaims to S3-compatible storage.</p>"},{"location":"operations/backup-recovery/#configuration-details","title":"Configuration Details","text":"<p>The VolSync component at <code>kubernetes/components/volsync/</code> provides reusable backup/restore templates:</p> Setting Value Schedule <code>0 2 * * *</code> (daily at 2 AM UTC) Compression <code>zstd-fastest</code> Copy method <code>Direct</code> Parallelism <code>2</code> threads Cache storage <code>openebs-hostpath</code> (5Gi) Mover user UID/GID 1000 <p>Retention policy:</p> <ul> <li>24 hourly, 7 daily, 4 weekly, 6 monthly, 2 yearly</li> </ul>"},{"location":"operations/backup-recovery/#applying-volsync-to-an-application","title":"Applying VolSync to an Application","text":"<p>Reference the VolSync component in your app's <code>ks.yaml</code>:</p> <pre><code># In your app's ks.yaml\nspec:\n  components:\n    - name: volsync\n</code></pre> <p>VolSync uses the <code>${APP}</code> variable (from Flux substitution) to name resources. Each app gets its own <code>ReplicationSource</code> and Kopia secret.</p>"},{"location":"operations/backup-recovery/#checking-backup-status","title":"Checking Backup Status","text":"<pre><code># List all backup sources and their last sync time\nkubectl get replicationsource -A\n\n# List all restore destinations\nkubectl get replicationdestination -A\n\n# Detailed status for a specific app\nkubectl -n &lt;namespace&gt; describe replicationsource &lt;app-name&gt;\n</code></pre>"},{"location":"operations/backup-recovery/#restoring-from-a-volsync-backup","title":"Restoring from a VolSync Backup","text":"<p>Warning</p> <p>Restoring overwrites the existing PVC data. Ensure you understand the implications before proceeding.</p> <ol> <li> <p>Scale down the application to release the PVC:</p> <pre><code>flux suspend hr &lt;app-name&gt; -n &lt;namespace&gt;\nkubectl -n &lt;namespace&gt; scale deploy/&lt;app-name&gt; --replicas=0\n</code></pre> </li> <li> <p>Trigger the restore by annotating the <code>ReplicationDestination</code>:</p> <pre><code>kubectl -n &lt;namespace&gt; patch replicationdestination &lt;app-name&gt; \\\n  --type merge -p '{\"spec\":{\"trigger\":{\"manual\":\"restore-once\"}}}'\n</code></pre> </li> <li> <p>Wait for the restore to complete:</p> <pre><code>kubectl -n &lt;namespace&gt; get replicationdestination &lt;app-name&gt; -w\n</code></pre> </li> <li> <p>Resume the application:</p> <pre><code>flux resume hr &lt;app-name&gt; -n &lt;namespace&gt;\n</code></pre> </li> <li> <p>Verify the application is running with restored data:</p> <pre><code>kubectl -n &lt;namespace&gt; get pods\n</code></pre> </li> </ol>"},{"location":"operations/backup-recovery/#mass-point-in-time-restore","title":"Mass Point-in-Time Restore","text":"<p>For disaster recovery scenarios where all VolSync-backed applications need to be restored simultaneously, use the automated mass restore script:</p> <pre><code>./scripts/volsync-restore-all.sh\n</code></pre> <p>Warning</p> <p>This script restores all 16 VolSync-backed applications at once. Ensure you understand the implications before running it.</p> <p>Supported applications (16 total):</p> Namespace Applications media autobrr, bazarr, plex, prowlarr, qbittorrent, radarr, recyclarr, seerr, sonarr, tautulli, thelounge, qui network unifi-toolkit observability gatus utils forgejo, penpot <p>How it works:</p> <ol> <li>Suspends all Flux Kustomizations for the target apps</li> <li>Scales down workloads (handles both Deployments and StatefulSets)</li> <li>Patches each <code>ReplicationDestination</code> with a <code>restoreAsOf</code> timestamp and manual trigger</li> <li>Waits for all restores to complete (20-minute timeout per app)</li> <li>Resumes Flux Kustomizations on success</li> </ol> <p>Configuration: Edit the <code>RESTORE_TIME</code> variable at the top of the script to set the desired point-in-time (RFC3339 format, e.g., <code>2026-03-01T23:59:59Z</code>).</p> <p>Notes:</p> <ul> <li>Handles multi-controller apps (e.g., penpot with separate frontend/backend deployments)</li> <li>Prevents <code>kubectl</code> hangs when pods are already absent</li> <li>Reports failures at the end with a summary of which apps failed</li> </ul>"},{"location":"operations/backup-recovery/#postgresql-backups","title":"PostgreSQL Backups","text":"<p>CloudNative-PG handles PostgreSQL backups independently via the barman-cloud plugin:</p> <ul> <li>Continuous WAL archiving to Garage S3 (enables point-in-time recovery)</li> <li>Scheduled base backups with configurable retention</li> <li>S3 bucket: <code>cnpg-garage</code></li> <li>Recovery cluster definition at <code>kubernetes/apps/database/cloudnative-pg/recovery/</code></li> </ul>"},{"location":"operations/backup-recovery/#triggering-a-manual-backup","title":"Triggering a Manual Backup","text":"<pre><code>kubectl -n database create -f - &lt;&lt;EOF\napiVersion: postgresql.cnpg.io/v1\nkind: Backup\nmetadata:\n  name: manual-backup-$(date +%Y%m%d%H%M)\nspec:\n  cluster:\n    name: postgres\n  method: barmanObjectStore\nEOF\n</code></pre>"},{"location":"operations/backup-recovery/#checking-backup-status_1","title":"Checking Backup Status","text":"<pre><code># List all backups\nkubectl -n database get backups\n\n# List scheduled backups\nkubectl -n database get scheduledbackups\n\n# Check backup details\nkubectl -n database describe backup &lt;backup-name&gt;\n\n# Check WAL archiving status\nkubectl -n database get cluster postgres -o jsonpath='{.status.firstRecoverabilityPoint}'\n</code></pre>"},{"location":"operations/backup-recovery/#mariadb-backups","title":"MariaDB Backups","text":"<p>The mariadb-operator handles MariaDB Galera backups via its <code>Backup</code> custom resource:</p> <ul> <li>Scheduled mysqldump backups to Garage S3 every 6 hours</li> <li>Compression: bzip2</li> <li>Retention: 30 days</li> <li>S3 bucket: <code>mariadb-backups</code> (prefix <code>galera</code>)</li> <li>Backup definition at <code>kubernetes/apps/database/mariadb-operator/cluster/backup.yaml</code></li> </ul>"},{"location":"operations/backup-recovery/#checking-backup-status_2","title":"Checking Backup Status","text":"<pre><code># List all MariaDB backups\nkubectl -n database get backups.k8s.mariadb.com\n\n# Check backup details\nkubectl -n database describe backup mariadb-backup\n</code></pre>"},{"location":"operations/backup-recovery/#restoring-from-a-mariadb-backup","title":"Restoring from a MariaDB Backup","text":"<p>To restore from S3, create a new MariaDB CR with <code>bootstrapFrom</code> referencing the backup:</p> <pre><code>apiVersion: k8s.mariadb.com/v1alpha1\nkind: MariaDB\nmetadata:\n  name: mariadb-recovery\nspec:\n  bootstrapFrom:\n    backupRef:\n      name: mariadb-backup\n  # ... same spec as production cluster\n</code></pre>"},{"location":"operations/backup-recovery/#disaster-recovery","title":"Disaster Recovery","text":""},{"location":"operations/backup-recovery/#full-cluster-recovery","title":"Full Cluster Recovery","text":"<p>Since the cluster is GitOps-managed, a full recovery follows these steps:</p> <ol> <li> <p>Prepare hardware \u2014 Boot new nodes with Talos Linux (see Machine Preparation)</p> </li> <li> <p>Bootstrap the cluster:</p> <pre><code>task bootstrap:talos\ntask bootstrap:apps\n</code></pre> </li> <li> <p>Flux restores application state \u2014 All manifests are pulled from Git automatically</p> </li> <li> <p>VolSync restores PVC data \u2014 Application data is restored from Garage S3 backups</p> </li> <li> <p>PostgreSQL recovers from WAL archives \u2014 Use the recovery cluster definition (see below)</p> </li> <li> <p>MariaDB recovers from S3 backups \u2014 Create a recovery MariaDB CR with <code>bootstrapFrom</code> (see above)</p> </li> <li> <p>Verify all services:</p> <pre><code>flux get ks -A\nflux get hr -A\nkubectl get pods -A | grep -v Running\n</code></pre> </li> </ol>"},{"location":"operations/backup-recovery/#postgresql-point-in-time-recovery","title":"PostgreSQL Point-in-Time Recovery","text":"<p>The recovery cluster definition at <code>kubernetes/apps/database/cloudnative-pg/recovery/cluster.yaml</code> bootstraps a new PostgreSQL cluster from S3 backups:</p> Setting Value Instances 2 (reduced for recovery, scale up after) PostgreSQL 17.7 (matches production) Source <code>postgres-backup</code> (Garage S3 via barman-cloud) S3 bucket <code>cnpg-garage</code> <p>Recovery steps:</p> <ol> <li> <p>Apply the recovery cluster (modify target time if needed for PITR):</p> <pre><code>kubectl apply -f kubernetes/apps/database/cloudnative-pg/recovery/cluster.yaml\n</code></pre> </li> <li> <p>Monitor recovery progress:</p> <pre><code>kubectl -n database get cluster postgres-recovery -w\nkubectl -n database logs -l cnpg.io/cluster=postgres-recovery -f\n</code></pre> </li> <li> <p>Verify data integrity once the cluster is ready:</p> <pre><code>kubectl -n database exec -it postgres-recovery-1 -- psql -U postgres -c '\\l'\n</code></pre> </li> <li> <p>Promote the recovery cluster to production (update the main cluster definition to point to the recovered data, or rename the recovery cluster).</p> </li> </ol> <p>Tip</p> <p>For point-in-time recovery, add a <code>recoveryTarget</code> to the recovery cluster spec: <pre><code>bootstrap:\n  recovery:\n    source: postgres-backup\n    recoveryTarget:\n      targetTime: \"2026-02-14T12:00:00Z\"\n</code></pre></p>"},{"location":"operations/backup-recovery/#whats-backed-up-vs-not","title":"What's Backed Up vs. Not","text":"Backed up Not backed up (ephemeral) Application PVCs (via VolSync) Active VM state (VMs restart from disk images) PostgreSQL databases (via WAL archiving) In-memory caches (Dragonfly data) MariaDB databases (via scheduled mysqldump) Real-time metrics (Prometheus TSDB rebuilds) Cluster manifests (in Git) Pod logs (Victoria Logs rebuilds from Fluent Bit) Secrets (SOPS in Git + 1Password)"},{"location":"operations/backup-recovery/#garage-s3","title":"Garage S3","text":"<p>Garage provides the S3-compatible storage backend:</p> <ul> <li>Self-hosted within the cluster (<code>kubernetes/apps/volsync-system/garage/</code>)</li> <li>Stores both VolSync and PostgreSQL backups</li> <li>Lightweight and resource-efficient</li> <li>Compatible with standard S3 clients and tools</li> </ul>"},{"location":"operations/day2/","title":"Day-2 Operations","text":""},{"location":"operations/day2/#flux-reconciliation","title":"Flux Reconciliation","text":"<p>Force Flux to pull the latest changes from Git:</p> <pre><code>task reconcile\n</code></pre> <p>Check the status of all Flux resources:</p> <pre><code>flux get ks -A       # Kustomizations\nflux get hr -A       # HelmReleases\nflux get sources -A  # All sources\n</code></pre>"},{"location":"operations/day2/#talos-operations","title":"Talos Operations","text":""},{"location":"operations/day2/#update-node-configuration","title":"Update Node Configuration","text":"<pre><code># Regenerate configs from templates\ntask talos:generate-config\n\n# Apply to a specific node\ntask talos:apply-node IP=10.10.10.10 MODE=auto\n</code></pre>"},{"location":"operations/day2/#upgrade-talos","title":"Upgrade Talos","text":"<ol> <li>Update <code>talosVersion</code> in <code>talenv.yaml</code></li> <li>Run:</li> </ol> <pre><code>task talos:upgrade-node IP=10.10.10.10\n</code></pre>"},{"location":"operations/day2/#upgrade-kubernetes","title":"Upgrade Kubernetes","text":"<ol> <li>Update <code>kubernetesVersion</code> in <code>talenv.yaml</code></li> <li>Run:</li> </ol> <pre><code>task talos:upgrade-k8s\n</code></pre>"},{"location":"operations/day2/#application-management","title":"Application Management","text":""},{"location":"operations/day2/#suspend-an-application","title":"Suspend an Application","text":"<pre><code>flux suspend hr &lt;app-name&gt; -n &lt;namespace&gt;\n</code></pre>"},{"location":"operations/day2/#resume-an-application","title":"Resume an Application","text":"<pre><code>flux resume hr &lt;app-name&gt; -n &lt;namespace&gt;\n</code></pre>"},{"location":"operations/day2/#force-redeploy","title":"Force Redeploy","text":"<pre><code>flux reconcile hr &lt;app-name&gt; -n &lt;namespace&gt; --force\n</code></pre>"},{"location":"operations/day2/#roll-back-a-helmrelease","title":"Roll Back a HelmRelease","text":"<pre><code># Check history\nhelm history &lt;release-name&gt; -n &lt;namespace&gt;\n\n# Rollback\nhelm rollback &lt;release-name&gt; &lt;revision&gt; -n &lt;namespace&gt;\n</code></pre>"},{"location":"operations/day2/#renovate","title":"Renovate","text":"<p>Renovate runs on a weekend schedule and creates PRs for dependency updates:</p> <ul> <li>Auto-merge: GitHub Actions (minor/patch), Mise tools (minor/patch)</li> <li>Manual review: Helm charts, container images (major versions)</li> <li>Dashboard: Check the \"Dependency Dashboard\" issue in GitHub</li> </ul>"},{"location":"operations/day2/#adding-a-new-application","title":"Adding a New Application","text":"<p>Follow the checklist in order:</p> <ol> <li>Create directory: <code>kubernetes/apps/&lt;namespace&gt;/&lt;app-name&gt;/app/</code></li> <li>Create manifests: <code>helmrelease.yaml</code>, <code>ocirepository.yaml</code>, <code>kustomization.yaml</code></li> <li>Create <code>ks.yaml</code> Flux Kustomization</li> <li>Update <code>kubernetes/apps/&lt;namespace&gt;/kustomization.yaml</code></li> <li>Encrypt any secrets with SOPS</li> <li>Consider Kanidm SSO integration</li> <li>Add to Homepage dashboard</li> <li>Add VolSync backup config if stateful</li> <li>Add monitoring (ServiceMonitor/PodMonitor) if metrics are exposed</li> <li>Add Discord alerts if critical</li> <li>Add NFS-scaler if mounting NFS volumes</li> <li>Update CLAUDE.md with the new application</li> </ol>"},{"location":"operations/node-loss-recovery/","title":"Node Loss Recovery","text":"<p>How to recover when a node is permanently lost (hardware failure, rebuild, etc.) and you've been left with orphaned PVCs that block stateful apps.</p>"},{"location":"operations/node-loss-recovery/#why-this-happens","title":"Why this happens","text":"<p>The cluster uses OpenEBS hostpath as the default StorageClass for stateful workloads. Hostpath gives fast local-disk performance with minimal operational overhead, but each PV is hard-affined to the node that provisioned it:</p> <pre><code>nodeAffinity:\n  required:\n    nodeSelectorTerms:\n      - matchExpressions:\n          - key: kubernetes.io/hostname\n            operator: In\n            values: [\"node-01-pve\"]\n</code></pre> <p>When a node is permanently lost (rebuild, hardware death, replacement), every PV pinned to it becomes inaccessible. Pods that mount those PVCs stay in <code>Init</code> or <code>Pending</code> forever with messages like:</p> <pre><code>MountVolume.NewMounter initialization failed for volume \"pvc-xxx\" :\n  path \"/var/mnt/local-hostpath/pvc-xxx\" does not exist\n</code></pre> <p>This is by design \u2014 local storage trades durability for speed. The recovery model is:</p> Workload type Recovery source HA databases (CNPG, MariaDB Galera) Replicate from surviving instances Single-instance apps with VolSync (forgejo, teslamate, etc.) Restore from VolSync (Kopia repo on NFS) Apps with their own S3 backup cronjob (Kanidm) Restore from S3 (Garage) via app-specific restore command Garage itself (the S3 service backing everything above) Restore meta PVC from the NFS sidecar mirror (see Garage metadata recovery) CNPG-backed stateless apps (kguardian, teslamate, penpot) Heal automatically once Postgres is up Stale/removed apps Just delete the orphan resources <p>Note on app-specific backups: Some apps run their own scheduled backup cronjobs that ship to Garage S3 (e.g., <code>kanidm-backup-sync</code> does <code>aws s3 sync /data/backups/ s3://kanidm/backups/</code> hourly). These are easy to miss because they aren't VolSync \u2014 check <code>kubectl get cronjob -A | grep -i backup</code> when triaging.</p> <p>We don't use distributed storage (Longhorn / Rook-Ceph) because nodes have limited disk and we'd rather keep the hot path local and ship durability off-node via WAL streaming (CNPG \u2192 Garage S3) or scheduled snapshots (VolSync \u2192 Kopia \u2192 NFS).</p>"},{"location":"operations/node-loss-recovery/#triage-checklist","title":"Triage checklist","text":"<p>When you spot a node-loss situation, work through this in order:</p> <pre><code># Which workloads are wedged on missing volumes?\nkubectl get pods -A --field-selector=status.phase!=Running,status.phase!=Succeeded\n\n# Which Flux resources are unhealthy?\nflux get ks -A --status-selector ready=false\nflux get hr -A --status-selector ready=false\n\n# Which PVs are pinned to the dead node?\nkubectl get pv -o json | jq -r '\n  .items[] |\n  select(.spec.nodeAffinity.required.nodeSelectorTerms[]?.matchExpressions[]?.values[]? == \"&lt;dead-node&gt;\") |\n  \"\\(.spec.claimRef.namespace)/\\(.spec.claimRef.name) \\(.metadata.name)\"'\n</code></pre> <p>Categorize each affected workload:</p> <ol> <li>CNPG / MariaDB instance \u2192 see HA database</li> <li>VolSync-backed app \u2192 see Single-instance with VolSync</li> <li>Stateless app stuck on a downstream DB init \u2192 leave alone, will self-heal once the DB is back</li> <li>Removed/stale resource \u2192 just delete</li> </ol>"},{"location":"operations/node-loss-recovery/#ha-database-recovery","title":"HA database recovery","text":"<p>The data layer is already replicated at the app level. Recovery is \"delete the orphaned PVC and let the operator re-init a fresh replica on a live node.\"</p>"},{"location":"operations/node-loss-recovery/#mariadb-galera-the-easy-case","title":"MariaDB Galera (the easy case)","text":"<p>If only one instance was on the dead node and the surviving members still have quorum (or can reach quorum), the operator handles everything once the orphan is removed.</p> <pre><code># Delete the orphaned data + galera state PVCs for the dead instance\nkubectl delete pvc -n database storage-mariadb-&lt;N&gt; galera-mariadb-&lt;N&gt;\n\n# Force-delete the stuck pod\nkubectl delete pod -n database mariadb-&lt;N&gt; --force --grace-period=0\n\n# Watch the operator re-provision and the cluster come back\nkubectl get pod -n database -l app.kubernetes.io/instance=mariadb -w\nkubectl get mariadb -n database\n</code></pre> <p>The operator (with <code>spec.galera.recovery.enabled: true</code>) detects the missing instance, provisions fresh PVCs on a live node, and Galera SST-syncs the new replica from a healthy donor.</p> <p>If the cluster has lost quorum (all surviving instances crashlooping with <code>failed to reach primary view</code>), you need force-bootstrap:</p> <pre><code># Pick the survivor with the highest grastate seqno (or just the original primary)\nkubectl patch mariadb -n database mariadb --type=merge -p '\nspec:\n  galera:\n    recovery:\n      forceClusterBootstrapInPod: mariadb-0\n'\n</code></pre> <p>This is a temporary patch the operator clears after bootstrap. If Flux is reconciling the CR, suspend the ks first or it'll get reverted before the bootstrap completes.</p> <p>If force-bootstrap fails (corrupted grastate, etc.), fall back to S3 restore using the scheduled <code>backup.k8s.mariadb.com</code> resource pointing at Garage.</p>"},{"location":"operations/node-loss-recovery/#cloudnative-pg-the-trickier-case","title":"CloudNative-PG (the trickier case)","text":"<p>CNPG won't auto-clean dangling PVCs (by design \u2014 protects against accidental data loss). The danger is that the lost instance was the primary, in which case you need a switchover before deleting the PVC, otherwise the operator gets stuck trying to keep a dead instance as primary.</p> <p>Step 1 \u2014 check who's primary:</p> <pre><code>kubectl get cluster -n database postgres -o jsonpath='\ncurrentPrimary={.status.currentPrimary}\ntargetPrimary={.status.targetPrimary}\nphase={.status.phase}\ndanglingPVC={.status.danglingPVC}\nreadyInstances={.status.readyInstances}/{.status.instances}\n'\n</code></pre> <p>If <code>currentPrimary</code> is the dead-node instance, you need to force a switchover before the PVC delete.</p> <p>Step 2 \u2014 switchover (if needed):</p> <p>If you have the <code>cnpg</code> kubectl plugin: <pre><code>kubectl cnpg promote postgres-3 -n database\n</code></pre></p> <p>Without the plugin, patch the cluster status directly: <pre><code>kubectl patch cluster postgres -n database --subresource=status --type=merge -p '\n{\"status\":{\"targetPrimary\":\"postgres-3\",\"currentPrimary\":\"postgres-3\"}}\n'\n</code></pre></p> <p>Step 3 \u2014 delete the orphaned PVC and stuck pod:</p> <pre><code>kubectl delete pod -n database postgres-&lt;dead-N&gt; --force --grace-period=0\nkubectl delete pvc -n database postgres-&lt;dead-N&gt;\n</code></pre> <p>The PV has <code>Delete</code> reclaim policy, so it goes too. The operator now sees <code>instances: 3</code> but only 2 PVCs, and creates a fresh <code>postgres-&lt;next&gt;</code> via pg_basebackup from the new primary.</p> <p>Step 4 \u2014 kick the new primary if it's stuck in \"Waiting for the new primary to be available\":</p> <p>If the new primary was previously a replica, its on-disk state may say \"I'm a replica of the old primary.\" The instance manager waits for someone to promote it. Restarting the pod re-reads the cluster's <code>currentPrimary</code> and triggers the local promotion:</p> <pre><code>kubectl delete pod -n database postgres-&lt;new-primary&gt;\n</code></pre> <p>Verify with: <pre><code>kubectl exec -n database postgres-&lt;new-primary&gt; -c postgres -- \\\n  psql -U postgres -c 'SELECT pg_is_in_recovery();'   # should return f\nkubectl get endpoints -n database postgres-rw          # should have an IP\n</code></pre></p> <p>Step 5 \u2014 dangling PVCs:</p> <p>If other instance PVCs were also <code>dangling</code> (e.g., from a previous failed reconcile), the operator re-adopts them on its next loop. You can confirm by re-reading <code>.status.danglingPVC</code> \u2014 it should drain to empty as instance pods come up.</p>"},{"location":"operations/node-loss-recovery/#single-instance-app-recovery-volsync","title":"Single-instance app recovery (VolSync)","text":"<p>For apps with no app-layer replication, VolSync's Kopia repo on NFS is the recovery point. Use the existing <code>scripts/volsync-restore-all.sh</code> for mass restores, or this single-app flow for one app at a time.</p>"},{"location":"operations/node-loss-recovery/#flow","title":"Flow","text":"<pre><code>APP=forgejo           # PVC name = RD name prefix\nNS=utils              # target namespace\nKS_NS=utils           # Flux Kustomization namespace (often same as target)\n\n# 1. Suspend Flux so it doesn't fight us\nflux suspend ks -n \"${KS_NS}\" \"${APP}\"\n\n# 2. Scale down so nothing holds the PVC\nkubectl -n \"${NS}\" scale deployment \"${APP}\" --replicas=0\nkubectl -n \"${NS}\" wait pod -l app.kubernetes.io/name=\"${APP}\" --for=delete --timeout=60s\n\n# 3. Suspend the ReplicationSource (so it doesn't try to back up the empty PVC)\nkubectl -n \"${NS}\" patch replicationsource \"${APP}\" --type=merge -p '{\"spec\":{\"paused\":true}}'\n\n# 4. Delete the orphaned PVC + VolSync source cache\nkubectl -n \"${NS}\" delete pvc \"${APP}\" \"volsync-src-${APP}-cache\" --wait=false\n\n# 5. If a PVC is stuck Terminating on a missing PV finalizer, force-clear it:\nkubectl -n \"${NS}\" patch pvc \"${APP}\" --type=merge -p '{\"metadata\":{\"finalizers\":null}}'\n\n# 6. Recreate the destination PVC with the same spec but no node affinity.\n#    Pull size/storageClass from the original Helm values, or use what's typical.\nkubectl apply -f - &lt;&lt;EOF\napiVersion: v1\nkind: PersistentVolumeClaim\nmetadata:\n  name: ${APP}\n  namespace: ${NS}\nspec:\n  accessModes: [ReadWriteOnce]\n  resources: { requests: { storage: 10Gi } }\n  storageClassName: openebs-hostpath\nEOF\n\n# 7. Trigger restore from latest snapshot\nTRIGGER=\"restore-$(date -u +%Y%m%d-%H%M%S)\"\nkubectl -n \"${NS}\" patch replicationdestination \"${APP}-dst\" --type=json -p \"[\n  {\\\"op\\\":\\\"remove\\\",\\\"path\\\":\\\"/spec/kopia/restoreAsOf\\\"},\n  {\\\"op\\\":\\\"replace\\\",\\\"path\\\":\\\"/spec/trigger/manual\\\",\\\"value\\\":\\\"${TRIGGER}\\\"}\n]\"\n\n# 8. Wait for the restore to complete\nuntil [[ \"$(kubectl -n \"${NS}\" get replicationdestination \"${APP}-dst\" \\\n    -o jsonpath='{.status.lastManualSync}')\" == \"${TRIGGER}\" ]]; do sleep 10; done\nkubectl -n \"${NS}\" get replicationdestination \"${APP}-dst\" \\\n  -o jsonpath='result={.status.latestMoverStatus.result}{\"\\n\"}'\n\n# 9. Resume the source backup and Flux\nkubectl -n \"${NS}\" patch replicationsource \"${APP}\" --type=merge -p '{\"spec\":{\"paused\":false}}'\nflux resume ks -n \"${KS_NS}\" \"${APP}\"\n\n# 10. Helm/Flux often won't reset replicas back from 0 on a no-op reconcile.\n#     Scale the workload up manually:\nkubectl -n \"${NS}\" scale deployment \"${APP}\" --replicas=1\n</code></pre>"},{"location":"operations/node-loss-recovery/#why-this-differs-from-the-mass-restore-script","title":"Why this differs from the mass-restore script","text":"<p><code>volsync-restore-all.sh</code> uses VolSync's <code>copyMethod: Direct</code> to restore into the existing PVC. That works when the PVC is on a live node and you just want to roll back data \u2014 but in our case the PVC is bound to a PV on a dead node, so we have to delete and recreate it before the mover can mount it on a live node.</p> <p>The destination Kopia mover provisions the cache PVC on first use. Watch for <code>Pending</code> PVCs \u2014 if the OpenEBS provisioner is itself wedged (e.g., its pod was on the dead node and is still pulling its image after rescheduling), the whole chain stalls. <code>kubectl get pod -n openebs-system</code> is the first thing to check if mover pods stay <code>Pending</code> with no events.</p>"},{"location":"operations/node-loss-recovery/#app-specific-s3-restore-kanidm-pattern","title":"App-specific S3 restore (Kanidm pattern)","text":"<p>Some apps (notably Kanidm) ship JSON-format backups to a Garage S3 bucket via a CronJob, separate from VolSync. The data PVC has no off-node copy on its own \u2014 the backup files in S3 are the only recovery point.</p> <p>Pattern, using Kanidm as the example:</p> <pre><code>NS=identity\nAPP=kanidm\nPVC=kanidm-data-kanidm-default-0   # StatefulSet PVC name\nS3_BUCKET=kanidm                   # Garage bucket\nTLS_SECRET=kanidm-tls              # if kanidmd needs TLS for restore\n\n# 1. Suspend Flux so kaniop doesn't fight us\nflux suspend ks -n \"${NS}\" \"${APP}\"\n\n# 2. Scale the StatefulSet to 0 (orphan pod was Pending on dead node anyway)\nkubectl -n \"${NS}\" scale statefulset kanidm-default --replicas=0\nkubectl -n \"${NS}\" wait pod kanidm-default-0 --for=delete --timeout=60s || true\n\n# 3. Delete the orphaned PVC; if Terminating-stuck, clear finalizer\nkubectl -n \"${NS}\" delete pvc \"${PVC}\" --wait=false\nkubectl -n \"${NS}\" patch pvc \"${PVC}\" --type=merge -p '{\"metadata\":{\"finalizers\":null}}'\n</code></pre> <p>For Kanidm, the operator (kaniop) re-creates the PVC immediately from the volumeClaimTemplate even with the StatefulSet at 0 replicas \u2014 there's no need to recreate it manually. Verify with <code>kubectl get pvc -n identity</code>.</p> <pre><code># 4. Run a Job that downloads + decompresses + restores into the new empty PVC\n#    Notes:\n#      - The kanidm/server image has no shell (sh/gunzip) \u2014 use a busybox/alpine\n#        sidecar to decompress before invoking kanidmd directly.\n#      - The kanidm-s3-secret credentials work against Garage with\n#        `--region us-east-1 --endpoint-url http://garage.volsync-system.svc.cluster.local:3900`.\n#        Use `s3api get-object` rather than `s3 cp` \u2014 Garage 400s on the HEAD\n#        request `s3 cp` issues first.\n#      - `kanidmd database restore` requires KANIDM_TLS_CHAIN + KANIDM_TLS_KEY env\n#        even though it never starts the network listener.\nkubectl apply -f - &lt;&lt;'EOF'\napiVersion: batch/v1\nkind: Job\nmetadata: { name: kanidm-restore, namespace: identity }\nspec:\n  ttlSecondsAfterFinished: 600\n  template:\n    spec:\n      restartPolicy: Never\n      securityContext: { fsGroup: 999, runAsUser: 999, runAsGroup: 999 }\n      initContainers:\n      - name: download\n        image: amazon/aws-cli:latest\n        command: [\"sh\",\"-c\"]\n        args:\n        - |\n          set -eux\n          LATEST=$(aws --region us-east-1 --endpoint-url \"$S3_ENDPOINT\" s3 ls s3://kanidm/backups/ | awk '{print $NF}' | sort | tail -1)\n          aws --region us-east-1 --endpoint-url \"$S3_ENDPOINT\" s3api get-object \\\n            --bucket kanidm --key \"backups/${LATEST}\" /tmp/backup.json.gz\n        env:\n        - { name: S3_ENDPOINT,        value: http://garage.volsync-system.svc.cluster.local:3900 }\n        - { name: AWS_DEFAULT_REGION, value: us-east-1 }\n        - { name: AWS_ACCESS_KEY_ID,     valueFrom: { secretKeyRef: { name: kanidm-s3-secret, key: AWS_ACCESS_KEY_ID } } }\n        - { name: AWS_SECRET_ACCESS_KEY, valueFrom: { secretKeyRef: { name: kanidm-s3-secret, key: AWS_SECRET_ACCESS_KEY } } }\n        volumeMounts: [ { name: tmp, mountPath: /tmp } ]\n      - name: decompress\n        image: alpine:3\n        command: [\"sh\",\"-c\"]\n        args: [\"set -eux; gunzip /tmp/backup.json.gz\"]\n        volumeMounts: [ { name: tmp, mountPath: /tmp } ]\n      containers:\n      - name: restore\n        image: docker.io/kanidm/server:1.10.0\n        command: [\"kanidmd\"]\n        args:    [\"database\", \"restore\", \"/tmp/backup.json\"]\n        env:\n        - { name: KANIDM_DOMAIN,    value: auth.00o.sh }\n        - { name: KANIDM_ORIGIN,    value: https://auth.00o.sh }\n        - { name: KANIDM_DB_PATH,   value: /data/kanidm.db }\n        - { name: KANIDM_TLS_CHAIN, value: /etc/kanidm/tls/tls.crt }\n        - { name: KANIDM_TLS_KEY,   value: /etc/kanidm/tls/tls.key }\n        volumeMounts:\n        - { name: kanidm-data,  mountPath: /data }\n        - { name: tmp,          mountPath: /tmp }\n        - { name: kanidm-certs, mountPath: /etc/kanidm/tls, readOnly: true }\n      volumes:\n      - { name: kanidm-data,  persistentVolumeClaim: { claimName: kanidm-data-kanidm-default-0 } }\n      - { name: tmp,          emptyDir: {} }\n      - { name: kanidm-certs, secret: { secretName: kanidm-tls } }\nEOF\n\n# 5. Wait for restore Job and inspect logs\nkubectl -n identity wait --for=condition=Complete job/kanidm-restore --timeout=10m\nkubectl -n identity logs job/kanidm-restore -c restore | tail -5\n# Expected: \"\u2705 Restore Success!\"\n\n# 6. Scale StatefulSet back up and resume Flux\nkubectl -n identity scale statefulset kanidm-default --replicas=1\nflux resume ks -n identity kanidm\n</code></pre> <p>After Kanidm is healthy, bounce any OIDC clients (forgejo, dbgate, kubevirt-manager, opencost, penpot) \u2014 they often cache the previous discovery doc and need a restart to re-register against the restored identity store.</p>"},{"location":"operations/node-loss-recovery/#credential-mismatch-after-a-kanidm-restore-gotcha","title":"Credential mismatch after a Kanidm restore (gotcha)","text":"<p>After the restore Job completes and Kanidm starts up, you'll likely see kaniop logs spamming <code>client failed to authenticate: AuthenticationFailed</code> for every reconcile (KanidmGroup, KanidmPersonAccount, KanidmOAuth2Client). This blocks OAuth2 client registration, which in turn blocks OIDC consumers like forgejo (its <code>configure-gitea</code> init container hits a 302 on the well-known discovery URL because the client isn't registered).</p> <p>The cause: kaniop authenticates as <code>admin</code> / <code>idm_admin</code> using passwords stored in the <code>kanidm-admin-passwords</code> Secret. The restore may bring in a DB whose internal credential state for those accounts doesn't match what's in the Secret (this is a known kaniop+restore interaction). The fix is to reset the passwords on the restored DB to match what kaniop expects \u2014 or, easier, reset them to fresh values and update the Secret.</p> <pre><code># 1. Stop kanidmd so we can use the offline recover-account command\nflux suspend ks -n identity kanidm\nkubectl -n identity scale statefulset kanidm-default --replicas=0\nkubectl -n identity wait pod kanidm-default-0 --for=delete --timeout=60s\n\n# 2. Run recover-account for admin (generates a new password, prints to stdout)\n#    Note: kanidm/server image has no shell \u2014 invoke kanidmd directly via command/args.\nkubectl apply -f - &lt;&lt;'EOF'\napiVersion: batch/v1\nkind: Job\nmetadata: { name: kanidm-recover-admin, namespace: identity }\nspec:\n  ttlSecondsAfterFinished: 600\n  template:\n    spec:\n      restartPolicy: Never\n      securityContext: { fsGroup: 999, runAsUser: 999, runAsGroup: 999 }\n      containers:\n      - name: recover\n        image: docker.io/kanidm/server:1.10.0\n        command: [\"kanidmd\"]\n        args:    [\"recover-account\", \"admin\"]\n        env:\n        - { name: KANIDM_DOMAIN,    value: auth.00o.sh }\n        - { name: KANIDM_ORIGIN,    value: https://auth.00o.sh }\n        - { name: KANIDM_DB_PATH,   value: /data/kanidm.db }\n        - { name: KANIDM_TLS_CHAIN, value: /etc/kanidm/tls/tls.crt }\n        - { name: KANIDM_TLS_KEY,   value: /etc/kanidm/tls/tls.key }\n        volumeMounts:\n        - { name: kanidm-data,  mountPath: /data }\n        - { name: kanidm-certs, mountPath: /etc/kanidm/tls, readOnly: true }\n      volumes:\n      - { name: kanidm-data,  persistentVolumeClaim: { claimName: kanidm-data-kanidm-default-0 } }\n      - { name: kanidm-certs, secret: { secretName: kanidm-tls } }\nEOF\nkubectl -n identity wait --for=condition=Complete job/kanidm-recover-admin --timeout=2m\nADMIN_PW=$(kubectl -n identity logs job/kanidm-recover-admin | grep -oP 'new_password: \"\\K[^\"]+')\necho \"admin: $ADMIN_PW\"\n\n# 3. Same for idm_admin (don't run in parallel \u2014 Kanidm DB is RWO)\n#    Apply identical Job with name=kanidm-recover-idm-admin, args=[\"recover-account\",\"idm_admin\"].\nIDM_PW=$(kubectl -n identity logs job/kanidm-recover-idm-admin | grep -oP 'new_password: \"\\K[^\"]+')\n\n# 4. Patch the Secret with the new passwords\nkubectl patch secret -n identity kanidm-admin-passwords --type=json -p \"[\n  {\\\"op\\\":\\\"replace\\\",\\\"path\\\":\\\"/data/ADMIN_PASSWORD\\\",\\\"value\\\":\\\"$(printf '%s' \"$ADMIN_PW\" | base64 -w0)\\\"},\n  {\\\"op\\\":\\\"replace\\\",\\\"path\\\":\\\"/data/IDM_ADMIN_PASSWORD\\\",\\\"value\\\":\\\"$(printf '%s' \"$IDM_PW\" | base64 -w0)\\\"}\n]\"\n\n# 5. Scale Kanidm back up and resume Flux\nkubectl -n identity scale statefulset kanidm-default --replicas=1\nflux resume ks -n identity kanidm\n</code></pre> <p>Within ~30s of the Secret patch, kaniop should pick up the change (it watches the Secret), authenticate successfully, and start successfully reconciling all <code>KanidmOAuth2Client</code> and <code>KanidmPersonAccount</code> resources. Watch with:</p> <pre><code>kubectl -n identity logs deploy/kaniop -f | grep -iE 'auth|reconcil|fail'\n</code></pre> <p>Once you see <code>reconciling oauth2 client</code> INFO lines (not ERROR lines), bounce the OIDC consumers.</p>"},{"location":"operations/node-loss-recovery/#stale-removed-resources","title":"Stale / removed resources","text":"<p>Apps that have been removed from Flux (commented out in a parent kustomization) sometimes leave behind cluster resources that aren't pruned. ReplicationDestinations are a common one \u2014 they keep spawning mover pods that fail to schedule because the source PVC no longer exists.</p> <p>Just delete them:</p> <pre><code>kubectl delete replicationdestination -n &lt;namespace&gt; &lt;app&gt;-dst\n</code></pre> <p>The bound <code>volsync-dst-&lt;app&gt;-dst-cache</code> PVC will GC when its owner is gone.</p>"},{"location":"operations/node-loss-recovery/#garage-metadata-recovery","title":"Garage metadata recovery","text":"<p>Garage runs single-replica (no HA) and its meta dir cannot live on NFS \u2014 LMDB throws <code>Resource temporarily unavailable</code> on every background worker over NFS because mmap+file-lock semantics don't work there. So meta lives on a local <code>openebs-hostpath</code> PVC (<code>garage-meta</code>), which means if the node hosting it dies, the PVC dies with it. Data lives on NFS and is safe, but data shards are opaque without meta \u2014 losing meta = losing all S3 objects (CNPG WAL/base backups, MariaDB S3 backups, Kanidm hourly backups).</p> <p>The protection: a <code>backup-sync</code> sidecar inside the Garage pod (same controller) mirrors <code>/meta/</code> (minus the live <code>db.lmdb/</code> dir) to NFS at <code>/mnt/Speed/Kubernetes/apps/garage/meta-backup/</code> every 24h. It copies:</p> <ul> <li>All config files (<code>cluster_layout</code>, <code>data_layout</code>, <code>node_key</code>, <code>node_key.pub</code>, <code>peer_list</code>, <code>lifecycle_worker_state</code>, <code>scrub_info</code>)</li> <li>The <code>snapshots/</code> directory \u2014 Garage's own internal LMDB snapshots created via <code>mdb_env_copy()</code> (consistent, no locking issues)</li> </ul> <p>It does not copy the live <code>db.lmdb/</code> dir because Garage holds an exclusive flock on <code>lock.mdb</code>. The snapshot files inside <code>snapshots/</code> are the consistent recovery point.</p> <p>Why not VolSync for this PVC? VolSync's <code>copyMethod: Direct</code> mounts the source PVC into a mover pod concurrently with Garage. On RWO <code>openebs-hostpath</code>, this races and corrupts the metadata (we lost <code>cluster_layout</code> + <code>snapshots/</code> doing this once). <code>copyMethod: Snapshot</code> isn't an option either \u2014 <code>openebs-hostpath</code> doesn't support CSI VolumeSnapshots. So the in-pod sidecar approach sidesteps the whole RWO problem by never letting another pod mount the PVC.</p> <p>Alerting: the sidecar <code>set -e</code>'s and <code>exit</code>s on any failure \u2192 Kubernetes restarts it \u2192 <code>kube_pod_container_status_restarts_total</code> increments \u2192 the <code>GarageMetaBackupSidecarRestarted</code> PrometheusRule fires (warning, 5min for) \u2192 AlertManager \u2192 Discord. There's also a <code>GarageMetaBackupSidecarAbsent</code> (critical) for when the container isn't running at all.</p>"},{"location":"operations/node-loss-recovery/#recovery-procedure","title":"Recovery procedure","text":"<p>If <code>garage-meta</code> PVC is unusable (lost node, corrupted LMDB, etc.):</p> <pre><code># 1. Stop Garage so nothing writes during restore\nkubectl -n volsync-system scale deploy garage --replicas=0\nkubectl -n volsync-system wait pod -l app.kubernetes.io/name=garage --for=delete --timeout=60s\n\n# 2. Run the restore Job (it picks the latest snapshot automatically)\n#    The same logic is in scripts/volsync-restore-all.sh phase_restore_garage_meta \u2014\n#    that script's main() now calls it as Phase 6 after all VolSync restores.\nkubectl apply -f - &lt;&lt;'EOF'\napiVersion: batch/v1\nkind: Job\nmetadata: { name: garage-meta-restore, namespace: volsync-system }\nspec:\n  ttlSecondsAfterFinished: 600\n  backoffLimit: 0\n  template:\n    spec:\n      restartPolicy: Never\n      securityContext: { fsGroup: 10000, runAsUser: 10000, runAsGroup: 10000 }\n      containers:\n      - name: restore\n        image: alpine:3\n        command: [\"sh\",\"-c\"]\n        args:\n        - |\n          set -eux\n          LATEST=$(ls -1 /backup/snapshots | sort | tail -1)\n          [ -z \"$LATEST\" ] &amp;&amp; { echo \"FATAL: no snapshots found\"; exit 1; }\n          echo \"Restoring from snapshot: $LATEST\"\n          find /dst -mindepth 1 -delete\n          for f in cluster_layout data_layout node_key node_key.pub peer_list lifecycle_worker_state scrub_info; do\n            [ -e \"/backup/$f\" ] &amp;&amp; cp -v \"/backup/$f\" \"/dst/$f\"\n          done\n          chmod 600 /dst/node_key\n          mkdir -p /dst/snapshots /dst/db.lmdb\n          cp -rv /backup/snapshots/. /dst/snapshots/\n          cp -v \"/backup/snapshots/${LATEST}/db.lmdb\" /dst/db.lmdb/data.mdb\n        volumeMounts:\n        - { name: backup, mountPath: /backup }\n        - { name: dst,    mountPath: /dst }\n      volumes:\n      - name: backup\n        nfs: { server: nas.3226texas.com, path: /mnt/Speed/Kubernetes/apps/garage/meta-backup }\n      - name: dst\n        persistentVolumeClaim: { claimName: garage-meta }\nEOF\nkubectl -n volsync-system wait --for=condition=Complete job/garage-meta-restore --timeout=10m\n\n# 3. Bring Garage back\nkubectl -n volsync-system scale deploy garage --replicas=1\n\n# 4. Verify it sees the original buckets/keys\nGP=$(kubectl get pod -n volsync-system -l app.kubernetes.io/name=garage -o jsonpath='{.items[0].metadata.name}')\nkubectl exec -n volsync-system \"$GP\" -c app -- /garage bucket list   # should show volsync-data, cnpg, kanidm, mariadb-backups, plane-uploads\nkubectl exec -n volsync-system \"$GP\" -c app -- /garage key list      # should show the GK... keys\n</code></pre> <p>If the restore Job picks a snapshot you don't want (e.g., latest is also corrupt), override by setting <code>LATEST=&lt;timestamp&gt;</code> explicitly in the script. List options first with <code>ls /mnt/Speed/Kubernetes/apps/garage/meta-backup/snapshots/</code> from any pod that mounts NFS.</p> <p>Note: this is the same restore pattern as a fresh-PVC seed. During the original incident we used this same flow to seed the brand-new <code>garage-meta</code> PVC from the previously-NFS-backed <code>meta/</code> directory.</p>"},{"location":"operations/node-loss-recovery/#recovering-broken-cnpg-wal-archiving","title":"Recovering broken CNPG WAL archiving","text":"<p>WAL archiving can silently break for weeks if you're not monitoring <code>Cluster.status.conditions[?(@.type==\"ContinuousArchiving\")]</code>. The most common breakage after a recovery: forced primary promotions create new timelines, and barman-cloud's pre-archive check refuses to resume against an existing archive whose timeline state doesn't match the new primary's.</p> <p>The symptom in the <code>plugin-barman-cloud</code> sidecar logs:</p> <pre><code>barman-cloud-check-wal-archive checking the first wal\nERROR: WAL archive check failed for server postgres: Expected empty archive\n</code></pre> <p>Even though the bucket may look mostly empty (because retention pruning ate the old WAL segments), the residual <code>&lt;timeline&gt;.history</code> files and base backup directories are enough to fail the check.</p> <p>Fix: switch to a fresh barman serverName so a clean archive starts in a new prefix, preserving the orphaned data for posterity.</p> <pre><code># kubernetes/apps/database/&lt;cluster&gt;/cluster/cluster.yaml\nspec:\n  plugins:\n    - name: barman-cloud.cloudnative-pg.io\n      isWALArchiver: true\n      parameters:\n        barmanObjectName: cnpg-garage\n        serverName: postgres-r2     # bump this each time the archive needs reset\n</code></pre> <p>After applying the spec change:</p> <ol> <li>The Cluster's <code>plugin-barman-cloud</code> sidecar caches barman config at startup, so you must restart the primary pod for the new <code>serverName</code> to take effect. (Sequential restart of replicas first then primary is cleaner; or just delete the primary pod and let CNPG fail over to a replica.)</li> <li>Watch the plugin logs on the new primary \u2014 you should see options ending with <code>s3://cnpg/postgres-r2</code> instead of <code>s3://cnpg/postgres</code>:    <pre><code>kubectl logs -n database &lt;new-primary&gt; -c plugin-barman-cloud -f | grep -E 'serverName|empty|archive'\n</code></pre></li> <li>Verify the cluster conditions flip:    <pre><code>kubectl get cluster -n database postgres -o jsonpath='{.status.conditions}' \\\n  | python3 -c 'import sys,json;[print(c[\"type\"],c[\"status\"]) for c in json.loads(sys.stdin.read())]'\n</code></pre>    You want <code>ContinuousArchiving: True</code> and <code>LastBackupSucceeded: True</code>.</li> <li>CNPG will automatically take a fresh base backup once archiving works (or you can trigger one with <code>kubectl create backup ...</code>). Without a fresh base backup, the WAL stream you're now archiving has nothing to apply against \u2014 no recovery point.</li> <li>The orphaned <code>s3://cnpg/&lt;old-name&gt;/</code> prefix stays in Garage. If you want to GC it, do so manually after confirming no other systems reference it.</li> </ol> <p>Always alert on <code>ContinuousArchiving</code> and <code>LastBackupSucceeded</code>. Without those, your \"last backup\" timestamp can lie for months.</p>"},{"location":"operations/node-loss-recovery/#architectural-caveats","title":"Architectural caveats","text":"<p>A few things this design depends on, which you should verify periodically:</p> <ul> <li>CNPG WAL archiving must actually work. The cluster spec uses <code>barman-cloud.cloudnative-pg.io</code> to ship WAL to Garage. If <code>status.conditions[?(@.type==\"ContinuousArchiving\")].status == \"False\"</code> for any extended period, the \"last backup\" timestamp lies \u2014 you don't have a real recovery point. Alert on this. As of writing, archiving has been broken since 2026-01-29 and the last successful base backup was 2026-03-21, which made the node-01 incident a much bigger event than expected.</li> <li>VolSync <code>ReplicationSource</code> must actually be running on schedule. If a source pod can't run (e.g., its working cache PVC is on a dead node), backups silently stop. Check <code>kubectl get replicationsource -A</code> periodically \u2014 a stale <code>LAST SYNC</code> means no fresh recovery point.</li> <li>OpenEBS provisioner placement. The <code>openebs-localpv-provisioner</code> Deployment can land on any node. If it's on the node that dies, every new PVC provision request stalls until the provisioner reschedules and re-pulls. Not catastrophic, just adds latency to recovery.</li> <li><code>kubectl cnpg</code> plugin should be installed. The status-patch workaround for promotion works but it's brittle \u2014 the plugin's <code>promote</code> command is the supported path. Add it to <code>.mise.toml</code> if you don't already have it.</li> </ul>"},{"location":"operations/node-loss-recovery/#see-also","title":"See also","text":"<ul> <li>Backup &amp; Recovery \u2014 VolSync architecture, Kopia repo layout, S3 backups</li> <li>Day-2 Operations \u2014 Routine operations</li> <li><code>scripts/volsync-restore-all.sh</code> \u2014 Bulk restore script in the repository root</li> </ul>"},{"location":"operations/troubleshooting/","title":"Troubleshooting","text":""},{"location":"operations/troubleshooting/#flux-issues","title":"Flux Issues","text":""},{"location":"operations/troubleshooting/#resources-not-syncing","title":"Resources Not Syncing","text":"<pre><code># Check Flux health\nflux check\n\n# Check for failed reconciliations\nflux get ks -A --status-selector ready=false\nflux get hr -A --status-selector ready=false\n\n# View Flux logs\nflux logs --all-namespaces\n\n# Force sync\ntask reconcile\n</code></pre>"},{"location":"operations/troubleshooting/#helmrelease-stuck","title":"HelmRelease Stuck","text":"<pre><code># Suspend and resume\nflux suspend hr &lt;name&gt; -n &lt;namespace&gt;\nflux resume hr &lt;name&gt; -n &lt;namespace&gt;\n\n# Force reconciliation\nflux reconcile hr &lt;name&gt; -n &lt;namespace&gt; --force\n</code></pre>"},{"location":"operations/troubleshooting/#template-issues","title":"Template Issues","text":""},{"location":"operations/troubleshooting/#templates-not-rendering","title":"Templates Not Rendering","text":"<pre><code>task template:validate-schemas    # Check cluster.yaml &amp; nodes.yaml\ntask template:render-configs      # Force re-render\n</code></pre>"},{"location":"operations/troubleshooting/#secret-issues","title":"Secret Issues","text":""},{"location":"operations/troubleshooting/#secrets-not-decrypting","title":"Secrets Not Decrypting","text":"<pre><code># Verify age key exists\ntest -f age.key &amp;&amp; echo \"Key exists\" || echo \"Missing key\"\n\n# Verify SOPS can decrypt\nsops --decrypt bootstrap/sops-age.sops.yaml\n\n# Check SOPS_AGE_KEY_FILE is set\necho $SOPS_AGE_KEY_FILE\n</code></pre>"},{"location":"operations/troubleshooting/#verifying-encryption","title":"Verifying Encryption","text":"<pre><code># All .sops.yaml files should contain 'sops:' metadata\ngrep -l \"sops:\" kubernetes/**/*.sops.yaml\n</code></pre>"},{"location":"operations/troubleshooting/#node-issues","title":"Node Issues","text":""},{"location":"operations/troubleshooting/#nodes-not-joining","title":"Nodes Not Joining","text":"<pre><code>talosctl get members --nodes &lt;ip&gt; --insecure\ntalosctl logs --nodes &lt;ip&gt; --insecure\n</code></pre>"},{"location":"operations/troubleshooting/#node-health","title":"Node Health","text":"<pre><code>kubectl get nodes -o wide\nkubectl describe node &lt;node-name&gt;\n</code></pre>"},{"location":"operations/troubleshooting/#pod-issues","title":"Pod Issues","text":""},{"location":"operations/troubleshooting/#general-debugging","title":"General Debugging","text":"<pre><code># List pods in namespace\nkubectl -n &lt;namespace&gt; get pods -o wide\n\n# Check pod logs\nkubectl -n &lt;namespace&gt; logs &lt;pod-name&gt; -f\n\n# Describe pod for events\nkubectl -n &lt;namespace&gt; describe pod &lt;pod-name&gt;\n\n# Check namespace events\nkubectl -n &lt;namespace&gt; get events --sort-by='.metadata.creationTimestamp'\n</code></pre>"},{"location":"operations/troubleshooting/#crashloopbackoff","title":"CrashLoopBackOff","text":"<ol> <li>Check logs: <code>kubectl -n &lt;ns&gt; logs &lt;pod&gt; --previous</code></li> <li>Check resource limits: <code>kubectl -n &lt;ns&gt; describe pod &lt;pod&gt;</code></li> <li>Check if NFS-dependent -- add NFS-scaler component if so</li> <li>Check if secret is missing: <code>kubectl -n &lt;ns&gt; get secrets</code></li> </ol>"},{"location":"operations/troubleshooting/#pending-pods","title":"Pending Pods","text":"<ol> <li>Check events: <code>kubectl -n &lt;ns&gt; describe pod &lt;pod&gt;</code></li> <li>Check node resources: <code>kubectl top nodes</code></li> <li>Check PVC binding: <code>kubectl -n &lt;ns&gt; get pvc</code></li> <li>Check node affinity/taints</li> </ol>"},{"location":"operations/troubleshooting/#network-issues","title":"Network Issues","text":""},{"location":"operations/troubleshooting/#cilium","title":"Cilium","text":"<pre><code>cilium status\ncilium connectivity test\n</code></pre>"},{"location":"operations/troubleshooting/#dns","title":"DNS","text":"<pre><code># Test cluster DNS\nkubectl run -it --rm debug --image=busybox -- nslookup kubernetes.default\n\n# Test external DNS resolution\ndig @&lt;k8s-gateway-ip&gt; &lt;app&gt;.&lt;domain&gt;\n</code></pre>"},{"location":"operations/troubleshooting/#storage-issues","title":"Storage Issues","text":""},{"location":"operations/troubleshooting/#nfs-unavailable","title":"NFS Unavailable","text":"<p>If NFS is down, pods using NFS volumes will crash-loop. The NFS-scaler component handles this automatically for apps that include it.</p> <p>Check NFS availability:</p> <pre><code>kubectl -n observability get prometheusrule -l app=blackbox-exporter\n</code></pre>"},{"location":"operations/troubleshooting/#pvc-issues","title":"PVC Issues","text":"<pre><code>kubectl get pvc -A\nkubectl describe pvc &lt;name&gt; -n &lt;namespace&gt;\n</code></pre>"},{"location":"operations/troubleshooting/#vm-issues","title":"VM Issues","text":""},{"location":"operations/troubleshooting/#vm-not-starting","title":"VM Not Starting","text":"<pre><code># Check VirtualMachine status\nkubectl -n kubevirt get vm &lt;vm-name&gt;\nkubectl -n kubevirt describe vm &lt;vm-name&gt;\n\n# Check VirtualMachineInstance (the running instance)\nkubectl -n kubevirt get vmi &lt;vm-name&gt;\n\n# Check CDI DataVolume import status (for new VMs)\nkubectl -n kubevirt get dv &lt;vm-name&gt;-disk\nkubectl -n kubevirt describe dv &lt;vm-name&gt;-disk\n</code></pre>"},{"location":"operations/troubleshooting/#vm-console-not-connecting","title":"VM Console Not Connecting","text":"<pre><code># Ensure virtctl is installed\nvirtctl version\n\n# Try direct console access\nvirtctl console &lt;vm-name&gt; -n kubevirt\n\n# Check VMI is in Running phase\nkubectl -n kubevirt get vmi &lt;vm-name&gt; -o jsonpath='{.status.phase}'\n</code></pre>"},{"location":"operations/troubleshooting/#vm-live-migration-fails","title":"VM Live Migration Fails","text":"<p>Requirements for live migration:</p> <ol> <li>Storage must be ReadWriteMany (NFS <code>nfs-fast</code> class)</li> <li>LiveMigration feature gate must be enabled (default)</li> <li>Sufficient resources on the target node</li> <li>No hostPath mounts</li> </ol> <pre><code># Check migration status\nkubectl -n kubevirt get vmim\nkubectl -n kubevirt describe vmim &lt;migration-name&gt;\n</code></pre>"},{"location":"operations/troubleshooting/#database-issues","title":"Database Issues","text":""},{"location":"operations/troubleshooting/#postgresql-cluster-unhealthy","title":"PostgreSQL Cluster Unhealthy","text":"<pre><code># Check cluster status\nkubectl -n database get cluster postgres\n\n# Check individual pod status\nkubectl -n database get pods -l cnpg.io/cluster=postgres\n\n# View PostgreSQL logs\nkubectl -n database logs -l cnpg.io/cluster=postgres --tail=50\n\n# Check replication status\nkubectl -n database exec -it postgres-1 -- psql -U postgres -c 'SELECT * FROM pg_stat_replication;'\n</code></pre>"},{"location":"operations/troubleshooting/#postgresql-connection-issues","title":"PostgreSQL Connection Issues","text":"<pre><code># Verify the service exists\nkubectl -n database get svc postgres-rw\n\n# Test connectivity from a debug pod\nkubectl run -it --rm pg-debug --image=postgres:17 -- \\\n  psql -h postgres-rw.database.svc.cluster.local -U postgres\n</code></pre>"},{"location":"operations/troubleshooting/#postgresql-backup-failures","title":"PostgreSQL Backup Failures","text":"<pre><code># Check scheduled backup status\nkubectl -n database get scheduledbackups\nkubectl -n database get backups --sort-by='.metadata.creationTimestamp'\n\n# Check WAL archiving\nkubectl -n database get cluster postgres -o jsonpath='{.status.firstRecoverabilityPoint}'\n</code></pre>"},{"location":"operations/troubleshooting/#certificate-issues","title":"Certificate Issues","text":""},{"location":"operations/troubleshooting/#certificate-not-ready","title":"Certificate Not Ready","text":"<pre><code># Check certificate status\nkubectl -n network get certificates\nkubectl -n network describe certificate &lt;cert-name&gt;\n\n# Check certificate requests\nkubectl get certificaterequests -A\n\n# Check cert-manager logs\nkubectl -n cert-manager logs -l app.kubernetes.io/name=cert-manager --tail=50\n</code></pre>"},{"location":"operations/troubleshooting/#common-certificate-problems","title":"Common Certificate Problems","text":"<ol> <li>DNS challenge failing: Check Cloudflare API token permissions</li> <li>Rate limited by Let's Encrypt: Wait and retry (check <code>kubectl describe certificate</code>)</li> <li>Secret not found: Verify the certificate secret name matches the TLS secret reference in your Gateway</li> </ol>"},{"location":"operations/troubleshooting/#reset-cluster","title":"Reset Cluster","text":"<p>Danger</p> <p>This destroys everything. Use as last resort.</p> <pre><code>task talos:reset\n</code></pre> <p>After reset, re-bootstrap:</p> <pre><code>task bootstrap:talos\ntask bootstrap:apps\n</code></pre>"},{"location":"operations/vm-management/","title":"VM Management","text":""},{"location":"operations/vm-management/#overview","title":"Overview","text":"<p>Virtual machines are managed by KubeVirt 1.7.0 with VM definitions stored in Git at <code>kubernetes/apps/kubevirt/virtualmachines/</code>.</p>"},{"location":"operations/vm-management/#cli-operations","title":"CLI Operations","text":""},{"location":"operations/vm-management/#using-virtctl","title":"Using virtctl","text":"<pre><code># Access VM console\nvirtctl console &lt;vm-name&gt;\n\n# SSH into VM\nvirtctl ssh &lt;vm-name&gt;\n\n# Start/stop\nvirtctl start &lt;vm-name&gt;\nvirtctl stop &lt;vm-name&gt;\n\n# Restart\nvirtctl restart &lt;vm-name&gt;\n\n# Live migrate to another node\nvirtctl migrate &lt;vm-name&gt;\n\n# Pause/unpause\nvirtctl pause vm &lt;vm-name&gt;\nvirtctl unpause vm &lt;vm-name&gt;\n</code></pre>"},{"location":"operations/vm-management/#using-task-runner","title":"Using Task Runner","text":"<pre><code>task vm:console VM=&lt;name&gt;\ntask vm:start VM=&lt;name&gt;\ntask vm:stop VM=&lt;name&gt;\n</code></pre>"},{"location":"operations/vm-management/#web-ui","title":"Web UI","text":"<p>KubeVirt Manager is available at <code>kubevirt.00o.sh</code> with Kanidm SSO authentication.</p>"},{"location":"operations/vm-management/#creating-a-new-vm","title":"Creating a New VM","text":""},{"location":"operations/vm-management/#1-create-the-manifest","title":"1. Create the Manifest","text":"<p>Create a directory under <code>kubernetes/apps/kubevirt/virtualmachines/&lt;vm-name&gt;/</code>:</p> <pre><code>apiVersion: kubevirt.io/v1\nkind: VirtualMachine\nmetadata:\n  name: my-vm\nspec:\n  running: true\n  template:\n    spec:\n      domain:\n        cpu:\n          cores: 2\n        memory:\n          guest: 2Gi\n        devices:\n          disks:\n            - name: rootdisk\n              disk:\n                bus: virtio\n          interfaces:\n            - name: default\n              macvtap: {}\n              macAddress: \"XX:XX:XX:XX:XX:XX\"\n      networks:\n        - name: default\n          multus:\n            networkName: macvtap-net\n      volumes:\n        - name: rootdisk\n          persistentVolumeClaim:\n            claimName: my-vm-disk\n</code></pre>"},{"location":"operations/vm-management/#2-create-storage","title":"2. Create Storage","text":"<p>Use CDI DataVolume to import a disk image:</p> <pre><code>apiVersion: cdi.kubevirt.io/v1beta1\nkind: DataVolume\nmetadata:\n  name: my-vm-disk\nspec:\n  source:\n    http:\n      url: \"https://cloud-images.ubuntu.com/...\"\n  storage:\n    accessModes:\n      - ReadWriteMany\n    storageClassName: nfs-fast\n    resources:\n      requests:\n        storage: 50Gi\n</code></pre>"},{"location":"operations/vm-management/#3-configure-networking","title":"3. Configure Networking","text":"<p>Each VM gets a macvtap interface with a dedicated MAC address for direct L2 network access. Add an external-dns annotation for DNS.</p>"},{"location":"operations/vm-management/#4-add-to-kustomization","title":"4. Add to Kustomization","text":"<p>Reference the new VM in <code>kubernetes/apps/kubevirt/virtualmachines/kustomization.yaml</code>.</p>"},{"location":"operations/vm-management/#live-migration","title":"Live Migration","text":"<p>VMs using NFS storage (<code>nfs-fast</code>) support live migration:</p> <pre><code>virtctl migrate &lt;vm-name&gt;\n</code></pre> <p>Requirements:</p> <ul> <li>ReadWriteMany storage (NFS)</li> <li>LiveMigration feature gate enabled (default)</li> <li>Sufficient resources on target node</li> </ul>"},{"location":"operations/vm-management/#freepbx-vms","title":"FreePBX VMs","text":"<p>Three FreePBX telephony instances are deployed:</p> <ul> <li><code>freepbx-b1-k3s01</code></li> <li><code>freepbx-b2-k3s01</code></li> <li><code>freepbx-b3-k3s01</code></li> </ul> <p>Each has dedicated SOPS-encrypted secrets.</p>"},{"location":"research/","title":"Research","text":"<p>Technical research and analysis documents for cluster design decisions.</p>"},{"location":"research/#documents","title":"Documents","text":"<ul> <li>SSO Providers -- Comparison of identity providers for the Kubernetes homelab (Authelia, Authentik, Keycloak, Zitadel, Kanidm)</li> </ul>"},{"location":"research/sso-providers/","title":"SSO Provider Research for Kubernetes Homelab (GitOps-Friendly)","text":"<p>Researched 2026-02-07. Stack: Flux CD, Envoy Gateway, CloudNative-PG, Talos Linux, onedr0p/cluster-template.</p>"},{"location":"research/sso-providers/#recommendation-authelia","title":"Recommendation: Authelia","text":"<p>Authelia is the best fit for this cluster due to native Envoy Gateway support, fully declarative YAML configuration (GitOps-native), minimal resource usage (~30 MB RAM), and strong adoption in the cluster-template community.</p>"},{"location":"research/sso-providers/#comparison-matrix","title":"Comparison Matrix","text":"Feature Authelia Authentik Keycloak Zitadel Kanidm RAM (baseline) ~30 MB ~600 MB-1 GB ~400+ MB ~512 MB-1 GB ~80 MB OCI Helm Chart No (traditional repo) Yes (ghcr.io) Yes (Bitnami) No (traditional) Yes (Kaniop) Envoy Gateway ext-auth Native (first-class) Supported (minor bug) Via oauth2-proxy Via oauth2-proxy Via oauth2-proxy or native OIDC SecurityPolicy Forward Auth Native (core feature) Native (proxy outpost) Via oauth2-proxy Via oauth2-proxy Via oauth2-proxy GitOps Config Excellent (YAML) Mixed (GUI + API) Partial (config-cli) API/Terraform Kaniop CRDs + CLI bootstrap PostgreSQL Optional Required Required Required Not needed (embedded DB) OIDC Provider Yes (certified) Yes Yes Yes Yes (PKCE mandatory) SAML No Yes Yes Via brokering No LDAP Server No (pairs with LLDAP) Yes (built-in) Yes (federation) No Read-only gateway RADIUS No No No No Built-in (FreeRADIUS wrapper) SSH Key Distribution No No No No Built-in (cached + direct) Unix/PAM Integration No No No No Built-in (TPM-backed) Passkeys/WebAuthn Yes Yes Limited Yes Best-in-class Admin UI None (YAML files) Full web UI Full web UI Full web UI None (CLI) Homelab Popularity Very High High Medium Low Growing Gateway API Support v4.37+ 2025.4+ No No No"},{"location":"research/sso-providers/#detailed-analysis","title":"Detailed Analysis","text":""},{"location":"research/sso-providers/#authelia-recommended","title":"Authelia (Recommended)","text":"<ul> <li>Version: v4.39+, OpenID Certified, Apache 2.0</li> <li>Helm: <code>https://charts.authelia.com</code> (traditional repo, not OCI). Most homelab users deploy via bjw-s app-template chart.</li> <li>Envoy Gateway: First-class support since v4.37.0 with dedicated documentation. Uses <code>SecurityPolicy</code> CRD with <code>extAuth.http</code> pointing to <code>/api/authz/ext-authz/</code>. Supports per-HTTPRoute and per-Gateway scoping.</li> <li>GitOps: All configuration (OIDC clients, access rules, users, MFA) is YAML file-driven. No GUI. Diffs cleanly in PRs.</li> <li>Resources: ~20 MB image, ~25-30 MB RAM. SQLite built-in (sufficient for homelab), optionally PostgreSQL + Redis for HA.</li> <li>App Integration: Forward auth protects apps without native OIDC (Sonarr, Radarr, Prowlarr, qBittorrent). OIDC for Grafana.</li> <li>Community: Most popular in onedr0p/cluster-template repos (coolguy1771/home-ops, AskAlice/lakewood-ops, bbangert/homelab-gitops).</li> </ul> <p>Cluster readiness: Prowlarr, Radarr, and Sonarr already configured with <code>AUTH__METHOD: External</code>.</p>"},{"location":"research/sso-providers/#authentik-runner-up","title":"Authentik (Runner-up)","text":"<ul> <li>Version: v2025.12.3, actively maintained</li> <li>Helm: OCI chart at <code>oci://ghcr.io/goauthentik/helm/authentik</code> (fits OCIRepository pattern)</li> <li>Envoy Gateway: Supported via proxy outpost on port 9000. Known X-Forwarded-Proto bug with mobile browsers (fix expected). Gateway API HTTPRoute support added in 2025.4.</li> <li>GitOps: Mixed. Deployment is GitOps-compatible but app-level config (flows, providers, OIDC clients) lives in database. REST API and Terraform provider available for automation.</li> <li>Resources: ~600 MB-1 GB RAM, 2 containers (server + worker). Redis dependency removed in 2025.10.</li> <li>Extra features: Web admin UI, built-in LDAP server, SAML, Plex social login, browser-based RDP/SSH/VNC (RAC).</li> <li>Community: Well-represented in homelab Flux repos. Can share existing CloudNative-PG cluster.</li> </ul> <p>Choose Authentik if you need a web UI, LDAP, SAML, or Plex integration.</p>"},{"location":"research/sso-providers/#keycloak-not-recommended","title":"Keycloak (Not recommended)","text":"<ul> <li>Enterprise-grade, heaviest option (~400+ MB RAM, JVM)</li> <li>No native Envoy Gateway support (requires oauth2-proxy)</li> <li>Partially GitOps-compatible via keycloak-config-cli</li> <li>Overkill for homelab use</li> </ul>"},{"location":"research/sso-providers/#zitadel-not-recommended","title":"Zitadel (Not recommended)","text":"<ul> <li>No forward auth support (requires oauth2-proxy bridge)</li> <li>No OCI Helm chart</li> <li>Low homelab adoption</li> <li>More suited to SaaS/startup environments</li> </ul>"},{"location":"research/sso-providers/#kanidm-worth-considering-if-radius-is-needed","title":"Kanidm (Worth considering if RADIUS is needed)","text":"<ul> <li>Version: v1.8.x, MPL-2.0, ~4,474 GitHub stars, 106+ contributors, quarterly releases</li> <li>Helm: OCI chart via Kaniop operator at <code>oci://ghcr.io/pando85/helm-charts/kaniop</code></li> <li>Architecture: Single Rust binary with embedded database. No external PostgreSQL/Redis needed. ~80 MB RAM.</li> <li>OIDC: Built-in provider with mandatory PKCE, ES256 tokens (legacy RS256 available), scope/claim maps, RFC 9068 JWT</li> <li>RADIUS: Built-in container wrapping FreeRADIUS with Kanidm backend. WPA2-Enterprise (MSCHAPv2/PEAP), EAP-TLS, group-to-VLAN mapping. Isolated per-service credentials.</li> <li>Passkeys/WebAuthn: Best-in-class. Maintains <code>webauthn-rs</code> (used in Firefox). Security-audited by SUSE. Attestation support for restricting authenticator models.</li> <li>SSH keys: Built-in distribution with cached mode (survives network outages) and direct mode</li> <li>Unix/POSIX: PAM + nsswitch daemon with TPM-backed credential caching, auto home directory creation</li> <li>LDAP: Read-only gateway (intentional design -- not a write-capable LDAP server)</li> <li>No SAML, no upstream OIDC federation, no native forward-auth</li> </ul> <p>Kaniop Kubernetes Operator (v0.4.1): - CRDs: <code>Kanidm</code>, <code>KanidmPersonAccount</code>, <code>KanidmGroup</code>, <code>KanidmOAuth2Client</code>, <code>KanidmServiceAccount</code> - All declarable in Git and managed by Flux - Supports HA with 2-node replication (all nodes accept writes, eventually consistent) - ~50 GitHub stars -- early-stage but functional for homelab</p> <p>GitOps caveats: - OAuth2 client secrets are generated by Kanidm, not settable from Git (need bootstrap step or ExternalSecrets) - Initial admin recovery requires imperative <code>kanidm recover-account admin</code> command - User credentials (passkeys, passwords, RADIUS) are self-managed by users via WebUI - Admin operations are CLI-only (no web admin panel)</p> <p>Envoy Gateway integration (two options): 1. oauth2-proxy as ext-auth -- deploy oauth2-proxy with Kanidm as OIDC provider, use SecurityPolicy extAuth.http 2. Envoy Gateway native OIDC SecurityPolicy -- eliminates oauth2-proxy entirely, but less flexible for header injection</p> <p>Replaces: LLDAP + Authelia (single service for OIDC + user store), standalone FreeRADIUS</p> <p>Choose Kanidm if: You need RADIUS (WiFi WPA2-Enterprise, 802.1X, VPN auth), SSH key distribution, or Unix/PAM integration alongside OIDC. Accept the trade-off of needing oauth2-proxy for non-OIDC apps and being an early Kaniop adopter.</p>"},{"location":"research/sso-providers/#envoy-gateway-v170-blocker","title":"Envoy Gateway v1.7.0 Blocker","text":"<p>All SSO providers are affected. Envoy Gateway v1.7.0 (bundled Envoy Proxy v1.37.0) strips the <code>Location</code> header from ext-auth denial responses, breaking redirect-based auth flows. See envoyproxy/gateway#8202.</p> <p>Workarounds: 1. Pin Envoy Gateway to v1.6.x until the fix lands 2. Use EnvoyPatchPolicy to inject <code>allowed_client_headers</code> (workaround, if available) 3. Wait for v1.7.1 or v1.8.0</p> <p>The proposed fix adds a <code>headersToDownstream</code> field to <code>SecurityPolicy</code>. Status: open, triaged.</p>"},{"location":"research/sso-providers/#implementation-notes-for-this-cluster","title":"Implementation Notes for This Cluster","text":""},{"location":"research/sso-providers/#authelia-deployment-path","title":"Authelia deployment path","text":"<ol> <li>Deploy Authelia in a new <code>auth</code> or <code>security</code> namespace</li> <li>Use bjw-s app-template chart (most common in cluster-template repos) or official Helm chart</li> <li>Configure Envoy Gateway <code>SecurityPolicy</code> with <code>extAuth.http</code> pointing to Authelia</li> <li>OIDC clients for Grafana defined in Authelia's YAML config</li> <li>Forward auth for *arr apps via ext-authz endpoint</li> <li>User storage: YAML file (small homelab) or LLDAP (if LDAP needed)</li> <li>Database: SQLite (simplest) or CloudNative-PG (if HA required)</li> </ol>"},{"location":"research/sso-providers/#envoy-gateway-securitypolicy-example","title":"Envoy Gateway SecurityPolicy example","text":"<pre><code>apiVersion: gateway.envoyproxy.io/v1alpha1\nkind: SecurityPolicy\nmetadata:\n  name: authelia-auth\nspec:\n  targetRefs:\n    - group: gateway.networking.k8s.io\n      kind: HTTPRoute\n      name: &lt;app-route&gt;\n  extAuth:\n    http:\n      backendRef:\n        name: authelia\n        namespace: auth\n        port: 9091\n      headersToBackend:\n        - cookie\n        - authorization\n      path: /api/authz/ext-authz/\n</code></pre>"},{"location":"research/sso-providers/#kanidm-deployment-path","title":"Kanidm deployment path","text":"<ol> <li>Deploy Kaniop operator via HelmRelease (<code>oci://ghcr.io/pando85/helm-charts/kaniop</code>)</li> <li>Declare <code>Kanidm</code> cluster, <code>KanidmPersonAccount</code>, <code>KanidmGroup</code>, <code>KanidmOAuth2Client</code> CRDs in Git</li> <li>Bootstrap: run <code>kanidm recover-account admin</code> once to set initial credentials</li> <li>For OIDC apps (Grafana, Penpot, kubevirt-manager): configure OAuth2 clients via Kaniop CRDs</li> <li>For non-OIDC apps (*arr stack): deploy oauth2-proxy with Kanidm as OIDC provider, or use Envoy Gateway native OIDC SecurityPolicy</li> <li>For RADIUS: deploy <code>kanidm/radius</code> container, configure WiFi APs as RADIUS clients</li> <li>OAuth2 secrets: retrieve via <code>kanidm system oauth2 show-basic-secret</code>, store in SOPS-encrypted secrets</li> </ol>"},{"location":"research/sso-providers/#envoy-gateway-native-oidc-securitypolicy-example-no-oauth2-proxy-needed","title":"Envoy Gateway native OIDC SecurityPolicy example (no oauth2-proxy needed)","text":"<pre><code>apiVersion: gateway.envoyproxy.io/v1alpha1\nkind: SecurityPolicy\nmetadata:\n  name: kanidm-oidc\nspec:\n  targetRefs:\n    - group: gateway.networking.k8s.io\n      kind: HTTPRoute\n      name: &lt;app-route&gt;\n  oidc:\n    provider:\n      issuer: \"https://idm.${SECRET_DOMAIN}/oauth2/openid/&lt;client-name&gt;\"\n    clientID: \"&lt;client-name&gt;\"\n    clientSecret:\n      name: \"&lt;client-secret-name&gt;\"\n    redirectURL: \"https://&lt;app&gt;.${SECRET_DOMAIN}/oauth2/callback\"\n</code></pre>"},{"location":"research/sso-providers/#references","title":"References","text":"<ul> <li>Authelia Envoy Gateway Integration</li> <li>Authelia OIDC for Envoy Gateway</li> <li>Authelia Grafana OIDC Guide</li> <li>Authentik Helm Chart (OCI)</li> <li>Authentik Envoy Docs</li> <li>Authentik Redis Removal (2025.10)</li> <li>Envoy Gateway External Authorization</li> <li>Envoy Gateway OIDC Documentation</li> <li>Envoy Gateway v1.7.0 ext-auth redirect bug</li> <li>Kanidm OAuth2 Documentation</li> <li>Kanidm RADIUS Documentation</li> <li>Kanidm SSH Key Distribution</li> <li>Kaniop Kubernetes Operator</li> <li>Kanidm Forward Auth Feature Request</li> <li>kubesearch.dev - Authelia deployments</li> <li>kubesearch.dev - Authentik deployments</li> </ul>"}]}