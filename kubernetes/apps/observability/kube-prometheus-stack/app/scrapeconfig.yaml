---
# NAS scrape configs disabled — node_exporter and smartctl_exporter were
# never installed on the NAS, so scraping them just produces persistent
# TargetDown alerts. Re-enable after installing on the NAS:
#   - node_exporter:     https://github.com/prometheus/node_exporter
#   - smartctl_exporter: https://github.com/prometheus-community/smartctl_exporter
#
# apiVersion: monitoring.coreos.com/v1alpha1
# kind: ScrapeConfig
# metadata:
#   name: node-exporter
# spec:
#   staticConfigs:
#     - targets: ["nas.3226texas.com:9100"]
#   metricsPath: /metrics
#   relabelings:
#     - { action: replace, targetLabel: job, replacement: node-exporter }
# ---
# apiVersion: monitoring.coreos.com/v1alpha1
# kind: ScrapeConfig
# metadata:
#   name: smartctl-exporter
# spec:
#   staticConfigs:
#     - targets: ["nas.3226texas.com:9633"]
#   metricsPath: /metrics
#   relabelings:
#     - { action: replace, targetLabel: job, replacement: smartctl-exporter }
#
# Note: Removed JetKVM scrape configs (onedr0p-specific hardware)
