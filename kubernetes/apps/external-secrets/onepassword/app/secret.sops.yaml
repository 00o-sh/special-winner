apiVersion: v1
kind: Secret
metadata:
    name: onepassword-secret
    namespace: external-secrets
type: Opaque
stringData:
    # 1Password Service Account token
    # Create with: op service-account create "ESO" --expires-in 90d --vault Kubernetes
    token: <REPLACE_WITH_SERVICE_ACCOUNT_TOKEN>
