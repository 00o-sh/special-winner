---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # cdi-operator.yaml is a build artifact (release-only), not in the
  # upstream git tree, so kustomize/flux-local can't fetch it remotely.
  # Vendored at v1.65.0 from:
  #   https://github.com/kubevirt/containerized-data-importer/releases/download/v1.65.0/cdi-operator.yaml
  # To bump: download the new release asset and replace this file.
  - ./cdi-operator.yaml
  - ./cdi.yaml
