---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: utils

components:
  - ../../components/alerts

resources:
  - ./namespace.yaml
  - ./smtp-relay/ks.yaml
#  - ./penpot/ks.yaml waiting on cloudnative PG cluster to be up make sure secrets are setup first